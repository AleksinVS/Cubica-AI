# PROJECT STRUCTURE
# =================
# AUTO-GENERATED FILE - DO NOT EDIT MANUALLY.
# Use `node scripts/dev/generate-structure.js` to update.
# This file provides a machine-readable overview of the repository layout.
# Note: 'draft', 'sandbox', '.tmp' and 'node_modules' are excluded.
# Note: ONLY documented files and directories (via .desc.json or package.json) are shown.

.github: # GitHub repository automation configuration, including required CI workflows.
  workflows: # GitHub Actions workflows for canonical validation and legacy/stub governance gates.
    ci.yml: file # Required CI workflow for legacy/stub gate, canonical verification, portal launch rule tests and player-web e2e tests.
  required-checks.md: file # Repository ruleset and branch protection notes for required CI checks.
apps: # Приложения и импортированные frontend-заготовки 
  player-web: # Канонический web player scaffold для Antarctica 
    e2e: # Playwright end-to-end tests for browser-level player-web, runtime-api and portal launch binding flows.
      player-web.spec.ts: file # E2E coverage for normal Antarctica session boot, runtime action dispatch and portal launch session binding.
  portal-nextjs: directory... # Next.js портал для каталога, покупок, ссылок запуска и первого test VPS launch с Antarctica.
archive: directory... # Архив старых документов и черновиков 
data: directory... # Фикстуры и моки данных 
docs: # Архитектура, задачи, планирование 
  architecture: # Архитектурные документы, ADR, схемы
    schemas: # JSON Schema contracts for runtime manifests, authoring manifests, design artifacts and related architecture data.
      game-authoring.schema.json: file # JSON Schema for editable game authoring manifests compiled into runtime game.manifest.json files.
      manifest-authoring-common.schema.json: file # Shared JSON Schema definitions for ADR-030 authoring manifests and source maps.
      manifest-source-map.schema.json: file # JSON Schema for tooling-only source map companion files generated next to runtime manifests.
      ui-authoring.schema.json: file # JSON Schema for editable UI authoring manifests compiled into channel-specific ui.manifest.json files.
  legacy:
    debt-log.csv: file # Центральный реестр долга (tech debt).
    stub-marker-allowlist.json: file # Документированные исключения для CI-проверки маркеров заглушек с владельцем, причиной и сроком пересмотра.
    stubs-register.md: file # Реестр временных заглушек.
  tasks: # Легковесная система задач и планов: текущие рабочие задачи, архив старой иерархии и постоянные артефакты задач.
    active: # Активные рабочие задачи проекта. Каждый TSK-файл объединяет план, критерии приемки, проверки, артефакты и журнал передачи.
      TSK-20260518-architecture-repair-and-task-system-migration.md: file # Активный план восстановления архитектурных проверок и завершения миграции системы задач на ADR-031.
      TSK-20260518-contracts-neutrality-cleanup.md: file # План очистки общего contracts layer от game-specific drift без потери Antarctica coverage.
      TSK-20260518-json-schema-strict-validation.md: file # План перевода manifest validation на строгий Ajv-режим без императивного дрейфа.
      TSK-20260518-portal-test-vps-and-antarctica-launch.md: file # План подготовки portal launch surface, test VPS и запуска Antarctica через ссылки и runtime sessions.
      TSK-20260518-runtime-repository-boundary-and-readiness.md: file # План укрепления runtime repository boundary и честной readiness-проверки game content.
      TSK-20260518-session-persistence-hardening.md: file # План production-ready session persistence вместо runtime-only in-memory store.
      TSK-20260518-workspace-project-references-cleanup.md: file # План классификации workspace, scaffold и TypeScript project reference gaps.
      TSK-20260520-project-review-remediation.md: file # Планово-исполнительская задача по исправлению находок ревью проекта от 2026-05-20.
      TSK-20260521-game-agnostic-architecture-simplification.md: file # План упрощения создания новых игр и снятия Antarctica-specific архитектурных утечек из платформенного слоя.
      TSK-20260521-semantic-prototype-authoring-layer.md: file # План внедрения обязательного authoring-слоя манифестов с компиляцией в runtime game/UI manifests.
    archive: # Архив задач: старая иерархия milestones/epics/features/content-packs и завершенные TSK-файлы после внедрения новой системы.
      README-legacy-task-system.md: file # Старые правила многоуровневой системы задач, сохраненные для исторического контекста.
    artifacts: directory... # Постоянные артефакты рабочих задач. Для каждой задачи используется подкаталог с идентификатором задачи.
    README.md: file # Правила текущей системы задач, планов и передачи артефактов.
    ROADMAP.md: file # Историческая дорожная карта старой системы планирования; не используется как текущая доска.
    STRATEGY.md: file # Стратегический слой планирования: горизонты, стратегические темы и правила приоритизации.
games: # Пакеты игр и связанные ресурсы 
  antarctica: directory... # Пакет игры «Antarctica» (канонический content layer) 
  simple-choice: # Minimal game-agnostic fixture used to prove that runtime-api and player-web can run a second game without a custom web plugin.
    authoring: # Editable ADR-030 authoring manifests for the simple-choice pilot. Runtime manifests are generated from these files and should not be edited directly.
      game.authoring.json: file # Authoring source for the generated simple-choice game manifest.
packages: # Переиспользуемые платформенные пакеты (contracts) 
  contracts: # Слой общих контрактов между сервисами 
    ai: directory... # AI task, result and eval contracts for Cubica.
    manifest: directory... # Manifest and content contracts for Cubica.
    runtime: directory... # Runtime execution contracts for Cubica.
    session: directory... # Session lifecycle and player API contracts for Cubica.
scripts: # Скрипты разработки и индексирования 
  ci: # Cross-platform CI validation scripts that enforce repository governance rules before merge.
    validate-legacy.js: file # Node.js validator for legacy debt rows, stub register consistency, stub marker governance and .desc.json parsing.
    validate-legacy.ps1: file # PowerShell wrapper that delegates legacy/stub validation to the cross-platform Node.js validator.
    validate-manifest-authoring.js: file # Node.js validator for ADR-030 authoring manifest compilation, generated drift and authoring-only key leakage.
  debug: directory... # Поддерживаемые debug-скрипты для локальной проверки UI, журналов и визуального сравнения. Скрипты не являются частью canonical verification и используются вручную при расследовании регрессий.
  manifest-tools: # Manifest transformation and generation tools used by development and CI workflows.
    compile-authoring-manifests.cjs: file # Compiler for ADR-030 authoring manifests that generates runtime manifests and source map companion files.
SDK: # SDK и вспомогательные библиотеки 
  core: directory... # Базовые компоненты SDK и общая логика 
  react-sdk: directory... # SDK для React/Next.js‑приложений 
  shared: directory... # Общие UI‑компоненты и утилиты 
  viewers: # Библиотека плееров для отображения игр 
    web-base: directory... # Базовый веб-viewer для платформы Cubica. Содержит переиспользуемые компоненты для загрузки манифестов, управления состоянием и маршрутизации действий.
services: # Сервисы и инфраструктура 
  game-engine: directory... # Service boundary for a future game engine service. The current canonical runtime path executes deterministic manifest actions in services/runtime-api.
  game-repository: directory... # Service boundary for a future game repository. The current canonical runtime loads game content through services/runtime-api.
  portal-backend: # Strapi backend портала для каталога, заказов, покупок, платежной заглушки и launch-session API.
    test: directory... # Focused backend tests that can run without a Strapi database; currently covers pure portal launch-session rule helpers.
  router: directory... # Service boundary for router scaffold. The current canonical runtime path is owned by services/runtime-api until this boundary is reclassified or extracted.
  runtime-api: directory... # Целевой единый deployable backend (модульный монолит) 
.gitignore: file # Правила исключения локальных зависимостей, сборочных и тестовых артефактов из Git.
AGENTS.md: file # Правила работы ИИ‑агентов
NEXT_STEPS.md: file # Ближайшие инженерные шаги и приоритеты миграции
package-lock.json: file # Lockfile корневого npm-workspace
package.json: file # Корневой npm-workspace
playwright.config.ts: file # Конфигурация Playwright e2e-тестов для browser-level проверки player-web и runtime-api.
PROJECT_OVERVIEW.md: file # Общий обзор проекта
PROJECT_STRUCTURE.yaml: file # Описание структуры репозитория (этот файл)
PROJECT_WORKFLOW_CONFIG.json: file # Конфигурация workflow-инструментов, поддерживаемая как корневой tool entry point
README.md: file # Основное описание репозитория
