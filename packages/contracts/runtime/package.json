{
  "name": "@cubica/contracts-runtime",
  "version": "0.0.0",
  "private": true,
  "description": "Runtime execution contracts for Cubica.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo \"TODO: add contracts-runtime build\" && exit 1",
    "test": "echo \"TODO: add contracts-runtime tests\" && exit 1",
    "lint": "echo \"TODO: add contracts-runtime lint\" && exit 1"
  },
  "engines": {
    "node": ">=18"
  }
}
