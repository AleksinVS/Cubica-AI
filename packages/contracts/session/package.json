{
  "name": "@cubica/contracts-session",
  "version": "0.0.0",
  "private": true,
  "description": "Session lifecycle and player API contracts for Cubica.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo \"TODO: add contracts-session build\" && exit 1",
    "test": "echo \"TODO: add contracts-session tests\" && exit 1",
    "lint": "echo \"TODO: add contracts-session lint\" && exit 1"
  },
  "engines": {
    "node": ">=18"
  }
}
