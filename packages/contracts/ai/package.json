{
  "name": "@cubica/contracts-ai",
  "version": "0.0.0",
  "private": true,
  "description": "AI task, result and eval contracts for Cubica.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo \"TODO: add contracts-ai build\" && exit 1",
    "test": "echo \"TODO: add contracts-ai tests\" && exit 1",
    "lint": "echo \"TODO: add contracts-ai lint\" && exit 1"
  },
  "engines": {
    "node": ">=18"
  }
}
