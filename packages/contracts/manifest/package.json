{
  "name": "@cubica/contracts-manifest",
  "version": "0.0.0",
  "private": true,
  "description": "Manifest and content contracts for Cubica.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo \"TODO: add contracts-manifest build\" && exit 1",
    "test": "echo \"TODO: add contracts-manifest tests\" && exit 1",
    "lint": "echo \"TODO: add contracts-manifest lint\" && exit 1"
  },
  "engines": {
    "node": ">=18"
  }
}
