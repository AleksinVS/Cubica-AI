{
  "name": "@cubica/contracts-manifest",
  "version": "0.0.0",
  "private": true,
  "description": "Manifest and content contracts for Cubica.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo \"[contracts-manifest] source-only TS package; no build step\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "lint": "echo \"[contracts-manifest] no lint yet (see TSK-20260630-manifest-contract-parity)\""
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  },
  "engines": {
    "node": ">=22.7.0"
  }
}
