{
  "description": "Eval fixtures for the returned-intent interpreter (ADR-038 replay/eval corpus, design-spec §6): each case maps an authored text edit to the expected fast-path/agent decision, report buckets, and deterministic ChangeSet. Extend by adding one JSON file."
}
