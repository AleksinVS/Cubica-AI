{
  "name": "@cubica/view-protocol",
  "version": "0.0.0",
  "private": true,
  "description": "Abstract View Protocol (ADR-002) contracts and JSON state patch utilities shared by Cubica delivery channels (ADR-064).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo \"[view-protocol] source-only TS package consumed via src; no build step\"",
    "test": "vitest run",
    "lint": "echo \"[view-protocol] no dedicated lint step\""
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  },
  "keywords": [
    "cubica",
    "view-protocol"
  ],
  "engines": {
    "node": ">=22.7.0"
  }
}
