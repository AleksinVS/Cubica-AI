'use strict';

/**
 * session-launch-event service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::session-launch-event.session-launch-event');
