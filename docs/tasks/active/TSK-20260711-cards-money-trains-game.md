# TSK-20260711-cards-money-trains-game: Игра «Карты, деньги, поезда»

## Оглавление

- [Status](#status)
- [Parent](#parent)
- [Understanding](#understanding)
- [Why](#why)
- [Product Sources](#product-sources)
- [Confirmed Product Decisions](#confirmed-product-decisions)
- [Architecture](#architecture)
- [Scope](#scope)
- [Non-Goals](#non-goals)
- [Plan Approval](#plan-approval)
- [Plan](#plan)
- [Execution Decomposition](#execution-decomposition)
- [Execution](#execution)
- [Acceptance](#acceptance)
- [Validation](#validation)
- [Artifacts](#artifacts)
- [Risks](#risks)
- [Plan Amendments](#plan-amendments)
- [Handoff Log](#handoff-log)

## Status

in_progress

Status note: пакет `cards-money-trains-mock` реализует полный 88-шаговый runtime-сценарий на вымышленных данных, постоянное хранение, защиту повторного действия и канонический replay. Текущее ревью сохраняет открытыми полную интерфейсную приёмку фаз и известные игровые края, поэтому mock-блок имеет статус `implemented-with-findings`. Общий декларативный режим `map-first` уже реализован: разрешённая авторская WebP-карта совпадает с рабочей областью, а статус, закрываемые панели, действия и камера лежат поверх неё без прокрутки документа. Нормативная плоскость приведена к исходному размеру карты `5079 × 3627`. Из начальной сети извлечены 25 точек и 10 дорог; они хранятся как проверяемый `review-draft`, имеют контрольное наложение и технически не могут попасть в runtime до четырёх подтверждений автора. Общий schema-first импорт доказан нейтральной фикстурой и переиспользуется mock-пакетом. В AI подтверждены отдельные векторные слои и точные пути; следующий содержательный шаг — классификация и замыкание контуров областей. В `R-07` остаётся судьба техники/грузов исключённой команды, в `R-09` — конкретные единицы и срок изъятия. Автоматическая дорога, свободный выбор узлов и точки полустанка, read-only расчёт маршрута и стоимости и открытие обычного строительства в начале `N+2` реализованы на нейтральных и тестовых данных. Нормативный GSR-032 не подключает реальные контуры до их содержательной проверки.

## Parent

none

## Understanding

Работа понята так: реализовать «Карты, деньги, поезда» как эталонную фасилитируемую бизнес-игру Cubica, начиная с подготовки обычной партии и первого реального хода и постепенно доводя один континент до полной партии, которую завершает ведущий без заранее заданного лимита ходов. Отдельный демонстрационный ход отменён автором. Основой общего экрана является масштабируемая авторская карта; управление, карточки и методика должны помогать работать с ней, а не вытеснять её маленьким холстом. Параллельно закрываются только те общие архитектурные пробелы, без которых очередной срез нельзя реализовать чисто. Учебная методика, работа ведущего, рефлексия и объяснимость последствий являются обязательной частью продукта, а не документацией «после игры».

## Why

Игра проверяет стратегические свойства платформы на реальном сценарии:

- развитие от учебной методики к исполнимым правилам;
- разные типы взаимозависимых команд в одной сессии;
- общий изменяемый рынок и транспортная сеть;
- сочетание переговоров человека и проверяемых цифровых операций;
- ведущий, помощь ассистента через общий экран и методические паузы;
- отделение универсальных механизмов от контента конкретной игры;
- один источник истины логики в манифестах и переносимая UI-проекция.

Пользовательский результат первой нормативной очереди: ведущий может подготовить обычную партию на авторской карте, провести первый реальный ход и объяснить участникам все изменения через журнал. Результат полной одноконтинентальной очереди: очная группа проходит полный игровой день без ручного пересчёта состояния вне системы, кроме живых переговоров и методической работы ведущего.

## Product Sources

При конфликте источников действует порядок:

1. `draft/trains/Копия Сценарий Карты Деньги Поезда 040225.docx` — абсолютный приоритет по прямому указанию PM.
2. Явные ответы автора, переданные PM, — уточнения цифровой версии, если они не противоречат главному сценарию.
3. `draft/trains/Начальная транспортная сеть.png`, `Игровая Карта.png`, `Карта Гвиней  а4.ai/.pdf` — сеть, визуальная карта и точные контуры.
4. `draft/trains/Колоди + Новости.xlsx` и `Описания стран Гвинеи 1 стол.docx` — колоды, эффекты и учебное описание стран.
5. `draft/trains/Инструкция КДП — исправленная.pdf` — дополнительные правила.
6. `draft/trains/presentation.pptx` — объяснение и визуальная демонстрация.

Сверка, расхождения и недостающие данные: `docs/reviews/2026-07-11-cards-money-trains-source-analysis.md`.

После реализации источником истины исполнимой логики становятся authoring-манифесты игры и их скомпилированный runtime output. Исходники в `draft/` остаются provenance/reference и не читаются runtime.

## Confirmed Product Decisions

- Первая версия работает на одном общем экране ведущего.
- Начальная сеть, контуры, колоды и описания стран получены; их ещё нужно перевести в проверяемые authoring-данные.
- Дорога стоит 2 монеты за каждый сегмент в пределах области; граница области начинает новый сегмент.
- Штрафов за ошибочную отчетность нет.
- Новости выбираются случайно воспроизводимым runtime-механизмом.
- Заранее заданного числа ходов нет; ведущий завершает сессию явной кнопкой с подтверждением.
- Второй континент не нужен и исключен из продукта.
- Отдельных демонстрационных грузов и контрольного хода в электронной версии нет.
- Локомотивы двигаются с востока на запад, начиная с самого правого на карте.
- Игроки выбирают концы дороги, а система должна автоматически выбрать траекторию с минимальным числом пересекаемых областей; намеренно длинные варианты не поддерживаются.
- Полустанок можно поставить на любую существующую дорогу с минимальным отступом «одна область» от каждого конца.
- Полустанок нельзя поставить ни в одной области, содержащей любой конец исходной дороги; обычное строительство хода `N` открывается в начале `N+2`.
- Все дороги двусторонние; новость № 12 закрывает только `3 ↔ 3,14`, а любое закрытие запрещает въезд, выезд, проезд, загрузку и выгрузку.
- Равенство восточного положения локомотивов разрешается по деньгам команды, затем числу её локомотивов, затем воспроизводимым случайным выбором.
- Phaser принят для поля в гибридной границе ADR-073.
- Принят общий декларативный режим ADR-080: карта занимает весь игровой
  viewport, а статус, команды, контекст, действия и методика располагаются
  ограниченными смысловыми слоями поверх неё.
- Подтверждён гибридный формат карты: WebP остаётся художественной runtime-подложкой, векторные источники дают отдельную геометрию областей и связей; полная SVG-подложка внутри Phaser не используется.
- Одна локальная роль `facilitator`; ассистент использует тот же экран без отдельной идентичности, свободные персональные заметки не сохраняются.
- Ведущий строит дорогу между двумя существующими станциями/полустанками либо полустанок точкой на существующей дороге; может повторять действия и отдельно завершает фазу.
- Старый режим платформы строит прямой отрезок и сохраняется для совместимости. Для этой игры принято отдельное явно включаемое правило ADR-081: сервер строит траекторию с минимумом областей и сохраняет её ломаную геометрию.
- Автоматические кредиты и отрицательный баланс запрещены; техника продается только в рыночной фазе, недостаток денег отклоняет добровольное действие. Для обязательного платежа ведущий выбирает исключение команды либо отдельный журналируемый займ с возможностью позднего взыскания и процентов.
- Подтверждены длительности и эффекты новостей № 14, 25, 26, 27 и 34 и добавляемых грузовых карт; у № 19 подтверждено количество `floor(техника/5)`, но не конкретный выбор и срок изъятия.
- Обычный старт: 10 монет каждой команде, 2 вагона логистической компании, 1 локомотив паровозной гильдии.
- Актуальная форма денежного эффекта использует `from.scope`/`to.scope` и `onInsufficient: "fail"`; прежние поля `kind` и `insufficientFunds` должны быть удалены из всех `metric.transfer`, включая генератор mock-пакета.
- Итоговая работа с результатами будет разделена на две функции: фактический журнал ходов и отдельный ИИ-анализ с учебной рефлексией. Обе функции отложены из текущего mock-среза и зарегистрированы как `LEGACY-0053` и `LEGACY-0054`; ИИ-разбор не заменяет и не изменяет исходный журнал.

## Architecture

### Уже принятые зависимости, активируемые этой игрой

- ADR-012: учебные метаданные и методические материалы.
- ADR-030: редактируемый authoring-слой и компиляция runtime-манифестов.
- ADR-041: объектное состояние терминалов, дорог, техники, грузов и проектов строительства.
- ADR-053/054/055: панели, граница смысла и отображения, декларативные UI-действия.
- ADR-058, только необходимые фазы: состояние по командам, фазы хода, переводы денег, воспроизводимые колоды. Полный пакет для «Монополии» не реализуется автоматически.
- ADR-061: проверяемые параметры действий для сумм и выбора из ограниченного набора.
- ADR-063: доставка фоновой карты и игровых ассетов из пакета игры.
- ADR-062: общий лениво загружаемый Phaser-host; принятый ADR-073 отделяет пошаговое поле от realtime `simulationSurface`.

### Принятые новые решения

- Accepted ADR-071: общая граница персонала фасилитируемой сессии, декларативные
  права и журналирование действий. Конкретная локальная роль ведущего, общий
  экран и отсутствие отдельной роли ассистента закреплены в GSR-033 и
  продуктовой спецификации этой игры.
- Accepted ADR-072: динамическая декларативная транспортная сеть, каноническая геометрия, создание дорог/полустанков, движение, закрытия и кратчайший маршрут.
- Accepted ADR-073: Phaser для интерактивной карты при сохранении панелей, расчетов и методики в обычном web UI.
- Accepted ADR-074: объявленные схемой ссылки на динамические объекты без клиентских путей; решение позволяет выбирать созданные в сессии узлы и дороги.
- Accepted ADR-080: UI-манифест объявляет `layout_mode: "map-first"` и
  смысловые `workspaceSlot`; общий renderer владеет полнооконной компоновкой и
  адаптацией без ветки по идентификатору игры.
- Accepted ADR-081: для явно включённой транспортной модели сервер строит
  путь с минимумом непрерывных региональных сегментов по версионированной
  геометрии, сохраняет каноническую ломаную и воспроизводимо случайно выбирает
  равный маршрут. Клиент передаёт только концы и оплату. Решение принято PM
  2026-07-13; нормативное подключение всё ещё ждёт подтверждённых областей.

### Обязательные границы

- В generic-коде нет `if` по id игры, цветам команд, терминалам или странам.
- Схемы JSON являются источником истины; ручные TypeScript-проверки не заменяют их.
- Runtime владеет деньгами, фазами, сетью, грузами и техникой; plugin/UI владеет только взаимодействием и отображением.
- Канонические координаты узлов и контуры областей, нужные правилам, находятся в game manifest; UI хранит фон, стили и преобразование в экранные координаты.
- Общая возможность считается доказанной только после нейтральной фикстуры.
- Любой временный ручной шаг фиксируется как ограничение с планом снятия, а не скрывается под видом готовой автоматизации.

## Scope

### Первая очередь: один континент

- Полная оцифровка правил и доступного контента одного континента.
- Авторский пакет `games/cards-money-trains/`: authoring, runtime/UI manifests, методика, карта, design artifacts, fixtures и project-local plugin.
- Подготовка обычной авторской партии; отменённый `GSR-030` не входит в поставку.
- Полный цикл одного обычного хода.
- Покупка/продажа техники, загрузка, движение, доставка, расчеты, строительство, новости и результаты.
- Неограниченная заранее последовательность ходов до решения ведущего, две промежуточные и одна финальная рефлексия.
- Два независимых победителя по типам команд.
- Один локальный общий экран ведущего с Phaser-полем и web-панелями.

### Платформенные доработки

- Минимальные части принятых задач платформы, реально используемые текущим срезом.
- Реализация ADR-073 и принятых после решения PM ADR-071/072.
- Реализация ADR-080 и перевод mock/нормативного web UI на карту как основу.
- Нейтральные контрактные и runtime-фикстуры.
- Editor readiness: схемы, карты связи generated-результата с authoring-исходником,
  фикстуры состояния и preview без game-specific редактора.

## Non-Goals

- Второй континент и режим до 80 участников — исключены из целевого продукта по решению PM.
- Сетевые устройства команд и удаленная сессия.
- ИИ-команды — отдельная модель после стабильной человеческой партии.
- Автоматическая психологическая оценка участников.
- Траектории дорог, произвольно рисуемые участником или ведущим; геометрию строит система.
- Штрафы за ошибочную отчетность.
- Произвольный программный код, неограниченные циклы и отдельный сервис Game
  Engine. Универсальные селекторы, сценарии, графовые вычисления и массовые
  изменения ADR-083 входят в платформенный объём, когда нужны этому срезу и
  заменяют узкие предметные эффекты.
- Публичный портал, покупки и лицензии, если PM отдельно не включит удаленный запуск в цель первой версии.
- Переписывание всего пакета настольных возможностей ADR-058 «на будущее».
- Полноценный пользовательский журнал всей партии и его выгрузка — отложенный долг `LEGACY-0053`; текущий `public.log` остается технической основой и доказательством тестового сценария.
- Итоговый разбор партии ИИ-агентом — отложенный архитектурный долг `LEGACY-0054`; до отдельного решения не подключаются провайдер ИИ, персональная аналитика или автоматическая интерпретация поведения участников.

## Plan Approval

not_required_except_routing_architecture_gate

Обычный режим развития через конкретную игру; `$cubica` не используется. Продуктовые вводные и архитектурные решения подтверждены PM 2026-07-11; ADR-071/072/073 приняты. Режим `map-first` отдельно подтверждён PM и принят ADR-080 2026-07-13, поэтому его реализация не требует повторного согласования, пока не меняются публичные границы решения. Недостающие авторские данные принимаются по отдельному контракту содержимого и блокируют только зависящие от них срезы.

## Plan

### Фаза 0. Закрыть продуктовые и архитектурные входы

1. Выполнено: принята одна локальная роль ведущего, общий экран, без отдельной runtime-роли ассистента и без свободных персональных заметок (ADR-071).
2. Выполнено: принято динамическое строительство дороги/полустанка, прямая каноническая геометрия и серверный расчет областей (ADR-072).
3. Выполнено: статусы ADR синхронизированы с `PROJECT_ARCHITECTURE.md`.
4. Выполнено: автоматический кредит и отрицательный баланс запрещены; продажа техники разрешена только в рынке; недостаток денег отклоняет добровольное действие. Обязательный дефицит разрешается исключением команды или явным займом ведущего.
5. Выполнено: получены начальная сеть, исходные контуры, колоды грузов/новостей и описания стран; специальные демонстрационные грузы и контрольный ход исключены.
6. Извлечь и проверить авторский пакет по `content-intake-contract.md`: граф сети, координаты, контуры, грузы, новости и страны.
7. Выполнено по основной семантике: закрыты `R-01`–`R-06`, `R-08`, `R-10`–`R-15`; для `R-07` получить судьбу техники и грузов исключённой команды, для `R-09` — выбор конкретной техники, объединение типов и срок изъятия. Независимый импорт данных продолжать параллельно.
8. Выполнено: PM принял ADR-081; общий серверный выбор дороги с минимумом
   непрерывных региональных сегментов, ломаная геометрия, версионирование и
   воспроизводимое случайное разрешение равенства реализованы и проверены на
   нейтральной фикстуре и mock-игре. GSR-032 не подключает `review-draft`
   области.

Критерий выхода: нет домыслов в правилах первого среза; архитектурные публичные контракты согласованы.

### Фаза 1. Минимальный платформенный фундамент обычной партии

1. Реализовать состояние команд и необходимую player/team-проекцию из принятого пакета пошаговых игр.
2. Реализовать минимальную фазовую модель, которая допускает ведущего и несколько команд одного типа в общей фазе.
3. Реализовать переводы денег и атомарные экономические операции.
4. Реализовать минимальную сеть ADR-072: узлы, открытое/закрытое ребро, один шаг движения, вместимость, кратчайшее расстояние.
5. Реализовать локальную роль ведущего по ADR-071 без сетевой аутентификации; ассистент использует тот же экран без отдельной идентичности.
6. Реализовать канал карты и ассетов ADR-063.
7. Реализовать общий лениво загружаемый Phaser-host и `interactiveBoardSurface` по ADR-073.
8. Для каждой общей возможности добавить нейтральную схему, позитивные/негативные фикстуры, runtime-тест и проверку проекции секретов.

Критерий выхода: нейтральная фикстура проводит абстрактный транспорт между узлами, переводит деньги и отклоняет закрытый/переполненный путь без упоминаний игры.

### Фаза UI. Карта как основа общего экрана

Эта фаза не зависит от ответов по автоматической траектории дороги и может
выполняться параллельно с приёмом авторского содержимого.

1. Расширить UI JSON Schema и authoring-схему значением
   `layout_mode: "map-first"` и ограниченным `workspaceSlot`; синхронизировать
   generated manifest contracts.
2. Добавить нейтральные позитивные/негативные фикстуры, включая отсутствие,
   дублирование `board` и неизвестную зону.
3. Провести новый режим через Presenter, routing и общий manifest renderer без
   изменения старых `topbar`/`leftsidebar` экранов.
4. Реализовать общий полнооконный контейнер, смысловые слои, выдвижные панели,
   нижний лоток, плавающие органы камеры, управление фокусом и отсутствие
   обычной прокрутки документа.
5. Перевести `interactiveBoardSurface` в map-first варианте из схемы
   «canvas + постоянная колонка» в слой `board`; независимые DOM-действия
   открывать поверх карты.
6. Добавить pan/zoom, границы мира, «Показать всю карту» и полноэкранный режим;
   сохранить lifecycle и серверный откат ADR-073.
7. Подключить к mock UI разрешённую карту как визуальную основу, не заменяя
   вымышленные сеть/координаты/правила и сохраняя явную маркировку `MOCK`.
8. Перевести нормативный UI на тот же режим и ассет
   `board-guinea-optimized`; подключать реальные узлы/контуры только после их
   подтверждённого импорта.
9. Принять интерфейс по
   `map-first-interface-specification.md` на контрольных размерах и закрыть
   `LEGACY-0062` только после фактических browser/visual доказательств.

Критерий выхода: карта совпадает с рабочей областью, панели лежат поверх неё,
документ не прокручивается, камера и DOM-альтернатива работают, а нейтральная
фикстура доказывает отсутствие игрового хардкода.

### Фаза 2. Приём авторского содержимого и подготовка обычной партии

Первоначальный `GSR-030` отменён автором и сохраняется только как история происхождения mock-сценария.

1. Выполнено как `review-draft`: извлечь координаты 23 терминалов, специальных отметок `π`/`9¾` и 10 начальных дорог из `Начальная транспортная сеть.png`; получить четыре подтверждения автора до публикации.
2. Частично выполнено: открытые векторные слои AI извлечены, 23 терминала
   сопоставлены с PNG с максимальной ошибкой `0,697` пикселя, 981 линия
   сохранена как непубликационный кандидат. Далее классифицировать линии,
   собрать замкнутые контуры и присвоить стабильные идентификаторы областям.
3. Выполнено для сети: сформировать контрольное наложение и таблицу связей; закрепить отдельный идентификатор дороги новости № 12 `road-3-3-14`. Наложение областей остаётся частью пункта 2.
4. Импортировать 174 грузовые строки и 34 новости из XLSX в отдельный authoring-вход; не изменять исходную книгу.
5. Импортировать описания стран и связи с терминалами из DOCX.
6. Проверить ссылки, выплаты, количество карт, колоды новостей, полноту эффектов и происхождение материалов.
7. Подключать к нормативному пакету только подтверждённые данные; у новости № 19 оставить явный content gate для выбора и срока изъятия техники.

Критерий выхода: нормативная игра создаёт обычную стартовую сессию на подтверждённой сети, а отчёты импорта не содержат необъяснённых ссылок или скрытых предположений.

### Фаза M. Полная тестовая партия — GSR-035

Эта фаза выполняется до нормативной приёмки GSR-031–033 и позволяет не
останавливать разработку из-за отсутствующих материалов автора. Результат —
работающая, но явно непубликационная игра `cards-money-trains-mock`, которую
ведущий может провести от настройки до двух победителей.

1. Перевести все денежные эффекты mock-пакета на принятую форму:
   `from.scope`/`to.scope` и `onInsufficient: "fail"`; удалить старые поля
   `kind` и `insufficientFunds`.
2. Обновить `games/cards-money-trains-mock/tools/build-mock-package.mjs`, заново
   сформировать authoring-вход, скомпилировать runtime/UI-манифесты и собрать
   браузерный плагин.
3. Реализовать полный фазовый цикл
   `setup → news → maintenance → market → cargo → operations → construction → debrief`,
   повторяемый без лимита ходов до ручного `finished`.
4. Реализовать воспроизводимые скрытые колоды новостей и грузов, выбор из двух
   грузов, загрузку в свободный вагон и удержание груза.
5. Реализовать рынок с покупкой/продажей техники только в своей фазе, запретом
   кредита и атомарным отказом при недостатке денег.
6. Реализовать прицепление, ручное и автоматическое отцепление, пять единиц
   действия, движение состава, вместимость терминала и сохранение недоехавшего
   состава до следующего хода.
7. Реализовать серверный кратчайший маршрут, объяснимый тариф 2 монеты за
   перегон и атомарную доставку «выплата банка → полная оплата гильдии».
8. Завершить жизненный цикл динамических дороги/полустанка: поддержать несколько
   строек до отдельного завершения, держать объекты закрытыми весь `N+1` и
   автоматически открывать их в начале `N+2`.
9. Добавить две промежуточные методические паузы, финальную рефлексию, ручное
   завершение с отменой/подтверждением и расчет двух победителей по типам команд.
10. Закрепить шестиходовой контрольный транскрипт, безопасные отказы, два
    перезапуска PostgreSQL-сессии, replay и browser-сценарий общего экрана.
11. Для каждой новой общей возможности добавить нейтральную фикстуру; вымышленные
    команды, сеть, цены, колоды и методические тексты оставить только в
    mock-пакете.

Критерий выхода: выполнены критерии `GSR-035`; ведущий проходит сокращенную
полную партию без ручной правки состояния и может продолжить игру после шестого
хода либо завершить ее явной кнопкой.

### Фаза 3. Первый обычный ход — GSR-031

1. Настраиваемое создание команд двух типов со стартом 10 монет + 2 вагона для логистики / 1 локомотив для гильдии.
2. Покупка и продажа локомотивов/вагонов.
3. Выбор двух грузов, приоритет по деньгам/вагонам, удержание груза и заявка на перегон.
4. Прицепление, флаги ограничений, пять единиц действия, остановка и продолжение состава.
5. Автоматическое отцепление, выплата банка перед полной платой паровозной гильдии в одной атомарной операции.
6. Общая таблица и явное завершение каждой фазы ведущим.
7. Порядок движения справа налево: деньги команды-владельца, затем число её локомотивов, затем воспроизводимый случайный выбор.
8. Обязательный платёж при дефиците открывает ведущему выбор исключения команды либо займа; добровольные платежи продолжают атомарно отклоняться.
9. Закрытия запрещают въезд, выезд, проезд, загрузку и выгрузку; эффекты новостей № 12, 14, 19, 25 и 34 проверяются по подтверждённым срокам и адресатам.

Критерий выхода: один реальный ход с несколькими вариантами решений проходит без ручного изменения состояния.

### Фаза 4. Стратегическое строительство и изменяемый рынок — GSR-032

1. Динамическая дорога между двумя выбранными узлами с автоматически рассчитанной траекторией, минимизирующей число пересекаемых областей; перед реализацией утвердить расширение ADR-072 для кандидатных траекторий, а равенство разрешать воспроизводимым случайным выбором.
2. Полустанок как точка любой существующей дороги: создание узла, разделение связи, запрет областей обоих конечных узлов и возможность использовать новый узел для дороги.
3. Совместное атомарное финансирование несколькими командами без кредита, минуса и частичного проекта.
4. Несколько объектов в одной фазе и отдельное действие ведущего для ее завершения.
5. Обычное строительство хода `N` остаётся закрытым весь `N+1` и открывается в начале `N+2`; все дороги двусторонние.
6. Новости, меняющие дороги, тарифы, цены или доступность только через объявленные эффекты; № 26 уменьшает первую дорогу хода на шесть сегментов без переноса, № 27 открывает `12 ↔ 22` в `N+1` либо ничего не меняет при существующей дороге.
7. Истощение старых терминалов и появление новых возможностей в соответствии с авторским балансом.
8. Платное совещание и журнал договоренностей без попытки формализовать живые переговоры.

Критерий выхода: команды могут выбрать краткосрочную перевозку либо вложение в будущую сеть, а последствия наблюдаемы через несколько ходов.

### Фаза 5. Полная учебная сессия одного континента — GSR-033

1. Полные колоды новостей и грузов с воспроизводимым случайным порядком.
2. Неограниченный заранее цикл ходов и доступная только ведущему кнопка «Завершить игру» с подтверждением.
3. Первая, вторая и финальная рефлексия как методические контрольные точки.
4. Роли внутри команд, инструкции участника и ведущего.
5. Объявленные интервенции для сложных случаев с причиной и аудитом.
6. Сохранение и восстановление займов, исключённых команд, постоянных эффектов № 14/34 и добавленных новостями грузовых карт.
7. Финальный расчет: деньги + техника по номиналу, два победителя, деролинг и итоговые вопросы.
8. Фикстуры начала, середины, точки невозврата и завершения партии.
9. Перевести сессии на постоянное хранилище и проверить восстановление после перезапуска; in-memory режим недостаточен для приемки восьмичасовой сессии.

Критерий выхода: ведущий проводит полную одноконтинентальную сессию по сценарию без внешней таблицы состояния и завершает ее по собственному решению.

### Фаза 6. Приемка и закрытие

1. Полный прогон контрактных, runtime, player, editor и browser-проверок.
2. Нейтральные фикстуры доказывают каждую новую общую возможность.
3. Проверка отсутствия id и терминов игры в platform core.
4. Синхронизация архитектуры, GSR, методики, `NEXT_STEPS.md` и реестра долга.
5. Удаление временных извлечений материалов из `.tmp/`.

## Execution Decomposition

Раздел задает границы исполнительных результатов. Он не является отдельным планом и обновляется внутри этого TSK.

| Пакет | Результат | Основные области | Зависит от | Можно параллельно |
|---|---|---|---|---|
| W0 Contracts | JSON Schema для минимальных команд, ролей, графа и `interactiveBoardSurface`; сгенерированные контракты | `docs/architecture/schemas`, `packages/contracts`, authoring compiler | принятие ADR-071/072 | нет: один владелец схемного источника истины |
| W1 Board foundation | команды, фазы, деньги и воспроизводимые колоды на нейтральной фикстуре | `services/runtime-api`, contracts tests | W0 по каждой конструкции | да, отдельными последовательными срезами возможностей |
| W2 Facilitator | локальный контекст ведущего, ролевые действия, журнал, ручное завершение | session/player API, Presenter | W0 role contract | параллельно W3/W4 после схемы |
| W3 Transport | узлы, связи, движение, вместимость, состояния и кратчайшее расстояние | runtime handlers, neutral fixture | W0 graph contract | параллельно W2/W4 после схемы |
| W4 Spatial surface | asset resolver, общий Phaser-host, `interactiveBoardSurface`, DOM-альтернатива и lifecycle | `apps/player-web`, plugin API | W0 UI/plugin contract | реализован как основа; параллельно W2/W3 |
| W4B Map-first workspace | UI schema/contract, общий полнооконный renderer, семантические слои, камера и перевод двух CMT UI-манифестов | UI schemas/contracts, `apps/player-web`, CMT authoring/plugin | W4 + Accepted ADR-080 | независимо от игрового маршрута и импорта колод; один владелец схемы |
| WU Universal mechanics | Game Intent → Cubica Mechanics IR, W0 principal/actor trust boundary, W1 immutable bundle и command ledger, общий executor, миграция всех игр и удаление production-контура `deterministic.effects` реализованы и прошли финальную межсистемную проверку | manifest/authoring schemas, session/receipt/event storage, runtime, Presenter, editor, operation discovery, neutral fixture, все игры и CMT mock builder | ADR-083 + ADR-084; immutable bundle, exact module/artifact/algorithm locks и durable command receipt включены в cutover | System dispatch без client version проверяет trigger под session lock; исполняемый код игры допускается только в изолированном game-scoped extension; старого production fallback нет |
| WM Complete mock session | полный цикл фаз, рынок, колоды, грузовые операции, тариф, методика, финал, replay и browser-приемка GSR-035 | `games/cards-money-trains-mock`, необходимые нейтральные platform handlers и тесты | W1–W4 + постоянное хранение | частями после фиксации соответствующей схемы; интеграционный транскрипт последовательно |
| W5 Author content intake | извлечённая сеть, контуры, грузы, новости, страны и обычная стартовая сессия | `games/cards-money-trains`, import tools, content reports | W1–W4 + полученные файлы | геометрию и текстовые данные можно разбирать параллельно |
| W6 Operating turn | GSR-031 | пакет игры + deck/economy handlers | W5 + закрытые правила первого хода | после W5 |
| W7 Construction | GSR-032 | пакет игры + transport state/geometric rules | W6 + канонические координаты и контуры областей | после W6 |
| W8 Full session | GSR-033, методика, ручной финал и восстановление | game package, persistence, browser tests | W7 + полный контент | после W7; persistence можно готовить раньше |

Правила координации:

1. Схема изменяется одним владельцем; runtime и UI начинают конкретную форму только после ее фиксации.
2. Каждый общий обработчик имеет нейтральную позитивную и негативную фикстуру до подключения игры.
3. Исполнители игры редактируют authoring-входы; generated manifests обновляет компилятор.
4. Изменение архитектурного контракта останавливает только затронутый пакет и выносится в ADR; независимые пакеты продолжаются.
5. Срез не считается завершенным по отдельным unit-тестам без своего browser-сценария и проверки чистоты платформы.

## Execution

| Результат | Статус | Следующий шаг |
|---|---|---|
| Анализ источников | updated | Основные файлы получены; большинство правил закрыто. В `R-07` остаётся судьба техники/грузов исключённой команды, в `R-09` — конкретный выбор и срок изъятия техники. |
| Архитектурный дизайн | implemented-verified | ADR-081 реализован schema-first планировщиком; нейтральное доказательство, mock-подключение и совместимость старых прямых связей проверены. |
| Универсальный язык механик | implemented-verified | JSON Schema 2020-12 Mechanics IR, typed `stateModel`, exact `moduleLock`, semantic checker, executor, authenticated command contract, immutable bundle, command/event ledger и exact retry реализованы. Пять deterministic-игр и AI-driven выбор мигрированы; Presenter и editor работают с `action.binding` и `params`; старые schema variants, interpreter и прямые agent effects удалены. Исторический аудит 29 variants / 693 stored / 967 expanded / 695 editable сохранён как доказательство объёма; сфокусированные, межсистемные и браузерные проверки зелёные. |
| GSR-036 | implemented-verified | `mock.debrief.next-turn` выполняет фазовый guard, увеличение хода, все сбросы, due activation, динамический reset ресурса и событие одной Mechanics-транзакцией. Исходник генератора, опубликованная привязка, нейтральный executor test и mock contract fixtures перенесены; старого handler fallback нет; полный канонический контур прошёл. |
| Продуктовая и содержательная спецификация | content-intake | Сеть извлечена в защищённый `review-draft`; AI откалиброван, 981 линия ожидает смысловой классификации. Получить четыре подтверждения автора, собрать контуры, затем импортировать 174 грузовые строки, 34 новости и описания стран. |
| Матрица требований | done | Обновлять статусы по мере реализации и фактических проверок. |
| GSR-035 | implemented-with-findings | Runtime mock-цикл, два PostgreSQL-рестарта, защита всех 88 действий и канонический отпечаток реализованы. Дефект ходового ресурса купленного локомотива закрыт динамической выборкой GSR-036; полная интерфейсная приёмка фаз остаётся открытой. |
| GSR-030 | superseded | Автор исключил демонстрационные грузы и контрольный ход; запись сохраняется как история mock-сценария. |
| GSR-031 | proposed-content-intake | Подтвердить извлечённую начальную сеть, импортировать грузы и новости; реализовать подтверждённые обязательные платежи, закрытия и порядок локомотивов; до нормативной приёмки закрыть остатки `R-07` и `R-09`. |
| GSR-032 | implemented-mock-content-gate | На нейтральных и тестовых областях реализованы свободный выбор узлов/дороги, серверный preview, атомарное строительство, два порядка road/waypoint и открытие в начале `N+2`. Для нормативной приёмки нужны подтверждённые реальные области и один авторский контрольный сценарий. |
| GSR-033 | proposed-content-intake | Импортировать страны и методику, получить конфигурации команд; постоянное хранение уже доказано mock-срезом. |
| ADR-071 | implemented | Доверенная локальная роль ведущего и ролевые действия проверены runtime-тестами. |
| ADR-072 | implemented-extended | Прямая совместимая road/waypoint-геометрия и атомарная оплата сохранены; явно включаемый минимальный по областям режим добавлен ADR-081. |
| ADR-073 | implemented-extended | Ленивый Phaser-host, сцена игры, lifecycle и DOM-альтернатива дополнены общим временным черновиком выбора и наложением серверного preview; правила и состояние остаются в runtime. |
| ADR-074 | implemented | Объявленные схемой ссылки и повторная runtime-проверка реализованы. |
| ADR-080 | implemented-awaiting-product-acceptance | Общий `map-first` контракт, схема, renderer, закрываемые панели, камера, нейтральная фикстура и миграция обоих CMT UI реализованы; технические browser-измерения пройдены. |
| ADR-081 | implemented-verified-mock | Серверный планировщик, канонические области и переходы, контрольный хеш, ломаная дороги, воспроизводимое равенство и совместимое отображение проверены нейтральными и mock-тестами. Реальные области остаются содержательным gate. |
| Канал ассетов | implemented | Карта зарегистрирована, оптимизирована и доставляется по id; автор и правообладатель подтвердили использование, изменение формата и публикацию. |
| Пакет игры | implemented-mock-with-findings | Отдельный test-only пакет проходит 88 серверных действий, все фазы, рынок, грузы, транспорт, оба порядка строительства и ручной финал. Дорога и полустанок выбираются свободно на карте или в DOM-форме; проекция разных методических пауз остаётся открытой. Нормативный контент не подменён. |
| Реализация | reviewed-with-findings | Серверный mock-сценарий принят, дефект порядка строительства закрыт общим жизненным циклом, а ресурс купленного локомотива — динамической Mechanics-выборкой. Открыты пользовательская приёмка всех фаз и полный ручной путь через UI. |
| Динамический экран ведущего | partial | ADR-079 реализовал серверную доступность, понятные причины и блокировку повторного ввода; строительство теперь имеет свободный выбор и проверяемый preview. В `LEGACY-0058` остаются выбор грузов и техники и полный UI-проход остальных фаз. |
| Карта как основа интерфейса | implemented-awaiting-product-acceptance | Canvas совпадает с рабочей областью на трёх контрольных размерах, реальная карта является основой, панели закрываются и не резервируют колонки. `LEGACY-0062` остаётся active до визуальной приёмки PM. |
| Методика на экране | open-finding | `LEGACY-0059`: выбирать разные паузы по состоянию и показывать ведущему импортированные вопросы, цели и инструкции. |
| Возврат к сессии | deferred-debt | `LEGACY-0060`: PostgreSQL сохраняет партию, но после потери localStorage нужен безопасный способ повторного подключения ведущего. |
| Полноценный журнал ходов | deferred-debt | `LEGACY-0053`: поверх фактических событий нужен отдельный понятный игровой лог и проверяемая выгрузка; текущий mock-срез этим не блокируется. |
| ИИ-разбор и учебная рефлексия | deferred-architecture | `LEGACY-0054`: сначала согласовать входной контракт, версии модели/промпта, приватность, аудит, отказоустойчивость и контроль ведущего. Производная не меняет счет или состояние и явно отделяет факты от интерпретаций. |
| Закрепление версии игры за сессией | implemented-verified | Сессия закрепляет неизменяемый `bundleHash`; хранилище сохраняет канонический пакет, точные версии и хеши модулей, а квитанция — `definitionHash` и `planHash`. Продолжение по молча изменившимся правилам исключено; `LEGACY-0055` закрыт. |

## Acceptance

### Продукт

- Отдельная mock-игра проходит полный цикл `setup`–`finished`, явно маркируется как тестовая и не смешивается с полученным авторским содержимым.
- Полная одноконтинентальная партия проводится до явного решения ведущего о завершении.
- Все заявленные правила из согласованной матрицы источников либо реализованы, либо явно вынесены за границы версии.
- Ведущий управляет единым понятным интерфейсом; действия паровозных гильдий и логистических компаний различаются по фазе и игровому субъекту. Ассистент первой версии использует тот же экран без отдельной runtime-роли.
- Авторская карта является полнооконной основой интерфейса; панели ведущего
  открываются поверх неё, а обычная страница не имеет вертикальной прокрутки.
- Определяются два победителя; итоговый расчет объясним и воспроизводим.
- Карта, составы, грузы, строительство, новости и таблица результатов не требуют параллельного ручного учета.

### Учебная методика

- Каждая компетенция связана с игровыми ситуациями, наблюдаемыми индикаторами и вопросами рефлексии.
- Система помогает ведущему замечать последствия, но не выдает непрозрачную автоматическую оценку личности.
- Методические паузы и интервенции являются явными событиями сессии.
- Инструкции участника и ведущего версионируются вместе с игрой.

### Архитектура

- Authoring JSON — редактируемый источник; runtime/UI JSON генерируется и валидируется по схемам.
- Общие возможности не содержат данных игры и имеют нейтральные тесты.
- Runtime является единственным владельцем игрового состояния и правил; UI-плагин не рассчитывает деньги и допустимость движения.
- Фоновая карта доставляется через платформенный реестр ассетов, а не из `apps/player-web/public`.
- Нет незарегистрированного архитектурного дрейфа или временных ручных обходов.
- Phaser не владеет правилами и применяет новое положение объектов только из подтвержденного runtime snapshot.
- `map-first` объявлен схемой и доказан нейтральной фикстурой; generic renderer
  не содержит игровых CSS-сигналов, а старые layout modes сохраняют поведение.
- Полная сессия восстанавливается после перезапуска runtime без изменения будущего порядка колод.

## Validation

Точный набор команд уточняется по мере реализации, но итоговый контур обязан включать:

```text
npm run generate:contracts
npm run verify:contracts-schema-parity
npm run verify:manifest-authoring
npm run verify:canonical
npm test --workspace services/runtime-api
npm test --workspace @cubica/player-web
node scripts/dev/generate-structure.js --check
git diff --check
```

Дополнительные проверки:

- replay обычного авторского хода и полного mock-сценария;
- негативные тесты закрытой дороги, переполненного терминала, недостатка денег и неверной роли;
- browser e2e первого обычного хода и сокращённой полной mock-партии;
- поиск `cards-money-trains`, названий стран и цветов команд в `services/runtime-api/src` и generic `apps/player-web/src` должен быть пустым, кроме тестов, загружающих игру как данные;
- визуальная проверка карты на широком и узком экране;
- измерение совпадения canvas с map-first viewport и отсутствия прокрутки на
  `1920 × 1080`, `1440 × 900` и `1024 × 768`;
- проверка pan/zoom, границ мира, полного обзора, fullscreen, управления
  фокусом и перекрытия карты панелями;
- проверка повторного монтирования/размонтирования Phaser-сцены без накопления обработчиков и памяти.

## Artifacts

- `docs/reviews/2026-07-11-cards-money-trains-source-analysis.md` — сверка источников, расхождения и недостающие материалы.
- `docs/reviews/2026-07-12-cards-money-trains-implementation-review.md` — текущее ревью реализации; findings считаются открытыми, пока не исправлены и не перепроверены.
- `docs/architecture/gameplay-slices/030-cards-money-trains-demonstration-round.md` — отменённый демонстрационный срез, сохранённый как история происхождения mock-сценария.
- `docs/architecture/adrs/071-facilitated-training-sessions.md` — принятое общее решение для ведущего и методических пауз.
- `docs/architecture/adrs/072-declarative-transport-network-board.md` — принятое общее решение динамического транспортного поля.
- `docs/architecture/adrs/073-phaser-interactive-board-surface.md` — принятая граница Phaser-поля и обычных web-панелей.
- `docs/architecture/adrs/080-declarative-map-first-workspace.md` — принятый общий режим, где карта заполняет рабочую область, а интерфейс располагается смысловыми слоями поверх неё.
- `docs/architecture/adrs/081-server-planned-region-minimal-roads.md` — принятое серверное планирование минимальной по областям ломаной дороги; реализация идёт на нейтральных и тестовых данных.
- `docs/architecture/adrs/083-universal-composable-gameplay-mechanisms.md` — принятое правило: игровой движок развивает универсальные комбинируемые примитивы вместо предметных runtime-эффектов.
- `docs/architecture/adrs/084-typed-transactional-mechanics-ir.md` — единое принятое решение по Game Intents, границе Presenter, двум discovery-проекциям, mechanics capability packs, изолированным игровым extensions, command idempotency, Cubica Mechanics IR и полному переходу без постоянного старого interpreter.
- `docs/architecture/cards-money-trains-platform-design.md` — единый архитектурный дизайн платформенных и игровых границ.
- `docs/architecture/gameplay-slices/031-cards-money-trains-operating-turn.md` — первый обычный ход.
- `docs/architecture/gameplay-slices/032-cards-money-trains-construction-cycle.md` — совместное строительство и открытие дороги.
- `docs/architecture/gameplay-slices/033-cards-money-trains-facilitated-session.md` — полная учебная сессия и ручное завершение.
- `docs/architecture/gameplay-slices/035-cards-money-trains-complete-mock-session.md` — полная сокращенная mock-партия и ее доказательства приемки до получения авторского содержимого.
- `docs/tasks/artifacts/TSK-20260711-cards-money-trains-game/product-specification.md` — продуктовая спецификация.
- `docs/tasks/artifacts/TSK-20260711-cards-money-trains-game/content-intake-contract.md` — контракт передачи авторского содержимого.
- `docs/tasks/artifacts/TSK-20260711-cards-money-trains-game/map-content-intake-report.md` — понятный отчёт об извлечённой сети, векторных слоях, калибровке и безопасной публикации.
- `docs/tasks/artifacts/TSK-20260711-cards-money-trains-game/requirements-traceability.md` — матрица требований и доказательств.
- `docs/tasks/artifacts/TSK-20260711-cards-money-trains-game/implementation-readiness-checklist.md` — контроль готовности входов и доказательств каждого среза.
- `docs/tasks/artifacts/TSK-20260711-cards-money-trains-game/map-first-interface-specification.md` — проект полнооконной карты, слоёв, камеры, адаптации и измеримой визуальной приёмки.
- `docs/tasks/artifacts/TSK-20260711-cards-money-trains-game/trial-run-guide.md` — руководство пробного запуска, остановки и возобновления mock-сессии для PM и ведущего.
- `docs/tasks/artifacts/TSK-20260711-cards-money-trains-game/universal-gameplay-mechanisms-refactoring-prompt.md` — самостоятельный промт для полного аудита узких игровых механизмов и последующего поэтапного рефакторинга.
- `docs/reviews/2026-07-14-universal-gameplay-mechanisms-audit.md` — доказательный отчёт стадии A, P0-разрывы, proposed публичная форма и первый ограниченный срез.
- `docs/tasks/artifacts/TSK-20260711-cards-money-trains-game/universal-gameplay-mechanisms-matrix.md` — полный инвентарь текущих операций, потребителей, классов и целевых замен.
- `docs/architecture/gameplay-slices/036-cards-money-trains-universal-mechanics-proof.md` — ограниченный первый игровой proof: due activation и reset ресурса фактической active-коллекции, включая купленный локомотив.

## Risks

| Риск | Снижение риска |
|---|---|
| Недостающий авторский контент заменят домыслами | Фаза 0 блокирует соответствующий срез; источники и вопросы зафиксированы отдельно. |
| Автоматическая дорога будет молча сведена к старому прямому отрезку | ADR-081 принят: новый режим включается явно, проверяется нейтральной фикстурой и тестовым пакетом; нормативный манифест ждёт подтверждённые области. |
| Платформа начнет реализовывать всю абстрактную программу настольных игр | Каждая фаза активирует только возможности, используемые следующим игровым срезом. |
| Ведущий получит неограниченное изменение состояния | Только объявленные ролевые действия, валидация, причина и журнал по ADR-071. |
| Правила движения окажутся во frontend-плагине | Топология и проверки принадлежат runtime по ADR-072; UI хранит координаты. |
| Геометрические граничные случаи дают разную стоимость | Runtime использует канонические контуры и единое правило границ; нейтральные фикстуры покрывают касание, вершины и общие границы. |
| Полный e2e займет восемь часов | Начало, середина и конец покрываются фикстурами; полный replay выполняется без реальных пауз. |
| Сетевой режим резко увеличит первую очередь | Решение о канале принимается до реализации; хотсит остается полноценной первой моделью. |
| Phaser увеличит загрузку и потребление памяти | Лениво загружать движок только для Phaser-игр, не включать физику без необходимости, проверять bundle budget и lifecycle cleanup. |
| Map-first будет реализован игровыми CSS-хаками | Сначала расширить JSON Schema и нейтральную фикстуру ADR-080; игра объявляет только режим и зоны, общий renderer владеет раскладкой. |
| Панели снова закроют большую часть карты | Измерять площадь перекрытия и canvas на контрольных размерах; боковые зоны являются выдвижными слоями, а не постоянными колонками. |
| Общие селекторы расширят существующий канал чтения secret-состояния | До selector language выполнить отдельный P0 trust-boundary: deny-by-default player projection, public-only availability и контроль secret → public. |
| Универсальный язык превратится в неограниченное программирование внутри JSON | Закрытый schema whitelist, platform-owned limits, канонический порядок, typed results и атомарный `failAll`; JavaScript, сеть, файловая система и системное время запрещены. |

## Plan Amendments

- 2026-07-11: создан исходный план. Архитектурные решения не считаются принятыми до отдельного ответа PM.
- 2026-07-11: PM выбрал один общий экран, стоимость 2 монеты за область, случайные новости, ручное завершение без лимита ходов, отсутствие штрафов и исключение второго континента. Гибридная Phaser-граница принята в ADR-073.
- 2026-07-11: подготовлен полный пакет проектной документации; план разложен на GSR-030–033, добавлены постоянное хранение полной сессии и контракт приема содержимого. Границы продукта не изменены.
- 2026-07-11: PM принял ADR-071/072, динамическое строительство road/waypoint, финансовые запреты и стартовые ресурсы; задача переведена в `in_progress`.
- 2026-07-11: PM разрешил отдельный test-only пакет с мок-данными до ответа автора, подтвердил права на использование и публикацию карты и поручил реализовать PostgreSQL-хранение сессий. Нормативный пакет остаётся заблокированным по авторскому содержимому; mock не считается доказательством правильности баланса или правил автора.
- 2026-07-12: PM поручил зафиксировать готовый блок и продолжать до полностью работающей игры на тестовых данных. В план добавлен GSR-035: полный фазовый цикл, рынок, колоды, загрузка/сцепление/отцепление, маршрут и тариф, строительство, методические паузы, ручной финал, два победителя, PostgreSQL-восстановление, replay и browser-приемка. Денежные эффекты и генератор mock-пакета переводятся на принятую форму `scope`/`onInsufficient` с обязательной пересборкой манифеста.
- 2026-07-12: GSR-035 реализован на mock-данных и прошел 88-шаговый HTTP-сценарий, семь атомарных отказов, два восстановления PostgreSQL и browser-путь. До итоговой приемки оставлены только решения PM по `expectedStateVersion` и исключению служебных wall-clock полей из канонического replay-хеша.
- 2026-07-12: PM подтвердил оба решения. `expectedStateVersion` стал обязательным публичным предусловием с HTTP 409 без мутации; ADR-078 закрепил версионированный отпечаток полного серверного состояния без двух служебных отметок времени. GSR-035 переведён в `implemented`.
- 2026-07-12: PM разделил будущую работу с результатами на независимый фактический журнал ходов (`LEGACY-0053`) и ИИ-анализ с учебной рефлексией (`LEGACY-0054`). Обе функции намеренно отложены; технический `public.log` не объявляется готовым пользовательским журналом, а ИИ-интерпретация потребует отдельного архитектурного решения, не будет менять счет/состояние и должна фиксировать версии модели и промпта.
- 2026-07-12: документационная сверка выявила, что сессия не закрепляет версию игрового пакета. Ошибочное утверждение снято из GSR-035, разрыв зарегистрирован как `LEGACY-0055`; до решения пакет нельзя менять в ходе пробной партии.
- 2026-07-12: ревью реализации сняло утверждение о полной интерфейсной приемке. Browser E2E автоматизирует старт/перезагрузку/финал, но длинную середину проводит через API; доступность и выбор действий в UI статичны (`LEGACY-0058`), а разные методические паузы не проецируются на экран (`LEGACY-0059`). Также воспроизведены дефекты порядка строительства (`LEGACY-0056`) и ходового ресурса купленного локомотива (`LEGACY-0057`); повторное подключение после потери localStorage отложено как `LEGACY-0060`. Исправления не входят в эту документационную синхронизацию.
- 2026-07-12: PM принял общий механизм серверной доступности действий. ADR-079 добавил безопасную проекцию в снимок сессии, web/Phaser блокировку недоступных и повторных команд и нейтральную проверку. Ошибка повторного `mock.news.draw` закрыта; динамический выбор объектов и полный UI-проход остаются в `LEGACY-0058`.
- 2026-07-13: получены начальная сеть, AI/PDF-контуры, XLSX с 174 грузовыми строками и 34 новостями и DOCX с описаниями стран. Автор исключил демонстрационные грузы и контрольный ход, подтвердил движение справа налево и автоматическую дорогу с минимумом областей. GSR-030 закрыт как `Superseded`; в план добавлены импорт содержимого, `R-01`–`R-15` и архитектурный gate для расширения прямолинейной модели ADR-072.
- 2026-07-13: PM принял общий декларативный режим «карта — основа». Создан Accepted ADR-080, отдельная проектная спецификация и независимая Фаза UI. Текущий малый canvas в topbar зарегистрирован как `LEGACY-0062`; реализация map-first может идти до закрытия спорных правил дороги и новостей.
- 2026-07-13: автор закрыл основную семантику `R-01`–`R-15`: точную связь № 12, случайное равенство дорог, запрет областей концов для полустанка, `N+2`, двусторонность, очередь локомотивов, развилку исключение/займ при обязательном дефиците, полные закрытия и эффекты новостей. В `R-07` остаётся судьба техники и грузов исключённой команды; в `R-09` — конкретный выбор, объединение типов и срок изъятия. Архитектурный gate дороги сужен до алгоритма кандидатных траекторий; продуктовое равенство больше не открыто.
- 2026-07-13: PM подтвердил сохранение WebP как художественной runtime-подложки и использование AI/SVG только для отдельной точной геометрии; при необходимости глубокого масштаба план расширяется дополнительным уровнем WebP или фрагментами, а не полной SVG-текстурой Phaser.
- 2026-07-13: реализован общий schema-first `map-first`: контракты и компилятор зон, полнооконный renderer, взаимно исключающие боковые панели, DOM-управление камерой, pan/zoom/границы/fullscreen и оба CMT UI. Mock использует разрешённую WebP-копию авторской карты с отдельной маркировкой тестовых данных. Техническая browser-проверка прошла на `1920×1080`, `1440×900`, `1024×768` и узком `390×844`; `LEGACY-0062` ждёт визуальную приёмку PM.
- 2026-07-14: PM отменил прежний запрет ранней универсализации игрового языка и принял ADR-083. Универсальные селекторы, ограниченные циклы, сценарии, планирование и массовые изменения теперь являются целевыми механизмами; отдельный `transport.construction.activateDue` признан переходным долгом `LEGACY-0063`. На этом промежуточном шаге конкретная форма ещё ожидала решения; следующая запись фиксирует последующее делегированное утверждение ADR-084.
- 2026-07-14: стадия A дополнена после прямого требования PM полностью удалить и мигрировать старые эффекты. Инвентарь уточнён: 29 schema variants, 27 используемых ops, 693 stored effects, 967 шагов после template expansion и 695 editable effects. PM делегировал продолжение без отдельного согласования; ADR-084 принимает `cubica.dev/mechanics/v1alpha1` как typed transactional IR с state model, query/assert/command/algorithm, ResultRef, security labels, static/runtime budgets и session pinning. `objects.updateMany` больше не считается центром языка. Постоянный v1 adapter отклонён: после поэтапной branch-миграции будет lockstep cutover и физическое удаление schema/types/handlers/oracle/converter. Первый GSR-036 одновременно заменяет `transport.construction.activateDue` и восстанавливает ресурс всем фактически активным объектам, включая купленные; `LEGACY-0063` остаётся active до полного удаления v1.
- 2026-07-15: PM принял расширение всего блока рефакторинга. ADR-084 теперь
  фиксирует Game Intent как единственный предметный write-фасад, миграцию
  существующего `actionId` в формальный Game Intent без переименования,
  Presenter как projector на
  read path и router на write path, отдельные authoring/runtime discovery
  scopes, build-time mechanics capability packs, изолированные game-scoped
  runtime extensions при недостаточности IR, bounded server binding, полный
  набор семантических гарантий операций и durable command receipts.
  `commandId` остаётся одним для всех HTTP retry,
  receipt lookup предшествует `expectedStateVersion`, а изменение
  state/random/events/audit/receipt атомарно. W0/W1 расширены этой trust,
  session pinning и idempotency foundation; публичные schema/runtime/game
  contracts на данном документационном шаге ещё не изменены.
- 2026-07-15: повторное ревью уточнило principal/actor, чистоту algorithm,
  structural control nodes, public/internal receipt и границу расширений.
  Раскрытие `planHash` для учебных данных не считается критичным и не блокирует
  выпуск.
  Исполняемый код игры разрешён, когда IR и общие modules недостаточны, но
  только в изолированном game-scoped runner без прямого доступа к платформе;
  выбор этой принятой границы не требует согласования для каждой игры.
  GSR-036 расширен до полного `mock.debrief.next-turn` — guard, счётчик, все
  сбросы, due activation, динамический resource reset и log выполняются одной
  транзакцией без смешивания со старыми effects.
- 2026-07-13: плоскость нормативной карты приведена к исходным `5079 × 3627`; из PNG извлечены 25 точек и 10 дорог в защищённый `review-draft`. Общая схема аннотации, нейтральная фикстура, визуальное наложение и запрет публикации неподтверждённых данных реализованы без новых пакетов. AI подтверждён как пригодный векторный источник; контуры требуют автоматической калибровки по 23 терминалам и ручной смысловой сверки.
- 2026-07-13: AI-карта воспроизводимо разобрана без новых пакетов. Извлечены
  23 центра терминалов и 981 кандидат линии, аффинная калибровка к PNG имеет
  среднеквадратичную ошибку `0,441` и максимальную `0,697` пикселя.
  Результат защищён локальной JSON Schema, имеет `publishable: false` и не
  участвует в runtime. Следующий содержательный шаг — классификация линий и
  сборка подтверждённых полигонов. Серверный планировщик дороги принят как
  ADR-081 и переводится в schema-first реализацию на безопасных фикстурах.
- 2026-07-13: принятый ADR-081 реализован общим schema-first планировщиком.
  Нейтральная и test-only сети доказывают канонические области и переходы,
  контрольный хеш, минимум непрерывных региональных сегментов,
  воспроизводимое разрешение равенства, сохранение ломаной и разделение её
  полустанком. Старый прямой режим сохранён. Предварительный просмотр цены
  отложен до отдельного read-only пути, а жизненный цикл открытия `N+2`
  остаётся самостоятельной частью GSR-032. Новые пакеты не устанавливались.
- 2026-07-13: GSR-032 доведён на тестовых данных до полного строительного
  взаимодействия. Общий lifecycle открывает дороги, полустанок и две части
  разделённой дороги в начале `N+2` и снимает только строительную причину
  закрытия. Ведущий свободно выбирает узлы либо точку ломаной на карте или в
  доступной форме. Отдельный read-only запрос показывает рассчитанную сервером
  ломаную и цену, не передаёт оплату и не изменяет случайность; подтверждение
  на той же версии использует тот же путь. `LEGACY-0056` закрыт. Реальные
  контуры остаются содержательным, а не архитектурным ограничением.
- 2026-07-15: выполнен общий cutover Game Intent → Cubica Mechanics IR.
  Отдельная JSON Schema 2020-12, типизированный `stateModel`, точные
  `moduleLock`, semantic checker, executor и command/event ledger подключены к
  runtime; публичная команда больше не доверяет `playerId`, использует один
  `params`, `commandId` и `expectedStateVersion`. Antarctica, Estate Race,
  Simple Choice, оба пакета «Карты, деньги, поезда» и AI-driven выбор
  перенесены; Presenter, editor preview, compiler и mock builder синхронизированы.
  Старые effect schema/types/handlers, runtime templates, JsonLogic executor и
  прямые agent state effects удалены. `mock.debrief.next-turn` реализует весь
  GSR-036 одной транзакцией и закрывает дефект ресурса купленного локомотива.
  На момент этой записи реализация была завершена, а полный канонический прогон
  ещё не был выполнен; итоговая приёмка добавлена в Handoff Log за 2026-07-17.

## Handoff Log

### 2026-07-11 — Codex

- Changed: создан анализ источников, GSR-030, Draft ADR-071/072, Accepted ADR-073 и этот корневой план.
- Sources reviewed: главный DOCX (18 страниц), карта PNG, PDF-инструкция (16 страниц), презентация PPTX (47 слайдов).
- Process note: Context7 был вызван дважды по обязательному правилу проекта, но сервер ответил `Transport closed`; библиотечные решения по этой причине не вводились.
- Done: определены правила, методика, расхождения, недостающие данные, первый вертикальный срез и архитектурные границы.
- Remaining: колоды/топология, решение о свободном строительстве, принятие или корректировка Draft ADR-071/072, затем реализация Фазы 1.
- Next: после ответа PM обновить рабочие трактовки, статусы ADR и GSR-030; не начинать изменение публичных контрактов до этого.
- Risks: исходная физическая игра допускает свободное строительство, которое пока не имеет однозначной цифровой топологии.

### 2026-07-11 — Codex, пакет готовности к реализации

- Changed: добавлены архитектурный дизайн, продуктовая спецификация, контракт содержимого, матрица требований и GSR-031–033; Draft ADR-071/072 уточнены до двух минимальных решений для PM.
- Validation: `generate-structure --check`, `verify:legacy`, JSON descriptors, 15 Markdown-файлов (ссылки/оглавления), междокументные статусы, `git diff --check` и проверка на признаки секретов — успешно.
- Done: документация отделяет платформенные расширения, данные игры, content gates, постоянное хранение и доказательства приемки.
- Remaining: подтверждение Draft ADR-071/072 и передача минимального контента GSR-030.
- Next: после подтверждения перевести ADR в Accepted и начать платформенный фундамент на нейтральных фикстурах, параллельно принимая авторские данные.
- Risks: без авторской топологии и цен нельзя зафиксировать контрольный replay демонстрации; эти значения не должны изобретаться исполнителем.

### 2026-07-11 — Codex, решения PM приняты

- Changed: ADR-071/072 переведены в `Accepted`; каталог кандидатных дорог заменен динамическими `road`/`waypoint`; синхронизированы архитектура, GSR-030–033, продуктовые артефакты и очередь работ.
- Confirmed: одна локальная роль ведущего; прямой канонический сегмент и серверный расчет областей; несколько строительств до отдельного завершения фазы; запрет автоматического кредита/минуса и продажи вне рынка; старт 10 монет + техника по типу.
- Remaining: авторские колоды, начальная сеть, координаты, контуры областей, контрольные движения и цены грузов.
- Next: реализация нейтрального платформенного фундамента и прием контента параллельными потоками.

### 2026-07-11 — Codex, безопасные ссылки на динамические объекты

- Changed: создан и принят ADR-074; `paramsSchema` может объявлять ограниченную ссылку на ресурс сессии, которую runtime проверяет без интерпретации строки как пути.
- Confirmed: PM разрешил публичное расширение контракта; полный динамический выбор узлов и дорог архитектурно разблокирован.
- Validation: синхронизированы `PROJECT_ARCHITECTURE.md`, архитектурный дизайн, матрица требований, checklist и `NEXT_STEPS.md`.
- Remaining: реализовать JSON Schema, межссылочную runtime-проверку и нейтральные негативные фикстуры.
- Next: параллельная реализация W0–W4 с одним владельцем схемы и последующая интеграция игрового пакета.

### 2026-07-11 — Codex, платформенный фундамент и оболочка игры

- Changed: реализованы параметры действий, безопасные ссылки, локальный ведущий, атомарные переводы, динамические road/waypoint-операции, `interactiveBoardSurface`, канал ассетов и пакет `games/cards-money-trains`.
- Verified: runtime 140/140; manifest contracts 19/19 после расширения ассетов; player-web 145/145; валидатор ассетов 18/18 плюс проверка реестра игры; 5/5 тестов пакета игры; компиляция и опубликованный plugin bundle; схемы, OpenAPI, платформенная чистота и структура репозитория согласованы.
- Browser evidence: в production-клиенте ведущий выбрал режим дороги, завершил строительный этап, запросил и отменил завершение, затем повторно запросил и подтвердил его; итоговое состояние `finished`, Phaser-canvas 1400×1000, ошибок страницы и консоли нет.
- Product state: ведущий может открыть общую оболочку, увидеть карту и команды демонстрационной фикстуры, выбирать режим строительства, явно завершить этап и завершить игру с подтверждением.
- Content gate: дорога/полустанок не включены в runtime-манифест игры до поступления начальной сети, канонических координат и контуров областей; точная форма действий сохранена в `logic.pendingActions` и исключена компилятором.
- Remaining: авторские колоды, выплаты грузов, топология/области, контрольные движения и подтверждение прав публикации карты.
- Next: принять минимальный контент GSR-030, включить `pendingActions`, собрать браузерный транскрипт и только после него переводить GSR-030 в приемку.

### 2026-07-11 — Codex, исполнимый mock-цикл до строительства

- Classification: общими являются проверка открытого ребра и соседства, расход единиц действия, вместимость узла, перенос сцепленных объектов и завершение груза; конкретная новость, тарифы, команды, выплаты и порядок действий остаются только в mock-пакете.
- Changed: транспортный контракт дополнен объявляемыми правилами движения и доставки; универсальный runtime применяет их без проверки `gameId`; mock-пакет объявляет новость, атомарное обслуживание, один переход локомотива и доставку с двумя расчетами.
- Control evidence: транскрипт продолжает цикл существующим строительством дороги, полустанка и явным завершением этапа; негативные сценарии фиксируют закрытую дорогу, переполненный терминал, преждевременную доставку и недостаток денег без частичного сохранения.
- Architecture: новое ADR не требуется — реализация является следующим ограниченным срезом уже принятой серверно-авторитетной транспортной модели и существующего атомарного действия.
- Contract migration: тестовая игра, её сборщик и скомпилированный манифест переведены на окончательную форму денежных переводов `scope` + `onInsufficient`; временная серверная совместимость со старой формой удалена.
- Validation: 7/7 тестов mock-пакета, 13/13 нейтральных платформенных тестов движения/ссылок/строительства, 5/5 узких тестов совместимости денежных переводов и 2/2 прямых smoke-теста транспортных операций; проверки типов manifest/runtime и совпадения generated-контрактов со схемой успешны.
- Not run: полный runtime suite и полный canonical-контур намеренно не повторялись — это промежуточный срез, а основной агент выполнит расширенную интеграцию после завершения постоянного хранения.
- Remaining: заменить вымышленные идентификаторы, топологию, выплаты и контрольные движения подтвержденными материалами автора; затем провести браузерную приемку нормативной игры.

### 2026-07-12 — Codex, техническая приёмка полного mock-цикла

- Architecture: PM подтвердил обязательный `expectedStateVersion` и
  канонический replay без двух служебных отметок времени. ADR-059 дополнен
  атомарным предусловием действия; новый ADR-078 закрепил версионированный
  отпечаток полного серверного состояния.
- Changed: Runtime API проверяет версию под исключительной блокировкой до
  обработчика и возвращает 409 без мутации; player-web передаёт подтверждённую
  версию, после конфликта загружает свежий снимок и не повторяет действие;
  replay использует платформенную канонизацию вместо широкого удаления всех
  полей `at`/`lastUpdatedAt`.
- Acceptance: 88-шаговая партия повторяет каждый успешный запрос с прежней
  версией и получает 409; семь игровых отказов остаются атомарными; два
  перезапуска на PostgreSQL 17 восстанавливают ту же сессию; браузерный путь
  общего экрана проходит до `finished`.
- Verification: Runtime API 186 passed / 2 infrastructure skips, player-web
  154/154 и production build, mock package 9/9, neutral replay/platform 22/22,
  OpenAPI/contract/schema/game-agnostic/asset/AI gates — успешно.
- Independent gate: полный `verify:canonical` останавливается только на
  `verify:external-skills`, потому что параллельная миграция каталога навыков
  изменила раздел `AGENTS.md`, а её реестр отпечатков ещё не синхронизирован.
  Автоматический `check-updates.mjs` не запускался; все последующие проверки
  канонического контура выполнены отдельно и прошли.
- Remaining: заменить только содержательные mock-входы подтверждёнными
  материалами автора и провести нормативную приёмку GSR-030–033.

### 2026-07-12 — Codex, серверная доступность действий

- Architecture: PM принял общий механизм, зафиксированный ADR-079. Каждый
  публичный снимок deterministic- и AI-driven-сессии содержит безопасный статус
  действий; runtime повторяет полную guard-проверку при dispatch.
- Changed: сервер проецирует роль, состояние и зависимость от параметров без
  внутренних JSON-путей; Presenter, обычные кнопки, DOM-альтернатива и Phaser
  блокируют недоступное и повторный ввод. Mock-пакет объявляет понятные причины
  для открытия и применения новости.
- Product evidence: после `mock.setup.start` доступно `mock.news.draw`, после
  открытия оно становится недоступно, а `mock.news.apply.block-road` — доступно.
  Защитный повтор получает HTTP 400 без мутации и без технического пути в
  публичном сообщении.
- Verification: Runtime API 187 passed / 2 infrastructure skips до последней
  локальной правки публичного сообщения; после неё целевые complete-session и
  replay tests прошли. Player-web 157/157, runtime/player typecheck, production
  player build, OpenAPI, manifest-authoring, 6/6 тестов плагина и нейтральный
  availability contract — успешно. PostgreSQL-интеграция в общем suite
  пропущена без отдельной disposable-переменной, но живой стенд подтвердил
  readiness и восстановление сохранённой версии 2.
- Runtime: пробный стенд снова работает на `http://127.0.0.1:3000`, runtime
  readiness отвечает 200, PostgreSQL-контейнер и прежние сессии сохранены.
- Remaining: `LEGACY-0058` теперь содержит только динамический выбор грузов,
  техники, узлов и дорог и полный ручной UI-проход; методика и известные
  игровые края остаются отдельными findings.

### 2026-07-13 — Codex, приём авторского пакета

- Sources reviewed: `Начальная транспортная сеть.png`, размеры исходной карты и сети, структура двух листов `Колоди + Новости.xlsx`, AI/PDF-контуры и текст `Описания стран Гвинеи 1 стол.docx`.
- Confirmed: специальных демонстрационных грузов и контрольного хода нет; локомотивы идут справа налево; полустанок ставится на любую существующую дорогу с отступом; концы новой дороги выбирают игроки, траекторию с минимумом областей строит система.
- Changed: GSR-030 помечен `Superseded`; фаза приёма теперь нормализует реальную сеть, 174 грузовые строки, 34 новости и страны; `R-01`–`R-15` собраны для последовательного согласования.
- Architecture: текущее ADR-072 и runtime строят прямой отрезок. Новое продуктовое правило требует отдельного архитектурного предложения и согласования; публичный контракт в этой работе не изменялся.
- Next: извлечь контрольный граф и контуры, подготовить импорт authoring-данных и начать с вопроса `R-01`; независимые операции импорта не блокируются архитектурным вопросом маршрутизации.

### 2026-07-13 — Codex, утверждён интерфейс «карта — основа»

- Decision: PM принял общий декларативный режим, а не специальную страницу
  одной игры. Карта заполняет игровой viewport; web-панели располагаются
  смысловыми слоями поверх неё.
- Architecture: создан Accepted ADR-080 с `layout_mode: "map-first"`,
  ограниченным `workspaceSlot`, обратной совместимостью и независимым
  доступным DOM-путём.
- Project: создана подробная спецификация композиции, авторского ассета,
  плоскости `5079 × 3627`, фаз, камеры, адаптации и измеримой приёмки.
- Execution: добавлена независимая Фаза UI и пакет W4B; блок не зависит от
  спорного алгоритма дороги или трактовки новостей.
- Current gap: существующий canvas `650 × 464` на окне `1440 × 900` остаётся
  временным состоянием `LEGACY-0062`, а не принятым интерфейсом.
- Dependencies: новых пакетов не требуется; текущие публичные возможности
  Phaser подтверждены через Context7. Любая новая зависимость потребует
  предварительного согласования с PM.
- Next: реализовать schema-first контракт и нейтральную фикстуру, затем общий
  renderer/камеру, mock UI и нормативный UI; закрывать долг только после
  browser/visual приёмки.

### 2026-07-13 — Codex, уточнение спорных правил автора

- Confirmed: основная семантика `R-01`–`R-15`; обычное строительство открывается
  в `N+2`, государственная дорога № 27 — в `N+1`, все дороги двусторонние,
  закрытия запрещают все транспортные и грузовые операции.
- Economy: автоматический кредит и отрицательный баланс остаются запрещены;
  займ оформляется только отдельной интервенцией ведущего при обязательном
  дефиците и хранится отдельно от денег.
- Randomness: полное равенство локомотивов и равно минимальных строительных
  траекторий разрешается через воспроизводимую случайность сессии.
- Remaining content questions: для ветви исключения не определена судьба
  техники и грузов выбывшей команды. Для новости № 19 подтверждено число изъятий
  `floor(техника / 5)`, но не выбор конкретных единиц, совместный порядок
  вагонов/локомотивов и срок изъятия.
- Architecture: публичное расширение транспортной геометрии не принято в этом
  изменении; gate сужен до генерации и проверки кандидатных траекторий вместо
  прямого отрезка ADR-072.
- Validation scope: документационная синхронизация; код и generated manifests
  не менялись, поэтому полный набор тестов не запускался.

### 2026-07-13 — Codex, реализован интерфейс «карта — основа»

- Platform: UI JSON Schema и authoring-схема получили `layout_mode:
  "map-first"` и ограниченные `workspaceSlot`; компилятор требует ровно один
  `board`, а нейтральная фикстура доказывает отсутствие игрового хардкода.
- Player: общий renderer создаёт полнооконную карту, компактный статус, нижний
  лоток и две закрываемые панели. Одновременно открыта не более одной панели;
  `Escape` закрывает её и возвращает фокус.
- Board: обе Phaser-сцены имеют pan/zoom, границы, полный обзор и fullscreen;
  изменение размера контейнера обновляет canvas в том же кадре. Mock-сцена
  больше не рисует старую страницу внутри canvas.
- Assets: mock-пакет использует локальную байт-идентичную копию разрешённой
  авторской WebP-карты; синтетический SVG сохранён только как геометрическая
  тестовая фикстура. Заголовок и сцена отдельно маркируют тестовые данные.
- Verification: schema/compiler/contracts, 31 focused player tests, по 12
  тестов обоих плагинов, 9 mock-package tests, 18 asset tests, typecheck,
  проверки generated drift и production build прошли. Browser-измерения дали
  точное совпадение canvas с окнами `1920×1080`, `1440×900`, `1024×768`; на
  `390×844` панели, камера и действия не перекрываются.
- Remaining: PM проводит продуктовую визуальную приёмку, после которой
  `LEGACY-0062` можно перевести в `removed`. Нормализация реальных узлов и
  контуров `5079×3627` остаётся отдельным содержательным импортом.

### 2026-07-13 — Codex, первый этап импорта авторской геометрии

- Network: из двух совмещённых PNG сняты координаты 23 терминалов, отметки
  `π / 3,14`, кандидата полустанка `9¾` и десяти видимых дорог. Стабильный ID
  `road-3-3-14` закрепляет точную связь новости № 12.
- Safety: данные сохранены как `review-draft` с четырьмя вопросами автору;
  общий конвертер разрешает проверку и SVG-наложение, но отклоняет создание
  runtime-фрагмента до подтверждения.
- Platform proof: прежний mock-конвертер вынесен в общий schema-first модуль,
  игра хранит только свои стоимости и типы. Нейтральная фикстура доказывает
  общий формат без терминов этой игры.
- Vector source: AI содержит раздельные слои, 23 векторные шестерёнки и точные
  пути границ. Прямое масштабирование ошибочно; импортёр должен вычислять
  калибровку по всем терминалам и возвращать overlay на ручную приёмку.
- Verification: 14/14 сфокусированных тестов общего и игрового конвейера,
  10/10 тестов mock-пакета, 13/13 тестов нормативного игрового модуля и его
  typecheck прошли; строгий
  mock-фрагмент собирается; попытка публикации `review-draft` отклонена без
  создания файла. Полный проектный набор тестов намеренно не запускался после
  этого среднего содержательного блока.
- Remaining: получить четыре подтверждения сети, извлечь области из AI,
  импортировать XLSX/DOCX. Публичную геометрию автоматической дороги не менять
  до отдельного архитектурного согласования.

### 2026-07-13 — Codex, воспроизводимое извлечение векторной карты

- Vector intake: из открытых PDF-слоёв AI-файла извлечены 23 векторных центра
  терминалов и 981 верхнеуровневый путь обводки. Служебные данные Illustrator
  и вложенные графические объекты не разбираются.
- Calibration: все 23 терминала однозначно сопоставлены с черновиком сети;
  среднеквадратичная ошибка — `0,441` пикселя, максимальная — `0,697` пикселя
  в канонической плоскости `5079 × 3627`.
- Safety: результат имеет отдельную JSON Schema, неизменяемый проверенный
  снимок, `review-draft` и `publishable: false`; CLI защищает исходники от
  перезаписи, а SVG не принимает сетевые или абсолютные ссылки на фон.
- Content boundary: 981 линия не объявлена готовыми областями. Человек должен
  удалить рамку/оформление, собрать замкнутые контуры и назначить страны и
  стабильные идентификаторы.
- Architecture: PM принял ADR-081 о серверном построении дороги с минимумом
  региональных сегментов. Публичный транспортный контракт изменяется только
  через явно включаемый и обратно совместимый schema-first режим.
- Verification: 19/19 тестов общего, нормативного и векторного конвейера,
  10/10 тестов mock-пакета, проверка 2/2 скомпилированных манифестов, 1/1
  игрового модуля, свежесть векторных артефактов и `PROJECT_STRUCTURE.yaml`
  прошли. Полный набор тестов проекта намеренно не повторялся: прочие
  подсистемы после предыдущей расширенной проверки не менялись.
- Remaining: четыре подтверждения начальной сети, классификация контуров,
  импорт XLSX/DOCX и завершение реализации принятого ADR-081 на тестовых данных.

### 2026-07-13 — Codex, серверное планирование дороги по областям

- Changed: JSON Schema транспортной модели получил явно включаемый режим
  `region-segment-minimum-v1`; общий конвертер карты канонизирует области,
  выводит точные переходы и контрольный хеш. Runtime строит и атомарно
  сохраняет минимальную по областям ломаную с объяснимой стоимостью и
  воспроизводимым выбором при равенстве.
- Compatibility: игры без нового режима продолжают строить прямые дороги;
  оба CMT-интерфейса читают как новую ломаную, так и старые прямые связи.
  Полустанок делит сохранённую ломаную по полной длине без перепланирования.
- Game proof: mock-пакет использует три синтетические области и путь через них;
  нормативный пакет не получает неподтверждённые авторские контуры.
- Verification: совпадение схемных контрактов, проверка типов runtime,
  22/22 нейтральных серверных теста и 21/21 тест конвейера карты прошли.
  После исправления двух интеграционных рассинхронизаций прошли 10/10 тестов
  mock-пакета, проверки типов обоих игровых модулей, 14/14 нормативных и
  13/13 mock-тестов интерфейса, сборка обоих публикуемых модулей, целевая
  компиляция обоих game/UI-манифестов, проверка структуры и `git diff --check`.
- Verification boundary: общая проверка всех authoring-манифестов была
  остановлена после минуты без вывода; вместо неё успешно выполнены две
  целевые компиляции изменённых игр. Полный набор проекта, browser E2E и
  PostgreSQL не повторялись, потому что этот блок не менял браузерный host или
  слой хранения.
- Remaining: подтвердить реальные контуры и подключить их к нормативной игре;
  строительная архитектура больше не блокирует этот импорт.

### 2026-07-13 — Codex, полный mock-срез динамического строительства

- Lifecycle: необязательная декларация транспортной модели задаёт счётчик
  ходов, задержку, состояния и именованную причину блокировки. Дорога,
  полустанок и обе части разделённой дороги, созданные в `N`, закрыты весь
  `N+1` и активируются в начале `N+2`; независимое закрытие новостью при этом
  сохраняется.
- Selection: Phaser-карта и обычная DOM-форма синхронизируют временный выбор
  двух узлов либо точки на существующей ломаной. Черновик не сохраняется и
  сбрасывается после каждой новой версии сессии.
- Preview: `POST /action-previews/transport-road` принимает точную версию и
  только две публичные ссылки на узлы. Сервер проверяет роль, действие,
  условия и геометрию, моделирует равный выбор на копии PRNG и возвращает
  ломаную, области и цену без оплаты, seed, счётчиков или мутации сессии.
- Confirmation: DOM показывает цену и объяснение, Phaser рисует отличимую
  временную линию; изменение выбора удаляет её, а подтверждение разрешено
  только для соответствующего расчёта. Runtime повторяет тот же общий путь
  планирования и атомарно создаёт дорогу.
- Compatibility: игры без lifecycle и регионального планировщика сохраняют
  прежнее немедленное и прямолинейное поведение; platform core не содержит
  идентификаторов CMT. Новых пакетов не устанавливалось.
- Verification: серверный блок прошёл typecheck контрактов/runtime, schema
  parity, OpenAPI, 25/25 нейтральных тестов, целевую HTTP-интеграцию и 15/15
  тестов mock-пакета. Интерфейсный блок прошёл 25/25 целевых player-web тестов,
  17/17 тестов нормативного и 16/16 тестов mock-модуля, их typecheck и
  воспроизводимую пересборку обоих опубликованных bundles. `git diff --check`
  успешен; полный набор проекта на этом промежуточном блоке не запускался.
- Remaining: подтвердить и опубликовать реальные контуры областей, провести
  строительный сценарий на авторских данных и завершить ручную приёмку всех
  фаз общего экрана.

### 2026-07-14 — Codex, стадия A аудита универсальных механизмов

- Scope: read-only аудит canonical schema, runtime, authoring/compiler,
  generated contracts, editor, Presenter и всех шести исполнимых игр; публичный
  контракт, runtime behavior и игровые манифесты не менялись.
- Inventory: зафиксированы 29 schema effect-вариантов, 27 используемых ops,
  196 действий, 693 stored effects, 967 шагов после template expansion и 695
  editable effects.
  Узкими runtime-кандидатами признаны прежде всего
  `transport.construction.activateDue`, `ranking.compute`, cargo workflows
  и части vehicle relations; graph/geometry, decks и PRNG сохраняются как
  общие версионированные алгоритмы.
- Safety gate: до появления общих selector/aggregate нужен отдельный P0-блок —
  deny-by-default player projection, точная full-snapshot семантика, единые
  actor/action IDs, public-only availability, safe write paths и bounded
  request/log.
- Accepted contract: Cubica Mechanics IR `cubica.dev/mechanics/v1alpha1` —
  typed transactional plan со state model, query/assert/command/algorithm,
  ResultRef, confidentiality/integrity labels и static/runtime budgets.
  JSON Schema 2020-12/Ajv 2020 вводятся отдельным dialect parity срезом.
- First gameplay slice: полное `mock.debrief.next-turn` использует
  assert/add/set/select/update/event одной transaction, заменяет activate-due
  lifecycle и восстанавливает ресурс всем фактически active units, включая
  купленный локомотив. Обязательны neutral fixture, canonical order,
  cardinality, scan/memory/output limits и полный rollback.
- Migration boundary: постоянный `deterministic.effects` adapter запрещён.
  Старый interpreter временно служит только тестовым сравнительным эталоном в
  ветке;
  финальный lockstep cutover удаляет old schema/types/switch/subject handlers,
  translator и oracle после доступного replay, проверенной snapshot migration
  либо archive.
- Artifacts: отчёт и матрица актуализированы; добавлены ADR-084 и GSR-036,
  `PROJECT_ARCHITECTURE.md` синхронизирован.
- Verification boundary: для стадии A предусмотрены только проверки Markdown
  и `git diff --check`; runtime/schema/game tests намеренно не повторяются,
  потому что исполняемые файлы не менялись.

### 2026-07-15 — Codex, консолидация принятого ADR-084

- Scope: повторный архитектурный анализ полного write path от UI/игрового ИИ
  до session commit; исполняемые schemas, runtime и игровые пакеты не менялись.
- Intent boundary: текущий `actionId` формально становится Game Intent без
  нового `intentId`. UI и игровой ИИ видят actor-scoped Game Intents, но не
  Mechanics IR. Presenter проецирует чтение и использует опубликованный
  action binding на записи без
  command maps, fallback names, rule evaluation или lowering.
- Catalog boundary: ИИ-разработчик использует широкий operation catalog через
  релевантные mechanics capability packs. Platform modules, изолированные
  game-scoped runtime extensions и UI packs имеют отдельные namespaces,
  полномочия и version locks.
- Reliability boundary: `commandId` обозначает одну логическую команду на всех
  transport retry. Совпавший fingerprint возвращает исходный receipt без
  повторного исполнения, а reused ID с другим содержимым отклоняется. Receipt
  lookup идёт до version check; state, RNG, events, audit и receipt образуют
  один commit.
- Identity boundary: command ledger использует стабильный principal, actor
  разрешается при первом выполнении и сохраняется в receipt; хотсит retry не
  зависит от уже сменившегося active actor.
- Session/replay boundary: session pins immutable `bundleHash`,
  action/pack/compiler/module/budget/random locks; каждый internal receipt хранит
  `definitionHash` и конкретный `planHash`.
- Migration boundary: кроме старого effects-контура финальный cutover удаляет
  двойные `params/payload`, client command maps и прямые agent state effects.
  Старые sessions проходят replay только при наличии истории и старых правил,
  иначе snapshot migration либо архивируются.
- Artifacts: ADR-084 расширен; `PROJECT_ARCHITECTURE.md`,
  `runtime-mechanics-language.md`, обзор проекта, реестры и текущий план
  синхронизированы.

### 2026-07-15 — Codex, закрытие семи замечаний к ADR-084

- Scope: документационная доработка целевого контракта без изменения
  исполняемых схем, runtime или игровых пакетов.
- Command identity: внешний `cli_` создаётся случайно, внутренний `sys_`
  детерминированно выводится из session/schedule/occurrence; транспортные и
  trace IDs не используются для дедупликации, а префикс не даёт полномочий.
- Scheduler concurrency: системная команда не использует неизвестный заранее
  `expectedStateVersion`, но под session lock проверяет schedule, trigger,
  authorization и budget и атомарно потребляет occurrence вместе с receipt.
- Version locks: `moduleLock` разделяет `moduleId`, точную `moduleVersion`,
  `artifactHash` и `algorithmVersions`; нейтральное имя графового алгоритма не
  кодирует строительство дороги конкретной игры.
- Trust boundary: `playerId` из тела удаляется как источник principal/actor во
  всех mutating и защищённых preview-путях; identity приходит из
  аутентификации, actor разрешается сервером.
- Clarity: добавлены связи с принятыми контрактами экономики и регионального
  пути, определения терминов, однозначные правила конфликтующих записей и
  последнего разрешителя сортировки.
- Artifacts: синхронизированы ADR, обзор архитектуры, руководство по Mechanics
  IR, аудит, план, `NEXT_STEPS.md` и реестры legacy.

### 2026-07-15 — Codex, реализация универсального языка механик

- Scope: полный cutover Game Intent → Cubica Mechanics IR для публичного
  контракта, runtime, Presenter, editor и всех игровых пакетов репозитория.
- Platform: добавлены JSON Schema 2020-12 Mechanics IR, state model, exact
  module registry, schema/semantic validation, общий transactional executor,
  immutable bundle identity и durable command/event ledger.
- Games: Antarctica, Estate Race, Simple Choice, оба CMT-пакета переведены на
  `action.binding` и опубликованные plans; AI-driven игра выбирает один
  доступный Game Intent вместо возврата прямых state effects.
- GSR-036: `mock.debrief.next-turn` выбирает фактически активные локомотивы и
  созревшие объекты из candidate state, поэтому купленный active-локомотив
  получает ресурс без перечисления ID, а строительная причина снимается
  атомарно с остальными сбросами и событием.
- Legacy boundary: старые schema variants, template/JsonLogic executor,
  deterministic handlers, предметные runtime branches, двойной
  `params/payload` и прямые agent patches удалены из production-пути.
- Documentation: руководство автора, описание языка и GSR-036 синхронизированы
  с реализацией. В этом документационном потоке тесты намеренно не запускались;
  окончательные команды и результаты межсистемной проверки должен добавить
  основной исполнитель после её завершения.

### 2026-07-17 — Codex, финальная приёмка полной миграции Mechanics

- Outcome: полный переход всех игр и платформенных потребителей с прежних
  `deterministic.effects` на Game Intent → Cubica Mechanics IR завершён.
  Production fallback, предметные ветки старого исполнителя и двойная форма
  параметров отсутствуют; закрытые разрывы `LEGACY-0055`, `LEGACY-0063`,
  `LEGACY-0064` и `LEGACY-0065` имеют статус `removed`.
- Authoring and locks: 15/15 сфокусированных тестов компилятора и границы
  compiler-owned `moduleLock` прошли; 13/13 опубликованных манифестов актуальны;
  `verify:manifest-authoring` проверил 13 рабочих заданий и два эталонных
  authoring-примера.
- Game evidence: все authoring-задания шести игр компилируются воспроизводимо;
  CMT mock проходит 10/10 пакетных проверок и точный 88-командный
  семиходовой серверный replay, Estate Race — 8/8, модуль поля — 14/14.
- Cross-system evidence: `verify:canonical` прошёл все 17 этапов за 6 минут
  13 секунд. В том числе: contracts-manifest 55/55, contracts-ai 38/38,
  game-assets 18/18, runtime 160 passed / 2 предусмотренно skipped / 0 failed
  из 162, player-web 208/208 и production build.
- Browser evidence: единый production-прогон Player Web прошёл 2/2 сценария
  одним worker. CMT на реальной серверной случайности провёл три полных цикла
  фаз, сохранил секрет будущих карт и завершился через запрос/отмену/
  подтверждение; Estate Race прошёл видимые бросок, покупку и оплату аренды
  после восстановления той же сессии. Точный seeded 88-командный CMT replay
  остаётся серверной интеграцией, чтобы тестовый seed не попадал в production.
- Editor evidence: до финального канонического прогона editor-engine прошёл
  192/192 теста, editor-web — проверку типов и production build на 24 страницах;
  последующие изменения не затрагивали editor-код.
- Operations: ресурсоёмкие проверки запускались последовательно после
  low-memory preflight; нехватки памяти и оставшихся процессов прогона нет.
  Существующие пользовательские сервисы не останавливались.
- Not rerun: browser E2E и editor-проверки не повторялись после финальных правок
  одной контрактной тестовой фикстуры и документации, поскольку исполняемый
  runtime/player/editor-код после их зелёного прогона не менялся.
- Remaining product scope: платформа мигрирована полностью. Открыты не
  миграционные разрывы, а нормативный контент «Карт, денег, поездов», полный
  ручной проход всех фаз интерфейса, методические экраны и визуальная приёмка
  карты; они сохраняют собственные статусы и не возвращают старый runtime.
