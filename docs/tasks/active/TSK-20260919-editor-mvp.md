# TSK-20260919-editor-mvp: лаконичный MVP игрового редактора

## Оглавление

- [Status](#status)
- [Parent](#parent)
- [Why](#why)
- [Scope](#scope)
- [Plan Approval](#plan-approval)
- [Plan](#plan)
- [Execution](#execution)
- [Acceptance](#acceptance)
- [Validation](#validation)
- [Artifacts](#artifacts)
- [Decision Audit](#decision-audit)
- [Architecture Blocker](#architecture-blocker)
- [Simplification Review](#simplification-review)
- [Plan Amendments](#plan-amendments)
- [Handoff Log](#handoff-log)

## Status

in_progress

Реализованы независимое меню, структурный предпросмотр и серверная основа
отладочных сессий ADR-106. PM уточнил актуальность сохранений и поручил полное завершение MVP
с самостоятельным принятием и аудитом архитектурных решений. Выполняются
проверка совместимости снимков с текущей моделью, новая оболочка, три блока элемента,
чат правил, рисование и итоговая интеграционная проверка.

## Parent

none

## Why

Автор создаёт игру через предпросмотр, выделение, рисунок и диалог с ИИ.
Технические представления не занимают рабочее пространство. Ввод и жесты
остаются отзывчивыми независимо от компиляции и сетевого ответа.

## Scope

Продуктовая спецификация: `docs/architecture/editor-mvp-ux.md`. Одна рабочая
поверхность редактора, общий и тематический чаты, плавающее меню, три блока
панели элемента, рисование, структурный макет, сохранение отладочных точек и
полная пауза. Первый игровой пример — Antarctica; общность проверяется
simple-choice. Поддержка ещё не реализованных механик не имитируется.

Не входит: hosted studio, публикация, совместное редактирование, новый игровой
runtime, поддержка устаревших JSON/graph UI как второго пользовательского режима.

## Plan Approval

not_required — PM 2026-09-19 прямо поручил реализовать утверждённые UX-требования,
а затем разрешил продолжать **до полного
завершения**, самостоятельно принимая архитектурные решения с записью для
аудита и возможного пересмотра. Эта прямая делегация действует вместо требования
согласовывать каждую новую границу. `$cubica` отдельно не активирован.
Коммиты разрешены; перед push/слиянием нужен Astra high review.
Слияние в main отдельно не поручено.

## Plan

| Блок | Результат и владелец | Проверка и зависимости |
|---|---|---|
| B0 | Primary: проектная спецификация, актуализация ADR, рабочая копия, инвентарь образца, выявление границ | Связи, схема brief, реестр задач; без изменений чужой ветки |
| B1 | Luna high: изолированное плавающее меню с шестью иконками; Primary: визуальное направление и интеграционный контракт | Целевые component tests → отдельный Luna xhigh критик → исправление Luna → визуальная и итоговая проверка Primary |
| B2 | Sol high: композиция новой оболочки, переключение preview/chat/rules и сохранение текущего контекста | B1; не переписывать глобальный контроллер целиком; отсутствие загрузки Monaco/React Flow в MVP |
| B3 | Luna high в независимом пакете UI: структурный макет из текущего authoring; Primary: интеграция | Реальная Antarctica + нейтральный simple-choice; неизвестная логика не исполняется; Luna xhigh критика после tests |
| B4 | Sol high: выбор слоёв, три блока панели и прямые геометрические жесты через существующий mutation seam | После интеграции ADR-105 из другой рабочей ветки; stale/undo/мультидокументная целостность |
| B5 | Luna high: обычные чатовые панели и локальный черновик рисунка; Sol high: связанный документ правил и отправка мультимодального контекста | Согласованное хранение/агентский контракт; штрихи и аннотации не становятся скрытыми инструкциями; критика и визуальная приёмка |
| B6 | Sol high: серверная пауза, именованный полный снимок и продолжение; UI сценария | ADR-106 одобрен PM 2026-09-19; конкурентные и отрицательные проверки |
| B7 | Primary либо Sol high: итоговая интеграция, упрощение, полная проверка этапа | Последовательные сборки, browser flow, UX-аудит; Astra high до любого push/merge |

Немеханический Luna-блок проходит обязательный цикл: исполнитель → тесты →
другой Luna xhigh критик → исходный исполнитель исправляет → Primary принимает.
Архитектура, план, риск-решения и окончательная UI/UX-приёмка принадлежат Primary;
критические реализации — Sol high. Задания агентам — на английском.

## Execution

- B0: спецификация, исходный макет и архитектурная актуализация сохранены
  коммитом `c5f7432`. AB1 снят прямым одобрением PM.
- B1: изолированное меню реализовано. Выполнен цикл Luna high → тесты →
  отдельный Luna xhigh критик → исправления → визуальная приёмка Primary.
  Меню пока не смонтировано в старой оболочке; это задача B2.
- B3: безопасная проекция authoring и структурный макет подключены в
  PreviewStage до первой успешной компиляции. Выполнен тот же цикл критики;
  Primary заменил подобие дерева на рамки/группы, проверил узкий экран,
  выбор с клавиатуры и связь выделения с существующим инспектором.
  Добавлен production E2E для выбора узла до первой компиляции; он проверяет
  отсутствие сброса выбора старым iframe-контроллером.
- B6: Sol high реализовал транзакционную паузу, защищённые снимки, восстановление
  новой сессии и миграцию 007 для двух хранилищ. Primary подключил команды
  статуса/паузы/возобновления через проверяемый iframe bridge и HttpOnly BFF.
  Независимое Sol high ревью выявило обход блокировки checkpoint через BFF и
  устаревающий локальный флаг паузы; оба исправлены и покрыты проверками.
- B2/B4/B5: не завершены. B4 зависит от незавершённой соседней ветки ADR-105.
  Старые trace/state fixtures пока остаются в старой оболочке и не являются
  новым механизмом полного сохранения. Полная пауза любых renderer timers,
  окончательный запуск/переключение режимов и новый UI сохранений требуют
  интеграционной проверки после B2 и реализации актуального ADR-106.

## Acceptance

1. Меню содержит ровно шесть основных иконок, поддерживает приближение,
   сворачивание, горизонтальное перемещение, закрепление и клавиатуру.
2. В MVP нет дерева манифестов, raw JSON editor или диаграммы исходников.
3. Три блока элемента имеют разные роли; разовая команда не становится описанием,
   YAML пересобирается из подтверждённых исходников; отмена и stale защищены.
4. Рамка элемента меняет элемент; рамка области меняет только область задания.
5. Рисунок, надписи и промты передаются с правильной ролью и областью; несколько
   штрихов сохраняются до отправки; временный карандаш возвращает Редактор.
6. Wireframe показывает структурный макет незавершённой игры без выдуманного
   исполнения; минимальная и реальная игры используют общий путь.
7. Чат Правила показывает редактируемое правило справа; диалог и документ не
   создают конфликтующих источников истины.
8. Только серверное подтверждение означает паузу всего исполнения; полный
   сохранённый снимок возобновляет скрытое состояние и запланированные действия.
9. Выбор состояния не выдаёт дизайн-фикстуру за точный checkpoint прохождения.
10. UI проверен сильной моделью в браузере, включая узкий экран, фокус и задержки.

## Validation

- `npm run verify:legacy`, `node scripts/dev/generate-adr-index.js --check`,
  `git diff --check` для документации.
- `node skills/C_ui-from-mockup/scripts/validate-reference-brief.mjs .tmp/ui-compare/reference-brief.json --ci`;
  inventory/profile validators для образца.
- Целевые Vitest tests изменённых компонентов и `npm run typecheck --workspace @cubica/editor-web`.
- Sol review после каждого крупного завершённого блока; Primary может выполнять
  окончательное ревью сам. Предварительная Luna-критика не заменяет его.
- Только после host preflight: последовательные web builds и production
  Playwright-проверки на собственных портах; не запускать параллельно с чужими сборками.
- При завершении всего этапа — canonical checks по риску изменений. Известная
  исходная ошибка `verify:manifest-authoring`: шесть generated game manifests
  устарели ещё до этой задачи; обновление игровых артефактов не включается молча.

## Artifacts

- `docs/architecture/editor-mvp-ux.md` — единственная подробная MVP UX-спецификация.
- `draft/макет редактора.png` — исходный набросок PM.
- Временные brief, инвентарь, кропы, screenshots и логи — только `.tmp/`.

## Decision Audit

| ID | Решение исполнителя | Основание и пересмотр |
|---|---|---|
| D1 | Меню удерживается раскрытым при focus/hover подменю; список слоёв не закрывается во время выбора | Иначе управление исчезает до завершения действия; проверить браузером |
| D2 | Название элемента меняет отображаемую подпись, а не технический id | Сохраняет идентичность и ссылки |
| D3 | Промт — разовая команда, описание — постоянное намерение, YAML — производное представление | Три блока утверждены PM; исключает повтор отправки старых команд |
| D4 | Отдельная ручка перемещения меню; геометрия ограничена окном | Предотвращает конфликт click/drag; значения задержек пересматриваются по UX |
| D5 | Использовать существующие React/CSS/SVG; локальное transient state | Нет оснований для новой библиотеки окон, canvas-framework или нового движка |
| D6 | Сначала независимое меню; mutation и debug runtime не имитируются UI-флагами | Другой агент владеет mutation boundary; pause требует отдельного решения |
| D7 | 20 снимков на сессию, 8 MiB на снимок; прежний предел 7 суток отменён D16 | Ограничение объёма сохраняется, действующие точки не вытесняются молча; LEGACY-0084 теряет основание после удаления TTL |
| D8 | Wireframe ограничен 64 экранами, 256 узлами и глубиной 24 с видимым сообщением о сокращении | Отзывчивость важнее показа неограниченного дерева; значения пересматриваются по реальным играм |
| D9 | Команды отладки проходят origin-checked iframe bridge и существующую HttpOnly cookie плеера | Ключ сессии не передаётся редактору или browser JSON; неоднозначный ответ не считается подтверждением паузы |
| D10 | Серверное подтверждение паузы сравнивается с версией снимка; более новый снимок побеждает | Защищает и от запоздалого GET, и от зависания паузы после resume в другой вкладке |
| D11 | Проверка выделения по объектам iframe действует только при наличии запущенного preview; wireframe удерживает точный исходный указатель | Иначе старый контроллер немедленно сбрасывает выбор до первой компиляции; проверка закреплена в editor-wireframe.spec.ts |

| D12 — отменено D16 | Полная сборка фиксируется до запуска; точки и действующие сессии удерживают её общую копию по хешу | AB3 одобрен PM; уточнение Primary предотвращает поломку открытой восстановленной сессии после удаления точки |
| D13 — отменено D16 | После перезапуска гарантии требуют постоянных файлов и PostgreSQL; хранение сессий в памяти явно обозначается как временное | Исключает обещание долговечности только на основании сохранённых изображений |
| D14 — отменено D16 | Не вводить побайтовую дедупликацию ресурсов между разными сборками для MVP | Достаточно одной копии целого пакета для одинаковой сборки; пересмотр при измеренном объёме данных |

| D15 — отменено D16 | Отладочная сессия с удерживаемым пакетом доступна 30 суток; точка — до 7 суток в пределах срока исходной сессии; возврат создаёт новый срок | Конечное удержание без отдельной системы heartbeat/leases; опубликованные сессии не затрагиваются; значение пересматривается по использованию |

| D16 | Актуальность определяет совместимость с текущей моделью, а не пересборка/TTL; старые UI/ресурсы не удерживаются | Последнее прямое уточнение PM заменяет D12–D15. Состояние, участники, привязки и расписания валидируются перед возвратом; неподтверждённая совместимость не превращается в автоматическую миграцию |
| D17 | Одна компиляция за раз; следующая объединяет накопившиеся применённые правки; устаревший ответ не устанавливается | Сохраняет существующую задержку 800 мс, устраняет конкурирующие сборки без нового планировщика/очереди сервиса |
| D18 | Временная копия выбранной игры нужна только для кандидата ИИ до Apply; не создаётся на каждый ввод/снимок | ADR-105 требует не менять текущий authoring/preview до подтверждения; предел 2 кандидата, 128 MiB исходников + 16 MiB plugin bundles; возможности copy-on-write используются без обязательной зависимости |
| D19 | Браузер хранит только идентификаторы исходных debug-сессий; список перечитывается через защищённый API | Перезагрузка не теряет каталог; снимки и удостоверения не дублируются в localStorage. Очистка браузера и перенос на другое устройство требуют отдельного восстановления доступа |
| D20 | Обычная сборка готовится в отдельной временной области и подключается целиком под блокировкой источника | Ревью выявило запись компилятора в зарегистрированный каталог одновременно с восстановлением. Два временных корня при переключении закрывают гонку; исторический архив не вводится |


## Architecture Blocker

**AB1 — снят 2026-09-19.** PM одобрил серверную границу; принят [ADR-106](../../architecture/adrs/106-editor-debug-session-control-and-checkpoints.md). Основание первоначального блокера:
`SessionRecord` не имеет общей паузы; scheduler и AI seats исполняются после
команд/создания сессии. Нынешние trace/fixtures получают player projection,
которая скрывает server/secret/чужое состояние. Pending actions лежат отдельно
в `system_schedules`; preview-restore их не восстанавливает. Повтор старых
system command IDs в той же сессии может столкнуться с прежними receipts.

Принято PM: авторитетное server-only управление локальной preview-
сессией; пауза подтверждается на границе завершённой атомарной операции,
запрещает новые ходы, ИИ и расписания, замораживает игровое время. Уже начатая
атомарная операция завершается до подтверждения паузы; UI показывает ожидание.
Снимок хранится на сервере и включает закрытое состояние и pending schedules,
а продолжение сохранённой точки создаёт новую preview-сессию с новым журналом
команд, сохраняя точную версию игрового пакета. Закрытые данные не отправляются
браузеру. Для хранения и очистки нужен явно ограниченный срок/объём.

Альтернатива: использовать прежние видимые state fixtures как начальные условия
дизайна. Это дешевле, но не выполняет полное сохранение прохождения и не является
реализацией принятого требования. Не выбрано автоматически.

Точки доказательства: `packages/contracts/session/src/index.ts` (`SessionRecord`),
`services/runtime-api/src/modules/session/session.service.ts`,
`services/runtime-api/src/modules/runtime/runtime.service.ts`,
`apps/player-web/src/components/editor-preview-bridge.ts`,
`apps/editor-web/src/lib/editor-fixture-store.ts`,
`services/runtime-api/migrations/003_system_schedules.up.sql`.

**Зависимость AB2:** ADR-105 уже принят в соседней ветке; новой архитектуры здесь
не нужно, но интеграция её незавершённого кода зависит от владельца ветки.

**AB3 — закрыт новым продуктовым решением PM.** Первоначальное требование
точного возврата к старому внешнему виду породило предложение постоянного
пакета UI/ресурсов/плагинов. PM сначала согласовал его, затем явно отменил:
разработчик ожидает видеть текущую изменённую игру. Промежуточное предложение
удалять точки при пересборке также заменено последним уточнением PM:
сохранения хранятся, пока не противоречат текущей модели.

Актуальная граница — [ADR-106](../../architecture/adrs/106-editor-debug-session-control-and-checkpoints.md).
Серверный снимок закрыт от браузера; восстановление валидируется по текущему
состоянию модели и создаёт новую сессию с текущим пакетом правил. Метаданные
различают совместимость, несовместимость и невозможность проверки.
Постоянного хранения прежних изображений/UI нет. Начатый незакоммиченный
runtime-блок постоянных пакетов удалён его исполнителем; исходная серверная
пауза/снимки сохранены. Документальный коммит `a75cbe9` остаётся в истории как
запись пересмотренного решения; действующая документация описывает новый выбор.

## Simplification Review

Один renderer/preview host, один mutation pipeline, один источник authoring и
одна область диалога с разными темами. Дерево/Monaco/graph не заменяются новым
универсальным деревом или редакторским форматом. Меню — небольшой компонент;
частое transient state не добавляется в большой workspace-controller.
Пауза и checkpoint не сводятся к iframe-флагу: такое упрощение теряет требуемую
корректность. Обобщения для hosted/collaborative режима отложены. После независимого ревью
удалён неиспользуемый обход общего HTTP-прокси для передачи нового cookie:
до уточнения жизненного цикла он только увеличивал поверхность изменений. Хранение
снимков использует существующие транзакции; внешних зависимостей не добавлено.

## Plan Amendments

2026-09-19: после краткой паузы PM отменил удерживаемые визуальные пакеты и
уточнил совместимость сохранений с текущей моделью как критерий актуальности.
Sol high выполняет B6 — проверку состояния/участников/расписаний и атомарный
возврат в текущий пакет. Второй независимый Sol high интегрирует серверный
ADR-105 из снимка соседней ветки, не меняя её рабочие файлы. Primary реализует
B2 и объединение пересборок. Luna выполняет рисование, после целевых тестов
другой Luna xhigh нашёл граничные проблемы; исходному исполнителю переданы
исправления. Далее B4/B5, независимые ревью, производственная браузерная
проверка и полная приёмка B7. Сборки/E2E выполняются последовательно.

Цель сохраняется. AB1 обнаружен до изменения runtime и снят явным одобрением PM
2026-09-19. B6 разрешён; B1 выполняется параллельно с серверным блоком.

## Handoff Log

### 2026-09-19 — Primary

- Подготовлена изолированная ветка; исходные незакоммиченные файлы сохранены.
- Sol high выполнил ограниченный read-only аудит паузы/снимков; Luna scout
  обнаружил повторно используемые UI seams и незавершённый ADR-105 в соседней ветке.
- Создана MVP-спецификация; B1/B3 и серверная часть B6 реализованы.
- Проверки: session contracts 17/17; schema parity и API contract gate прошли;
  editor component tests 21/21; player consumer tests 102/102; runtime debug
  tests 51/51, включая настоящий PostgreSQL в удалённом после проверки
  одноразовом контейнере. Проверки типов runtime и editor прошли.
- Chromium: меню 1600×1200, 768×1024, 320×800; wireframe Antarctica/simple-choice
  1280×900 и 320×900 — без горизонтального переполнения, с рабочей клавиатурой.
  Primary просмотрел screenshots. Это проверка отдельных компонентов, не
  заявление о полном соответствии ещё не собранного интерфейса макету.
- Полный runtime-прогон дал 374/427: исходные шесть игровых артефактов содержат
  устаревший хеш cubica.core; отсутствие вложенного sanitize-html в изолированной
  установке устранено ссылкой на установленную зависимость. Найденные проблемы
  теста paused schedules и повторных PG migrations исправлены; целевые 51/51
  после исправления прошли. Игровые generated manifests не менялись.
- `npm run build:web:sequential`: обе production-сборки прошли. После изменения
  интеграции выделения повторная сборка только editor-web также прошла.
- Дополнительные проверки: debug HTTP/store 8/8, отдельное доказательство
  отсутствия вызова AI provider в паузе 1/1. Runtime и editor typecheck прошли;
  legacy register 84/84, ADR index 101 записей, инструкции 11/11, diff check.
- Серверный блок зафиксирован коммитом `e679c6f`, UI-блок — `6e1a061`.
  Production `editor-wireframe.spec.ts` прошёл (1/1) на отдельном порту,
  в отдельном Git-проекте simple-choice. Screenshot просмотрен Primary:
  wireframe и выбор работают в прежней оболочке; старый инспектор и технические
  панели будут заменены B2/B4. Это не финальная UI/UX-приёмка всего MVP.
- `npm run verify:canonical` остановился на существующем
  `verify:external-skills`: `Policy source drift: agents-execution`.
  Файлы политики/адаптера не менялись относительно `5c7438d`.
  Отдельный `verify:manifest-authoring` подтвердил шесть устаревших файлов:
  ai-driven-choice, antarctica, cards-money-trains, cards-money-trains-mock,
  estate-race, simple-choice. Runtime smoke завершился на `/readiness` 503.
  Поэтому общий canonical gate и smoke не пройдены; оставшиеся полные suites
  за остановившимся gate не повторялись. Целевые проверки перечислены выше.
- Тестовые PostgreSQL, Next.js и Chromium остановлены; собственный одноразовый
  Git-проект удалён. Ветка реализации сохранена для продолжения после AB3.
  Полные журналы и визуальные свидетельства оставлены в `.tmp/` для аудита.
- Следующее решение PM — AB3. До push/merge нужен отдельный Astra high review;
  push и слияние не выполнялись.

### 2026-09-19 — уточнение PM о совместимости

- Отменено удержание постоянных визуальных пакетов; соответствующие незакоммиченные
  runtime-изменения удалены исполнителем без затрагивания соседних работ.
- Совместимые снимки переживают пересборку. Возврат использует текущую модель
  и ресурсы; фиксированный срок 7/30 суток больше не определяет актуальность.
- Архитектура и UX обновлены; реализация совместимости и новой оболочки продолжается.
- Рисование: повторный целевой прогон Primary 6/6; Luna xhigh критика завершена,
  исправления в работе. Финальная визуальная приёмка ещё не выполнена.

### Уточнение совместимости и интеграционный аудит

- Последнее решение PM закреплено в ADR-106: совместимость с текущей моделью
  определяет актуальность; пересборка и возраст сами по себе не удаляют точки.
- Серверный блок реализован в обоих хранилищах с миграцией 008. Независимое Sol high
  ревью выявило необходимость проверки текущей фазы и роли контроллера, а также
  отдельной области компиляции перед публикацией. Исправления проверяются до приёмки.
- ADR-105 перенесён из независимой рабочей копии без её изменения. Второй Sol high
  проход устранил неканонические пути, подмену поколений кандидата, подтверждение
  неготового preview и запись производных артефактов до успеха исходного batch.
  Неуспешный откат сохраняет журнал восстановления и запрещает последующие записи.
- Компонент рисования прошёл цикл Luna high → тесты → Luna xhigh критик → исправления;
  10 проверок компонента прошли в родительском запуске. Визуальная интеграция ожидает
  сборки полного нового интерфейса.
- Каталог сохранений, мост команд и плавающее меню подключены к совместимости.
  Шесть новых проверок идентичности окна/сессии и сохранения списка прошли; общий
  интерфейс ещё не принят. Полная проверка этапа и Astra high перед публикацией впереди.

### Аудит соразмерности по запросу PM

PM указал на низкий бизнес-приоритет сохранений и запросил проверку сложности.
Primary признаёт ошибку приоритизации: обсуждение хранения и прежней сборки
заняло несоразмерную часть этапа. Граница актуальности сохраняется, но развитие
сохранений ограничено минимальным сценарием «сохранить → проверить → восстановить
в текущей игре».

Не вводятся архивы визуальных пакетов, миграция произвольных состояний, перенос
между устройствами, отдельная база каталога, очереди фоновых проверок и кэши
совместимости. Используются существующие хранилища и валидаторы. Чтение каталога
по одному снимку заменяет загрузку всех 20 payload; нового компонента нет.

Завершение уже начатого блока включает только исправление гонки публикации
сборки, проверку актуальной версии на PostgreSQL и сквозной браузерный сценарий.
Публикация готовой сборки целиком, точный кандидат изменения ИИ и общая пауза
относятся к инфраструктуре предпросмотра всего редактора и не должны учитываться
как дополнительные возможности библиотеки сохранений. После этих проверок
приоритет возвращается основным поверхностям UI: три блока редактирования,
рисование, чат и правила.

Копирование выбранной игры перед сборкой имеет измеримую цену: Simple Choice
(20 файлов, 85 955 байт) — 166–481 мс в трёх прогонах на общем хосте, Antarctica
(около 67 MiB) — около 1 с в прежнем замере. Это дополнительная цена к задержке
объединения правок, а не часть интервала 800 мс. Оптимизация копирования должна
опираться на итоговый замер полного пользовательского сценария; отдельный
универсальный граф зависимостей ресурсов ради этой функции не вводится.
