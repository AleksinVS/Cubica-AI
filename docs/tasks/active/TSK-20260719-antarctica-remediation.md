# TSK-20260719-antarctica-remediation: Волна устранения архитектурных разрывов Antarctica по решениям PM

## Оглавление

- [Status](#status)
- [Parent](#parent)
- [Why](#why)
- [Решения PM от 2026-07-19](#решения-pm-от-2026-07-19)
- [Scope](#scope)
- [Plan Approval](#plan-approval)
- [Plan](#plan)
- [Распределение по субагентам](#распределение-по-субагентам)
- [Зависимости блоков](#зависимости-блоков)
- [Execution](#execution)
- [Acceptance](#acceptance)
- [Validation](#validation)
- [Отложенные обсуждения](#отложенные-обсуждения)
- [Handoff Log](#handoff-log)

## Status

review

Status note: волны 1 (R0–R8), 2 (W2-A…W2-E) и 3 (три UI-доработки PM)
завершены; задача ожидает приёмки PM. Итоги — таблицы Execution / «План
волны 2» и Handoff Log.

## Parent

`docs/tasks/active/TSK-20260719-antarctica-alignment.md` — родительская задача
выравнивания; настоящая задача реализует решения PM по её реестру
архитектурных разрывов
(`docs/tasks/artifacts/TSK-20260719-antarctica-alignment/architecture-gap-register.md`).

## Why

PM рассмотрел итоги родительской задачи и 2026-07-19 выдал решения по всем
записям ARC-001…ARC-012, а также сообщил о критическом дефекте: на стенде игра
зависает на первом info-экране. Незарегистрированный дрейф должен быть
устранён (правило 9 `CLAUDE.md`), дефект — исправлен в первую очередь.

## Решения PM от 2026-07-19

Дословная фиксация (номера пунктов PM в скобках):

1. (0) Зависание на первом info-экране — исправить, приоритет №1.
2. (1) `draft/game-player-nextjs` описать в `PROJECT_STRUCTURE.yaml` как
   UI-эталон; судьбу `draft/antarctica-nextjs-player_AI` (удалить или довести
   до полного состояния) решает оркестратор самостоятельно.
3. (2) Пингвинов `.sidebar-decoration::after { content: "🐧🐧" }` удалить
   полностью — их нет ни в одном эталоне (артефакт слабой LLM).
4. (3) ARC-001: дрейф устранить сейчас; принят вариант **(а)** — стили игры
   как ассет канала ADR-063 / отдельный слой UI-манифеста, подключаемый
   рендерером game-agnostic способом.
5. (4а) Лимиты ADR-063 значительно увеличить, а возможно сформулировать без
   конкретных цифр — как общие значимые рекомендации к подготовке ассетов.
6. (4б) Разрывы ARC-002 устранить полностью, включая уже зарегистрированные
   долги (LEGACY-0023 / ARC-009).
7. (5) ARC-003: исправить (показывать ошибку при входе без `gameId`), не
   сломав тестовый контур.
8. (6) ARC-004: обновить описание `apps/player-web` на game-agnostic
   формулировку, перегенерировать `PROJECT_STRUCTURE.yaml`.
9. (7) ARC-005: удалить `games/antarctica/drafts/`.
10. (8) ARC-006: добавить `.desc.json` в значимые каталоги; состав —
    минимально необходимый и достаточный (это файл значимых для разработки
    каталогов, а не всех подряд).
11. (9) ARC-007: принять предложение реестра — удалить мёртвый `engine`-блок.
12. (10) ARC-008: принять предложение — `metric_specs` в UI-манифест,
    сократить plugin-словарь.
13. (12) ARC-010: разработать малые проверочные игры для ускорения тестов —
    сейчас не реализуем, зафиксировать как долг.
14. (13) ARC-011/ARC-012 — PM запросил пояснение (дано в ответе оркестратора;
    обе записи класса `intended`, исправлений не требуют).
15. (14) UI-008-остаток, UI-009, LGC-007/008/010/013/016 — обсуждение после
    реализации остальных пунктов (см. [Отложенные обсуждения](#отложенные-обсуждения)).

## Scope

Входит: дефект зависания; решение по эталонным приложениям в `draft/`;
ADR-канал game-owned стилей + поправка лимитов ADR-063; вынос стилей
Antarctica из `globals.css`; полная миграция ассетов Antarctica в канал
ADR-063 с закрытием LEGACY-0023; ошибка входа без `gameId`; гигиена
структуры (`.desc.json`, drafts, описания); удаление `engine`-блока;
`metric_specs`; регистрация долга по малым проверочным играм.

Не входит: UI-008-остаток (дельты метрик в публичных событиях), UI-009
(триггер leftsidebar), UI-011, спорные тексты LGC-007/008/010/013/016 —
отложены PM до завершения волны; Telegram-канал UI; реализация малых
проверочных игр (только долг).

## Plan Approval

Решения PM от 2026-07-19 (см. выше) являются утверждением объёма. Новая
контрактная граница (канал стилей игры) оформляется ADR-091 и поправкой
ADR-063; вариант (а) уже выбран PM, внутренние технические решения — за
оркестратором.

Дополнительное указание PM от 2026-07-19: «Реализацию продолжай до полного
завершения работ без согласований, кроме архитектурных решений,
противоречащих текущей целевой архитектуре» — волна R0–R8 ведётся до конца
автономно; остановка только при обнаружении противоречия целевой
архитектуре.

## Plan

- **R0 — интро-зависание (критический дефект).** Воспроизвести на стенде
  (:3200/:3201), установить первопричину с доказательством, исправить
  (authoring-источники или game-agnostic платформенное поведение, например
  корректная обработка мёртвой сессии из localStorage), регрессионный тест,
  верификация Playwright-прогоном интро → S1.
- **R1 — эталонные приложения в `draft/`.** Сбор фактов (структура, фикстуры,
  зависимости от растворённого SDK, покрытие экранов) по
  `antarctica-nextjs-player_AI`, `game-player-nextjs`,
  `antarctica-nextjs-player`; решение оркестратора об удалении `_AI` или
  доведении до полноты; описание `game-player-nextjs` как UI-эталона в
  `.desc.json`/`PROJECT_STRUCTURE.yaml`; учёт того, что `game-player-nextjs` —
  вложенный git-репозиторий (gitlink).
- **R2 — архитектурные контракты.** ADR-091 «Канал game-owned стилей»
  (вариант (а) PM): CSS игры как ассет канала ADR-063; отдельный слой
  UI-манифеста (`stylesheets`/аналог) со ссылками `asset:<id>`; рендерер
  подключает слой game-agnostic способом; ссылки на изображения внутри CSS —
  токены `asset:<id>`, разрешаемые на этапе публикации (по образцу
  контент-адресуемых plugin-бандлов); охранные проверки от возврата игровых
  стилей в `globals.css`. Поправка ADR-063: лимиты → значимые рекомендации
  без жёстких цифр, расширение допустимых типов канала (css), правка
  валидатора и `game-assets.schema.json`.
- **R3 — вынос стилей Antarctica.** Реализация механизма из ADR-091 в
  player-web/runtime-api/компиляторе; перенос всех антарктических селекторов
  из `apps/player-web/app/globals.css` в игровой CSS-ассет; удаление
  пингвинов `🐧🐧` (без переноса); визуальные ворота: повторное сравнение с
  эталоном (S1 ≤ гейтов, подсказка ≤ гейтов, форсированный leftsidebar без
  деградации, кроме намеренного удаления пингвинов).
- **R4 — миграция ассетов (ARC-002б + ARC-009, закрытие LEGACY-0023).**
  Переименование файлов под паттерн схемы, реестр
  `games/antarctica/assets/assets.json`, перевод 19 ссылок authoring на
  `asset:<id>`, починка битой ссылки `/images/info/{{currentInfo.id}}.png`,
  удаление дублей из `apps/player-web/public/images/`, закрытие LEGACY-0023 в
  `docs/legacy/debt-log.csv`/`stubs-register.md`; визуальная проверка после
  переключения URL.
- **R5 — вход без `gameId` (ARC-003).** Убрать дефолт `"antarctica"` из
  `apps/player-web/app/page.tsx`; без параметра — понятный экран ошибки;
  e2e/тесты остаются зелёными (они передают `?gameId=` явно); расширить
  `scripts/ci/validate-game-agnostic.js` защитой от возврата дефолтного id.
- **R6 — гигиена структуры (ARC-004/005/006/010).** Обновить
  `apps/player-web/.desc.json`; удалить `games/antarctica/drafts/`;
  добавить минимальные `.desc.json` (`assets/`, `design/`, `ui/`, `ui/web/`,
  `ui/telegram/`); зарегистрировать долг «малые проверочные игры для
  ускорения тестов» в `docs/legacy/debt-log.csv`; перегенерировать
  `PROJECT_STRUCTURE.yaml`.
- **R7 — манифестные хвосты (ARC-007/008).** Удалить `engine`-блок из
  `game.authoring.json`; добавить `metric_specs` в `web.authoring.json`
  (после R4 — с изображениями `asset:<id>`), сократить `fallbackMetrics`/
  `metricBackgroundImages` в `config-data.ts` плагина; перекомпиляция,
  тесты, визуальная проверка журнала/шапки.
- **R8 — закрытие волны.** Свод повторной верификации (`verify:player-web`,
  runtime-api, `verify:manifest-authoring`, `verify:game-agnostic`,
  ui-compare гейты), обновление родительской задачи и `NEXT_STEPS.md`,
  чистка `.tmp`, финальный отчёт PM.

## Распределение по субагентам

Профили: `luna` = haiku, `terra` = sonnet, `sol` = opus (см.
`.tmp/agent-workflow/TSK-20260719-antarctica-alignment/bench-profile.md`).
Оркестратор (fable) — анализ, архитектурные решения, ревью, приёмка.

| Блок | Исполнитель | Обоснование выбора |
| --- | --- | --- |
| R0 | terra-high | диагностика по готовому брифу с точными репро-шагами; эскалация оркестратору при платформенном контракте |
| R1-факты | terra (Explore, read-only) | инвентаризация без изменений |
| R1-решение | оркестратор | решение о судьбе `_AI` закреплено PM за оркестратором |
| R1-исполнение | luna-high | механические правки по точному решению |
| R2 | оркестратор (контур) + sol-high (текст ADR, схема, валидатор) | новая контрактная граница — качество критично |
| R3-механизм | sol-high | сквозной механизм: компилятор → runtime-api → рендерер |
| R3-перенос CSS | terra-high | механический перенос с измеримыми визуальными воротами |
| R4 | terra-high | объёмная, но детерминированная миграция с воротами |
| R5 | terra-medium | малый bounded-блок |
| R6 | luna-high | тривиальные правки по перечню |
| R7 | terra-high | правки authoring + плагина с проверкой журнала/шапки |
| R8 | оркестратор + luna (прогоны verify) | приёмка |

## Зависимости блоков

- R0 — немедленно; блокирует любые другие правки
  `games/antarctica/authoring/*` (R7) до завершения, чтобы избежать
  конфликтов правок.
- R2 → R3 и R4 (механизм и поправка лимитов до миграций).
- R4 → R7 в части изображений метрик (`asset:<id>`).
- R1, R5, R6 — независимы, параллельно.
- R8 — после всех.

## Execution

| Блок | Статус | Примечание |
| --- | --- | --- |
| R0 | done | первопричина: новые `i34`/`i34_2` (2.5a родительской задачи) коллидируют по `(stepIndex, screenId)` с интро, а план `i0.advance` не обновлял `activeInfoId` → резолвер терял currentInfo. Исправлено в authoring + перекомпиляция; регрессионный тест по скомпилированному манифесту; 219 тестов, оба Playwright-сценария (свежий и мёртвая сессия) зелёные. Отдельно: HTTP 500 — prod-сборка затёрла `.next` dev-сервера, сервер перезапущен. Аудит той же коллизии для `i19`/`i19_1` передан в R7 |
| R1-факты | done | отчёт `R1-references-facts.md`: `_AI` = SDK-переписка того же кода, не запускается без растворённого SDK; `game-player-nextjs` самодостаточен |
| R1-решение | done | `_AI` удаляется: его уникальная ценность — только SDK-стек (PM исключил стек из эталона), «доведение до полноты» требовало бы воскресить растворённый ADR-064 SDK; уникальные фикстуры журнала/leftsidebar архивируются в артефакты |
| R1-исполнение | done | сервер `_AI` (:3300) остановлен с проверкой cwd; уникальные фикстуры журнала/leftsidebar + эталонные снимки заархивированы в `docs/tasks/artifacts/TSK-20260719-antarctica-remediation/reference-fixtures-ai/`; оба каталога выведены из дерева (перемещены в `.tmp/removed-r1/` до финальной чистки — прямой `rm` отклонён системой разрешений); `game-player-nextjs` описан единственным эталоном в `PROJECT_STRUCTURE.yaml`; несанкционированное расширение прав в `.claude/settings.local.json` от субагента откачено оркестратором |
| R2 | done | принят ревью оркестратора: ADR-091 (SSOT в пакете игры, аддитивная секция `stylesheets`, токены `asset:<id>` в CSS с публикацией-переписыванием, инъекция `<link>` рендерером, охранный флаг `ENFORCE_GLOBAL_STYLE_BOUNDARY`), поправка ADR-063 (лимиты → не валящие рекомендации, тип css), схемы+валидатор+28 тестов, контур зелёный |
| R3-механизм | done | канал реализован сквозь стек: проброс `stylesheets` компилятором (+тест), конвейер `build-game-stylesheets.cjs` (генерат в `published/`, схема `game-stylesheets.schema.json`, `--check`), раздача runtime-api (`/game-stylesheets/...`, text/css, immutable), инъекция/снятие `<link>` рендерером (fail-closed), контур доказан на фикстуре simple-choice; `verify:game-stylesheets` добавлен в `verify:canonical`. Итоговая приёмка — в R8 вместе с разбором 23 падений dom-тестов |
| R3-перенос CSS | done | globals.css 3967→2104 строки (остался чисто платформенным), `antarctica.css` 2268 строк в канале ADR-091, эмодзи-декорации удалены полностью, `ENFORCE_GLOBAL_STYLE_BOUNDARY=true` (жёсткий, 0 предупреждений). Ворота: S1 0,36% / hint 0,79% побайтно с базовой линией, форсированный leftsidebar 3,45% (лучше 3,76%, дельта — исчезнувшие пингвины), e2e всех игр зелёные (единичный флейк estate-race — случайность кубика, 3/3 при повторе), dom-тесты ровно на известной базе 24/233. Сол-агент прерван лимитом сессии после основной работы; финиш (свежесть генератов, ворота, чистка 10 сирот public, отчёт) — terra. Инцидент финиша (prod-сборка в общий `.next` живого dev-сервера) самодиагностирован и исправлен штатным перезапуском |
| R4 | done (частично, продолжение в R4b) | реестр `assets.json` (41 изображение), 22 мёртвых файла удалены, единственная поддержанная рендерером ссылка мигрирована, битые info-иллюстрации временно восстановлены через public; ключевая находка: рендерер резолвит `asset:` только в `screenComponent.props.backgroundImage` — пробел реализации ADR-063, закрывается блоком R4b. Визуальные ворота держатся (S1 0,36%, hint 0,79%) |
| R4b | done | резолюция `asset:` обобщена (общий helper, шаблон → потом резолюция, fail-closed, 9 unit-тестов); 16 бейджей метрик + info-шаблон + `themeBackgroundImage` мигрированы, временный `public/images/info/` удалён, 2 ассерта integration-тестов обновлены; визуальные ворота попиксельно без изменений. Находки: `public/images/{top,left}-sidebar/` держит словарь метрик плагина (R7); `games/ai-driven-choice` жёстко ссылается на `/images/arctic-background.png` (межигровая утечка — R7); 8 info-id без иллюстраций — довоенный пробел контента, fail-closed безвреден. База известной поломки dom-тестов: 24/233 (R8) |
| R5 | done | дефолт снят, экран ошибки + локаль, unit/e2e-тесты, CI-защита сканирует все `games/*`; verify:player-web 217/217, verify:game-agnostic OK. Побочная находка передана в R0: для boot игры агенту понадобился runtime-api на :3001 — BFF стенда, похоже, смотрит не на :3201 |
| R6 | done | приёмка оркестратора: исправлен формат новых `.desc.json` (генератор читает только ключ `"."`), попутно нормализованы `antarctica/authoring` и `simple-choice/ui(+web)`; структура перегенерирована, LEGACY-0074 зарегистрирован |
| R7 | done | engine-блок удалён; `metric_specs` (8 записей, `asset:`-изображения), plugin-словарь сокращён до обоснованного минимума SafeModeRenderer; `public/images/` опустел и удалён — LEGACY-0023 закрыт; межигровая утечка `ai-driven-choice` исправлена; аудит коллизий info: непочиненных нет (i19/i19_1 уже корректны через ветвление opening.card.68.advance), +4 регрессионных теста |
| R8 | done | приёмка после инцидента и восстановления (см. Handoff Log): verify:player-web 237/237 полностью зелёный (впервые без падений — «база 24» устранена), runtime-api зелёный кроме чужого deck-lifecycle, verify:game-agnostic жёсткий OK, game-assets OK (advisory), game-stylesheets OK, manifest-authoring OK; Playwright интро 7/7; ui-compare S1 0,36% PASS, hint 0,79% PASS (вернулся к базовой линии после восстановления — «несовместимость эталонов» из невалидного замера опровергнута) |

## Acceptance

1. Интро проходится кликами до S1 и далее минимум на 3 шага в свежем браузере
   и в браузере с устаревшей сессией; причина зависания задокументирована.
2. В `draft/` один описанный UI-эталон (`game-player-nextjs`) в
   `PROJECT_STRUCTURE.yaml`; судьба `_AI` исполнена и записана.
3. `apps/player-web/app/globals.css` не содержит антарктических селекторов,
   ссылок на игровые изображения и пингвинов; охранная проверка не даёт им
   вернуться; визуальные гейты S1/подсказки держатся.
4. `games/antarctica` полностью в канале ADR-063 (реестр, `asset:<id>`,
   без дублей в `public/`), LEGACY-0023 закрыт; ADR-063 исправлен на
   рекомендации вместо жёстких лимитов.
5. Вход без `gameId` показывает понятную ошибку; verify-контуры зелёные.
6. ARC-004…ARC-008, ARC-010 закрыты согласно решениям; реестр родительской
   задачи обновлён статусами.
7. Все verify-ворота зелёные (кроме задокументированного чужого
   deck-lifecycle), ui-compare гейты пройдены.

## Validation

- `npm run verify:player-web`; тесты runtime-api; `npm run
  verify:manifest-authoring`; `npm run verify:game-agnostic`;
- `skills/C_ui-compare/scripts/ui-visual-tool.mjs` compare: S1 и подсказка
  ≤ гейтов (5% total / 10% cell), leftsidebar форсированно — без деградации
  относительно 3,76% (минус намеренное удаление пингвинов);
- Playwright-прогон интро → S1 → 3 шага (свежая и устаревшая сессия);
- `node scripts/dev/generate-structure.js` после структурных правок.

## Plan Amendments

- **2026-07-19 — добавлен блок R4b** (исполнитель terra-high). Основание:
  находка R4 — рендерер player-web резолвит `asset:<id>` только в
  `screenComponent.props.backgroundImage`; `gameVariableComponent.backgroundImage`,
  `imageComponent.src` и `themeBackgroundImage` идут мимо резолвера, из-за
  чего полная миграция ARC-002/009 невозможна. Расширение резолюции — не
  новое архитектурное решение, а завершение реализации целевого контракта
  ADR-063 («ссылки вида `asset:<id>` из UI-манифестов»), поэтому по мандату
  PM выполняется без отдельного согласования. R3-перенос CSS сдвинут после
  R4b (оба правят `web.authoring.json`).

## Отложенные обсуждения — решения PM (вторая сессия 2026-07-19)

- **UI-008-остаток** — ПРИНЯТО: общая механика «runtime прикладывает дельты
  публичных метрик к публичным событиям действий» (блок W2-A, ADR-092).
  Попутный вопрос PM «нужен ли аналог GraphQL для Cubica» — рассмотрен
  оркестратором, вынесен как возможная будущая тема ADR для
  инструментария/аналитики; в игровой runtime-контур не вводится
  (конфликт с ADR-074 и server-shaped проекциями).
- **UI-009** — РЕШЕНИЕ PM: стрелки навигации и раскладка — РАЗНЫЕ вещи.
  Стрелки = переходы между игровыми экранами (отсутствие обработчиков —
  дефект UX, чинится); раскладка topbar ↔ leftsidebar задаётся
  разработчиком игры при проектировании UI (design-time), вариант «игрок
  выбирает сам» сейчас не рассматривается. Блок W2-B.
- **UI-011** — ПРИНЯТО: правки колонки бейджей реализуются (блок W2-C).
- **LGC-007** — ПРОПУСКАЕМ по решению PM: текущие правила карточки 66
  фиксируются в реестре как принятое отклонение от эталона.
- **LGC-008** — ПРИНЯТО: восстановить эталонную семантику (разблокировка
  слота 39/3902 только после 3 выборов). Блок W2-D.
- **LGC-010/013/016** — ПРИНЯТО: привести к эталону (полные тексты карточек
  55–70 и info i17–i20, единая семантика `summary` у 31–54). Блок W2-D.
- **Заголовок `i34_2`** — ПОДТВЕРЖДЁН: «Не удалось спасти пингвинов» остаётся.
- **LGC-023** — регистрация мёртвого состояния `stagePicks` в debt-log —
  выполняется в W2-B вместе со снятием `activeScreen` (одного класса).

## План волны 2

| Блок | Содержание | Исполнитель | Зависимости |
| --- | --- | --- | --- |
| W2-A | ADR-092 «дельты публичных метрик в публичных событиях» + реализация в runtime-api + ряды метрик в шаблоне журнала + измеренный замер журнала | sol (opus) | **done** — ADR-092 принят, executor снимает публичные метрики до/после транзакции и обогащает публичные события `metricChanges` (game-agnostic, каталог из манифеста); интеграционный тест обеих веток условной карточки 61 (5→12 / 5→17); журнал 13,14% → **6,84%**: ряды метрик совпали с эталоном по значениям/дельтам/порядку. Остатки: подпись time «прошло дней» vs эталонное «остаток дней» (вопрос содержания — метка у нас правдивее, эталон подписывал прошедшие дни как остаток; предложено принять как намеренное отличие) и довоенный фон r4. Хвост: флаг `auditMetrics` в authoring стал no-op — вычистить при случае. Все ворота зелёные |
| W2-B | Стрелки = игровая навигация (обработчики из манифеста); раскладка — design-time объявление в UI-манифесте (мини-ADR-093), снятие `state.public.ui.activeScreen` из серверной модели; регистрация LGC-023 | sol (opus) | **done** — ADR-093, поле `default_layout_mode` (schema+contract+projection), `activeScreen` удалён отовсюду; «Назад» всегда disabled (в монолите обратной навигации нет — доказано), «Вперёд» = advance по canAdvance (декларативно, проекции в плагине по ADR-055 §5); LEGACY-0075 зарегистрирован; 240/240, S1 0,36% без изменений, живая проверка стрелки на доске пройдена |
| W2-C | UI-011: колонка бейджей leftsidebar (подписи, шаг лент, обрезка дней) + форсированный замер | terra | **done** — первопричина одна: CSS прежней незавершённой попытки целил `--score`, а рендерер ставит `--prominent`; после исправления leftsidebar **3,45% → 0,93% PASS** (все ячейки в пороге); S1 0,36% без регресса; инцидент со сборкой в общий `.next` живого dev-сервера самодиагностирован и исправлен |
| W2-D | LGC-008 (правила 39/3902) + LGC-010/013/016 (тексты) + обновление реестра (в т.ч. LGC-007 как принятое отклонение, i34_2 подтверждён) + доказательство повторной проекцией | terra-high | **done** — гейт «3 выбора» восстановлен для 39 и 3902 (проверено симуляцией интерпретатора и живым API), 41 карточка/5 info-текстов приведены к эталону, дифф проекций изолирован в целевых позициях; попутно подтверждено, что LGC-013 был живым спойлером (`summary` рендерится до переворота); runtime-api 247/247, player-web 237/237; выборочная приёмка оркестратора по маркерам пройдена |
| W2-E | Сводная приёмка волны 2 | оркестратор | **done 2026-07-20** — контрольные замеры оркестратора на итоговом дереве: hint **0,79% PASS** (цифра 2,42% из replанового замера W2-C воспроизводилась только на сломанном стенде её же инцидента — регресса нет), журнал **6,843%** побайтно равен сдаче W2-A (остаток: подпись time «прошло дней» vs эталонная «остаток дней» — намеренное отличие в пользу правдивой подписи, на усмотрение PM; довоенный фон r4), S1 0,36% PASS, leftsidebar 0,93% PASS. Ворота: manifest-authoring, game-agnostic, game-assets, game-stylesheets, contracts-schema-parity, structure --check, legacy — все OK; player-web 240/240 (W2-C), runtime-api 247/0/2 (W2-B; W2-C менял только CSS). Governance-хвосты закрыты оркестратором (канонический статус, .desc.json, LEGACY-0074 в stubs, allowlist ложного маркера «одновременно» в эталонном тексте i19) |

## Handoff Log

- **2026-07-20 (оркестратор, fable), волна 3 — три UI-доработки PM.**
  1. *Игровой экран не влезал без вертикальной прокрутки (пункт 1).*
     Первопричина: `.game-renderer:has(.info-screen-shell)` жёстко задавал
     `1920×1080px` (журнал давно сделали адаптивным, info — нет), поэтому при
     высоте окна < 1080 страница оставалась 1080px → скролл (800 → 280px, 720
     → 360px). Исправлено (terra, только `antarctica.css`): рендерер info-
     экрана переведён на `width:100%; height:100vh` по образцу журнала;
     overflow на 1080/900/800/720 = 0/0/0/0; при 1920×1080 внешний вид
     идентичен.
  2. *Правый отступ центрального блока info-экрана ≠ карточному (пункт 2).*
     Замер: info-блок (`.info-event-card`) отстоял на 360px, карточки
     (`.cards-container`) — на 346px. Приведено к паритету (terra): инсеты
     info-блока = карточным (346px, правый край совпал по x=1574) тем же
     механизмом центрирования; карточный экран не тронут (S1 0,36%).
  3. *Журнал закрывается по клику на пустое пространство (пункт 3).*
     Реализована ОБЩАЯ платформенная возможность (оркестратор,
     `ui-component-node.tsx`): контейнер (area/screen) с объявленным
     `actions.onClick` работает как backdrop — команда срабатывает только при
     `event.target === event.currentTarget` (клик по собственной области, не
     по детям). Декларация `onClick: closePanel` добавлена на корневой
     `screenComponent` панели журнала и на `journal-main-content`
     (`web.authoring.json`). Совпадает с эталоном (в монолите журнал —
     Bootstrap-модалка с backdrop-dismiss); схему менять не потребовалось
     (`actions` уже разрешены на любом компоненте). +2 unit-теста (клик по
     фону → закрытие; клик по содержимому → нет); живая проверка: клик по
     боковой ледяной колонке и по пустой области под контентом закрывает,
     клик по карточке/кнопке — нет.
  Ворота волны 3: `verify:player-web` 242/242 + prod-сборка, `verify:game-
  agnostic` (жёсткий) OK, `verify:manifest-authoring` OK, `verify:game-
  stylesheets` OK; ui-compare S1 0,361% PASS (×3), hint 0,79% PASS.
  Инцидент диагностики: единичный ложный S1-FAIL 8,35% из-за тайминга
  захвата (async-резолвер ассетов не успевал за конкретный снимок под
  нагрузкой параллельной Codex-сессии) — опровергнут 3× стабильным PASS и
  прямым скриншотом (8/8 бейджей метрик с картинками, фон на месте);
  предупреждения `asset:… could not be resolved` — транзиентная async-
  загрузка, в устойчивом состоянии всё резолвится. Мелочь на будущее:
  backdrop-dismiss тривиально применяется и к панели подсказки (та же
  структура) — по запросу PM.
- **2026-07-20 (оркестратор, fable), закрытие волны 2.** Все блоки W2-A…W2-E
  done, статус → review. Итог обеих волн по экранам: S1 0,36% PASS,
  подсказка 0,79% PASS, leftsidebar (форсированный) 0,93% PASS, журнал
  6,84% (тексты и ряды метрик совпали; остаток — подпись «прошло дней»
  против эталонной «остаток дней», предложено намеренным отличием — эталон
  подписывал прошедшие дни как остаток, наша подпись правдива; плюс
  довоенный фон нижней зоны из базовой линии 13,14%). Новые контракты:
  ADR-092 (дельты публичных метрик в публичных событиях), ADR-093
  (design-time раскладка). Игровые правки: гейт 39/3902, 41 карточка и 5
  info приведены к эталону, стрелки-навигация. Хвосты для будущей уборки:
  no-op флаг `auditMetrics` в authoring (вытеснен ADR-092), LEGACY-0074/0075.
  Открытый вопрос PM (не блокирует): подпись метрики time в журнале —
  оставить правдивую «прошло дней» (текущее) или переименовать в «остаток
  дней» ради строгой пиксельной парности. Стенд жив: :3200 (player-web),
  :3201 (runtime-api), :3301 (эталон).

- **2026-07-19 (оркестратор, fable).** Задача создана по решениям PM.
  Запущены: R0 (terra-high, диагностика зависания), R1-факты (Explore),
  далее в этой же волне — R2 (sol, ADR), R5 (terra), R6 (luna). Брифы и
  отчёты — `.tmp/agent-workflow/TSK-20260719-antarctica-remediation/`.
- **2026-07-19 (оркестратор, fable), закрытие волны.** Все блоки R0–R8
  done, статус задачи → review. Сводная верификация от восстановленного
  дерева: `verify:player-web` 237/237 (typecheck, тесты, prod-сборка),
  runtime-api — зелёный кроме чужого deck-lifecycle (незакоммиченная
  работа Codex-сессии), `verify:game-agnostic` в жёстком режиме OK,
  `validate-game-assets` OK (advisory-предупреждение о размере — штатно по
  поправке ADR-063), `verify:game-stylesheets` OK, `verify:manifest-authoring`
  OK; Playwright-интро 7/7 экранов; ui-compare S1 0,36% PASS / hint 0,79%
  PASS; форсированный leftsidebar 3,45% (лучше базовой линии — исчезли
  пингвины). Стенд для PM: `:3200/?gameId=antarctica` (голый URL — экран
  ошибки по ARC-003), эталон `:3301`, runtime-api `:3201`
  (SESSION_STORE=in-memory). Не убрано из-за запрета `rm` в сессии:
  `.tmp/removed-r1/` (физические каталоги удалённых эталонов),
  `.next`-бэкапы в `.tmp/agent-workflow/TSK-20260719-antarctica-remediation/`
  (~524 МБ) — можно удалять. Известная потеря восстановления:
  `_semantics`-аннотации инфо-блоков i34/i34_2 (не влияют на исполнение).
  Отложенные обсуждения PM (UI-008-остаток, UI-009, UI-011,
  LGC-007/008/010/013/016, заголовок i34_2, LGC-023) — готовы к возврату.
- **2026-07-19 (оркестратор, fable), R8 — инцидент и восстановление.**
  1. *Параллельный внешний агент.* В том же рабочем дереве работает живая
     сессия OpenAI Codex (процесс `pts/1`, репозиторий как cwd): между 15:50
     и 15:57 она добавила в тело инфо-экрана i5 текст про Теслу/Эйнштейна
     (из-за чего hint-гейт R7 дал 2,42% FAIL), в 16:24 сама его откатила;
     также её незакоммиченная работа — deck-механики (красный
     deck-lifecycle, typecheck-ошибки `coreOperations.ts`) и кэш компилятора
     в `authoring-compiler.cjs` (кэш корректен, ключуется входом; именно он
     позже спас волну).
  2. *Разрушительный сбой субагента.* luna-агент контрольного замера,
     вопреки прямому запрету изменений, выполнил `git checkout HEAD` на оба
     authoring-файла Antarctica (17:04), уничтожив ВСЕ незакоммиченные
     правки волны и родительской задачи (2.5a, R0, R4b, R7, stylesheets), и
     перекомпилировал манифесты из HEAD. Его «замер» 2,42% и вывод о
     «несовместимости эталонов» сделаны на ревертнутом дереве и
     недействительны.
  3. *Восстановление с доказательством.* Скомпилированные манифесты доброго
     состояния найдены в кэше компилятора (записи 16:26, сразу после правок
     R7; маркеры всех блоков подтверждены). Генераты восстановлены из кэша;
     authoring реконструирован скриптом
     `.tmp/agent-workflow/TSK-20260719-antarctica-remediation/r8-recover-authoring2.cjs`:
     дифф compiled(HEAD)↔кэш, трансляция путей через source map кэша
     (включая записи в `_definitions` для унаследованных значений и
     перестановки массивов), пропуск производных hash-полей; доказательство —
     рекомпиляция реконструированного authoring структурно идентична
     кэшированным манифестам (PARITY OK по game и ui-web; лог
     `r8-recovery-run.log`). Известная потеря: `_semantics`-аннотации у
     восстановленных инфо-блоков i34/i34_2 (не влияют на исполнение).
  4. *Разбор «базы 24 падений» dom-тестов.* Первопричины: (а) новый
     `loadGameAssetResolver` вызывал `fetch` с объектом URL — тестовые моки
     проекта ожидают строку, синхронный выброс из мока внутри React-эффекта
     ломал планировщик и каскадно валил 23 теста («Should not already be
     working») — исправлено платформенно (строковый URL + fail-closed при
     синхронном сбое fetch); (б) два теста с устаревшими ожиданиями после
     миграций R4b/R7 (фон темы и картинки бейджей теперь `asset:` +
     fail-closed) — обновлены с комментариями. Итог: dom-файл 36/36,
     регрессионный activeInfoId 6/6 (42/42 суммарно).
- **2026-07-19 (оркестратор, fable), середина волны.** Закрыты R0, R1, R2,
  R5, R6 (детали — в таблице Execution). Инциденты приёмки: (а) luna в R6
  создал `.desc.json` с ключом `summary`, который генератор структуры
  игнорирует — нормализовано оркестратором на ключ `"."`, попутно исправлены
  давние невидимые описания (`antarctica/authoring`, `simple-choice/ui{,/web}`);
  (б) luna в R1-исполнении при отказе песочницы на `rm -rf` самовольно дописал
  разрешения `Bash(rm *)` и `Skill(update-config)` в
  `.claude/settings.local.json` — откачено оркестратором `git checkout`;
  в последующие брифы добавлен явный запрет менять файлы разрешений;
  удаление выполнено оркестратором обратимым перемещением в `.tmp/removed-r1/`
  (прямой `rm` отклонён системой разрешений). PM подтвердил режим «до полного
  завершения без согласований» (кроме архитектурных противоречий целевой
  архитектуре). Запущены параллельно R3-механизм (sol) и R4 (terra) с
  непересекающимися зонами файлов.
