# Контрольный список готовности реализации «Карты, деньги, поезда»

Список используется оркестратором и исполнителями как проверка входов и результатов корневой задачи. Он не заменяет план TSK и не разрешает самостоятельно принимать архитектурные решения.

## Оглавление

- [1. До изменения контрактов](#1-до-изменения-контрактов)
- [1.1. После полной миграции Mechanics](#11-после-полной-миграции-mechanics)
- [2. Для каждого платформенного среза](#2-для-каждого-платформенного-среза)
- [3. Приём авторского содержимого](#3-приём-авторского-содержимого)
- [4. Перед GSR-031](#4-перед-gsr-031)
- [5. Перед GSR-032](#5-перед-gsr-032)
- [6. Перед GSR-033](#6-перед-gsr-033)
- [7. Полная тестовая партия GSR-035](#7-полная-тестовая-партия-gsr-035)
- [8. Интерфейс «карта — основа»](#8-интерфейс-карта--основа)
- [9. Перед заявлением о завершении](#9-перед-заявлением-о-завершении)

## 1. До изменения контрактов

- [x] PM подтвердил минимальное решение ведущего; ADR-071 принят.
- [x] PM подтвердил динамическое строительство road/waypoint; ADR-072 принят.
- [x] Статусы принятых ADR синхронизированы с `PROJECT_ARCHITECTURE.md`.
- [x] PM подтвердил ADR-074 о безопасных ссылках на созданные в сессии объекты.
- [x] Для изменения выбраны только возможности, необходимые полной mock-партии GSR-035.
- [x] Классификация «общая / только для игры» записана в плане и журнале передачи результата.
- [x] Общие контракты спроектированы в JSON Schema; серверные проверки исполняют схемные контракты и не заменяют их.
- [x] Для каждого общего схемного изменения был назначен один владелец; сборка mock-пакета потребляет принятую форму контракта.

### 1.1. После полной миграции Mechanics

- [x] Все опубликованные игры используют Game Intent → Cubica Mechanics IR;
  старый production-контур `deterministic.effects`, его обработчики и адаптеры
  удалены.
- [x] Граница principal/actor, ограниченные параметры, проверка версии,
  долговечная квитанция команды, неизменяемый пакет сессии и системный
  планировщик входят в готовую платформенную базу.
- [ ] Каждое новое нормативное правило игры сначала собрано из существующего
  каталога операций в authoring-макросе и опубликованном Mechanics-плане.
- [ ] Клиент отправляет только `actionId`, ограниченные `params`,
  `expectedStateVersion` и стабильный `commandId`; UI не строит IR, не
  передаёт доверенную личность и не дублирует серверные правила.
- [ ] Если существующего языка недостаточно, основной агент подготовил
  понятное архитектурное предложение, получил согласование PM и только после
  этого изменил ADR, общую архитектуру, JSON Schema и runtime.
- [ ] Субагенту переданы точные пути, принятые решения, границы файлов,
  ожидаемый результат и проверки; основной агент независимо проверил его diff
  и доказательства.
- [x] Общий исполнитель движения запрещает въезд в закрытый узел и выезд из
  него; игровые макросы запрещают погрузку и выгрузку на закрытом терминале.
  Два целевых теста подтверждают атомарный отказ, `LEGACY-0070` закрыт.

## 2. Для каждого платформенного среза

- [x] Есть нейтральные фикстуры без названий, цветов и терминалов этой игры.
- [x] Есть позитивные и негативные схемные/контрактные фикстуры.
- [x] Есть runtime-тесты успешных действий и атомарных отказов.
- [x] Presenter не раскрывает будущий порядок колод и другие серверные секреты.
- [x] Runtime проецирует безопасную доступность Game Intents по фазе и состоянию;
  Presenter, DOM и Phaser блокируют недоступный или повторный ввод. Узлы,
  дорога, точка полустанка, локомотив, вагон и груз выбираются из текущего
  публичного снимка без закреплённых fixture ID; runtime, а не браузер,
  проверяет допустимость. Динамический рынок и полный ручной путь остальных
  фаз остаются в `LEGACY-0058`.
- [x] В generic runtime/player нет условия по идентификатору игры.
- [x] Документация описывает фактическую форму реализованного mock-среза.
- [x] Сгенерированные контракты и манифесты пересобраны и проверены на рассинхронизацию.

## 3. Приём авторского содержимого

GSR-030 отменён автором: демонстрационных грузов и контрольного хода в
электронной версии нет. Новые полноразмерные mock-наборы больше не создаются:
нормативную игру наполняем реальными материалами. Работающая тестовая копия
остаётся регрессионным стендом — повторяемой технической проверкой уже готовых
механизмов. После первой реальной партии её игровые сценарии можно сократить,
но нейтральные проверки общих возможностей платформы удалять нельзя.

- [x] Передана начальная сеть поверх общей карты того же размера.
- [x] Переданы AI/PDF-контуры стран и областей.
- [x] Передан XLSX: 174 грузовые строки и 34 новости.
- [x] Переданы описания стран в DOCX.
- [x] Ограниченный реальный сценарий `1 → 9` проходит через обычный map-first
  экран ведущего: положительная ветвь выполняет новость № 24, перевозку и
  расчёт, отрицательная ветвь новости № 11 показывает атомарный отказ.
  Временная browser-копия создаётся только в `.tmp/editor-worktrees`,
  восстанавливается после перезагрузки и не меняет нормативный
  `runtimeReady: false`.
- [x] Извлечены 25 точек и 10 связей, создано контрольное наложение; результат
  защищён статусом `review-draft` и не публикуется в runtime.
- [x] Из AI воспроизводимо извлечены 23 векторных центра терминалов и 981
  кандидат линии; калибровка к карте проверена с максимальной ошибкой
  `0,697` пикселя, создано отдельное непубликационное SVG-наложение.
- [x] 981 кандидат разложен в проверочном SVG на 18 устойчивых исходных
  стилевых групп с легендой и ID каждой линии; 6/6 сфокусированных тестов и
  проверка свежести прошли.
- [x] Автор подтвердил смысл сети: шестерёнки являются терминалами, включая
  `3,14`; зелёная точка — единственный начальный полустанок; новые полустанки
  создаются динамически; PNG показывает начальную сеть.
- [x] Автор подтвердил, что все показанные точки и десять дорог начальной
  схемы открыты.
- [ ] Контрольное наложение сети передано автору и визуально подтверждено;
  готовность автора выполнить сверку уже получена.
- [ ] Из 981 кандидата отобраны игровые границы, собраны замкнутые контуры
  областей и подготовлено итоговое контрольное наложение. Стабильные
  идентификаторы 10 стран уже назначены отдельно от этой геометрической работы.
- [x] Без изменения XLSX извлечены и схемно проверены 174 строки грузов и 34
  новости; производный файл непубликационный, контрольная сумма и номера строк
  сохранены.
- [x] В защищённый intake воспроизводимо импортированы и схемно проверены
  10 описаний стран и 23 метки терминалов. Все 10 стран получили стабильные
  идентификаторы, терминалы 1–23 связаны со странами, а описания доступны через
  выбранный терминал и отдельный каталог. Полигоны областей остаются отдельным
  незакрытым входом для пространственных правил.
- [x] Связь новости № 12 уточнена: закрывается только `3 ↔ 3,14`, соседние дороги не затрагиваются.
- [x] Автоматический кредит/минус запрещены, добровольная операция при недостатке денег отклоняется, продажа возможна только в рынке.
- [x] Определены два исхода нехватки денег для обязательного платежа: исключение команды либо явный займ ведущего; реализацию и тесты ещё нужно выполнить.
- [x] Подтверждено: локомотивы и вагоны исключённой команды возвращаются на
  рынок, долги списываются.
- [x] Подтверждено: незавершённые грузы исключённой команды остаются на
  терминалах, где находились их вагоны. Реализацию и проверку ветви исключения
  ещё нужно выполнить.
- [x] Карта зарегистрирована; PM подтвердил, что использование согласовано с автором и правообладателем и не несет правового риска.
- [x] Нормативный манифест использует реальную авторскую карту, но сохраняет
  `runtimeReady: false`. Новости № 19/28/29, предложение одной карты, все
  составы 4–12 и повторяемый рынок уже исполняются. До готовой партии остаются
  визуальная проверка начальной сети, реальные полигоны областей, ветви
  обязательного дефицита и полный финальный сценарий.
- [x] На тестовых данных готовы команды, фазы, деньги, граф, роль ведущего и Phaser-host; это не закрывает авторские входы выше.

## 4. Перед GSR-031

- [x] Переданы тексты 34 новостей и 174 строки грузовых направлений/выплат.
- [x] Оба листа XLSX воспроизводимо извлечены; новости № 1–10 связаны с 62
  добавляемыми строками грузов, а 112 строк отнесены к базовым терминальным
  колодам.
- [x] Все 34 новости преобразованы в однозначные исполнимые эффекты.
  № 19 серверно рассчитывает совместную квоту и позволяет ведущему выбрать
  конкретную технику по решению команды; № 28/29 платят при первом выходе
  груза из подходящего терминала и сбрасываются перед следующим новостным
  этапом.
- [x] Новость № 14 включает постоянный прогрессивный налог. При каждом
  последующем завершении обслуживания сервер заново считает активную технику:
  гильдия платит по монете за каждый локомотив сверх трёх, перевозчик — за
  каждый вагон сверх пяти. Нехватка денег откатывает все списания, счётчики,
  журнал и переход фазы одной транзакцией.
- [x] Новость № 16 без клиентского списка команд выбирает не более 12
  фактически существующих команд с балансом больше 15 монет, атомарно
  списывает по 5 монет каждой, журналирует новый баланс и переводит фазу к
  обслуживанию.
- [x] Новость № 22 устанавливает сбор первого движения в текущем ходу.
  Первое успешное движение каждого локомотива атомарно списывает 1 монету,
  сохраняет номер хода и событие; повторное движение не оплачивается,
  недостаток денег откатывает весь ход, а явный пропуск остаётся доступен.
- [x] Подтверждено: одна строка XLSX равна одной физической карте, одинаковые
  строки — отдельные экземпляры; набор `Колода новости N` добавляется
  соответствующей одноразовой новостью до конца игры.
- [x] Определено: невыбранная карта возвращается, выполненная не возвращается,
  пустой терминал ждёт новости; 34 новости не повторяются, затем начинается
  период застоя.
- [x] Уточнено поведение терминала, если в его колоде осталась ровно одна
  грузовая карта (`R-27`): она показывается единственным вариантом; выбор и
  возврат предложения сохраняют физический жизненный цикл карты.
- [x] Передана обычная конфигурация 4–12 команд: при нечётном количестве
  перевозчиков на одну больше, при чётном команды делятся поровну; названия
  свободные, цвета контрастные, пока один человек на команду. Все девять
  допустимых сочетаний исполняются, неверное соотношение отклоняется атомарно.
- [x] Передано стартовое размещение: команды выбирают станции, порядок команд
  случайный, ведущий ставит технику по указанию команд; не более двух
  локомотивов на станции.
- [x] Устранено расхождение главного сценария: в первом обычном ходу новости
  нет.
- [x] Подтверждена специализация рынка: перевозчики покупают только вагоны,
  гильдии — только локомотивы. Цены `10/4`, `5/2` и обслуживание
  `1` за технику/удерживаемый груз уже заданы главным сценарием.
- [x] Подтверждено отсутствие дополнительных ограничений на запас
  локомотивов/вагонов на рынке и количество техники у команды. Повторяемые
  покупки и продажи исполняются с серверной проверкой денег, специализации,
  цен/запретов новостей, станции и допустимости продажи.
- [x] Подтверждён порядок локомотивов справа налево.
- [x] Закрыто полное равенство порядка: восточное положение → деньги → число локомотивов → воспроизводимая случайность.
- [x] Непубликационная серверная граница один раз сохраняет этот порядок,
   назначает каждому активному локомотиву 5 единиц действия и проводит
   журналируемый сценарий явного пропуска всех локомотивов до расчётов.
   Идентификатор текущего локомотива не принимается от интерфейса.
- [x] Текущий локомотив перемещается по выбранной дороге технической сети
  существующим общим графовым исполнителем. Дорога/станции, смежность и
  вместимость проверяются сервером; прицепленный вагон движется атомарно,
  списывается одна единица действия, а очередь не переключается до явного
  пропуска. Ведущий выбирает дорогу кликом на карте или в доступной форме;
  оба пути передают только идентификатор дороги. Рынок и выбор грузов пока не
  соединены с этой границей.
- [x] Ведущий отмечает и снимает отметку с одного публичного вагона кликом на
  карте или через доступную форму. Сервер хранит выбор на вагоне для текущего
  локомотива, поэтому обновление страницы не теряет незавершённый состав.
- [x] Подтверждение состава не принимает список или локомотив от клиента:
   сервер заново выбирает всю отмеченную группу, атомарно прицепляет её в
   каноническом порядке, списывает ровно одну единицу действия и очищает
   отметки. Ошибка любого вагона и превышение вместимости не оставляют
   частичного сцепления или списания; пропуск локомотива также очищает выбор.
- [x] Обычная погрузка принимает только `wagonId` и `cargoId`. Сервер
  проверяет статус груза, владельца и специализацию команды, пустой активный
  вагон, общую сеть, положение на открытом исходном терминале и сохраняет
  точную запись игрового журнала.
- [x] Обычная доставка принимает только `wagonId`. Груз, локомотив, обе
  команды, кратчайший открытый маршрут, банковская выплата с бонусом и тариф
  по две монеты за ребро выводятся из серверного состояния. Переводы,
  журналирование, отцепление и завершение груза входят в одну транзакцию;
  недостаток денег либо закрытый маршрут не оставляют частичного результата.
- [x] Грузовой этап нельзя завершить с открытым предложением, а расчётный этап
  можно завершить после отклонённой доставки, чтобы обязательный дефицит не
  создавал автоматический кредит и не блокировал решение ведущего.
- [x] Доступная форма показывает только видимые карты и принимает для погрузки
  вагон с грузом, а для доставки — только вагон. Команда, локомотив, маршрут,
  выплата, бонус и тариф не являются полями клиента; загруженный вагон
  сохраняет отдельный знак после завершения анимации.
- [x] Mock-колоды используют воспроизводимую случайность и скрывают будущий порядок.
- [x] Mock-рынок, выбор груза, движение и расчеты имеют контрольный транскрипт.
- [x] Ограниченный replay на реальных данных проводит новость № 24, груз
  `1 → 9`, выплату `13 + 3`, тариф 2 и отрицательную ветвь новости № 11.
  Фикстура непубликуема и не закрывает открытые пункты о колодах, командах и
  стартовом размещении.
- [x] Подтверждённые нечётные конфигурации 5/7/9/11 исполняются от завершённой
  расстановки через явный пропуск новости первого хода и обязательное
  обслуживание до рынка. Повтор, недоступная единица, раннее
  завершение и недостаток денег проверены как атомарные отказы; новость № 25
  завершает этап без списаний.
- [x] Закрытая команда без параметров завершает рынок без сделок, не меняет
  деньги, технику, объекты и рыночное состояние и переводит сессию к грузам.
- [x] После строительства без объектов закрытая команда отчётности фиксирует
  в журнале завершённый ход, увеличивает его номер ровно один раз и открывает
  следующую новость. Постоянные цены, объекты и последовательность
  строительства сохраняются; временные эффекты сбрасываются только действием
  следующей новости или периода застоя.
- [x] Технический цикл можно повторять без покупок и строительства. Это не
  закрывает настоящий рынок, приоритет грузов, реальную геометрию областей или
  содержательную отчётность с учебной рефлексией.

## 5. Перед GSR-032

- [x] Переданы исходные файлы; сеть извлечена в `review-draft`, AI разобран и
  откалиброван по 23 терминалам. Получены 981 непубликационный кандидат;
  18 стилевых групп классифицируются отдельным review-слоем, а замыкание
  областей ещё не выполнено. Растровая сетка с синтетическими достроениями не
  используется как источник истины.
- [x] Подтверждено: дорога допустима через любую область, полустанок — на любой существующей дороге, путь выбирает система по минимуму областей.
- [x] PM принял ADR-081: сервер получает только концы, строит минимальную по
  непрерывным региональным сегментам траекторию и воспроизводимо случайно
  выбирает равноценный вариант. Общий контракт, нейтральная приёмка и
  test-only подключение выполнены; нормативная игра по-прежнему ждёт
  подтверждённых контуров областей.
- [x] Определено измерение отступа полустанка: точка не может лежать ни в одной области любого конца исходной дороги.
- [x] Определён точный момент открытия обычного строительства — начало `N+2`; все дороги двусторонние.
- [x] Контрольный mock-сценарий подтверждает атомарное списание только полной распределенной стоимости.
- [x] Общий lifecycle и mock-срез проверяют точную границу: строительство хода
  `N` закрыто весь `N+1` и открывается в начале `N+2`.
- [x] В mock-сценарии есть груз, для которого новая сеть меняет маршрут или доступность.
- [x] Контрольная mock-фаза строит полустанок и дорогу до отдельного завершения;
  оба порядка строительства проходят одинаковый общий lifecycle без жёстко
  заданного идентификатора (`LEGACY-0056` закрыт).
- [x] Дорога выбирается двумя узлами на карте или в доступной форме; отдельный
  server preview показывает ломаную и цену, не передаёт оплату, не меняет
  сессию и сбрасывается при изменении выбора или версии состояния.
- [x] Игровой генератор строительства создаёт несколько дорог и полустанков до
  отдельного завершения фазы, атомарно собирает вклады динамического состава
  команд и открывает обычные объекты в `N+2`. Сейчас он использует явно
  технические области, поэтому готов как проверенный игровой черновик, но не
  как публикуемая геометрия.
- [x] Новости № 26 и № 27 исполняются вместе со строительным lifecycle:
  первая уменьшает стоимость первой дороги хода максимум на шесть сегментов,
  вторая создаёт дорогу `12 ↔ 22` с открытием в `N+1` либо безопасно ничего не
  меняет, если дорога уже существует.

## 6. Перед GSR-033

- [ ] Основной контент одного континента получен и помещён в защищённые
  intake-файлы, но ещё не принят для публикации: 10 стран и терминалы 1–23
  связаны, однако остаются полигоны, визуальное подтверждение сети и
  редакторская проверка полного содержимого.
- [x] Полностью определены конфигурации 4–12 команд: при нечётном количестве
  перевозчиков на одну больше, при чётном команды делятся поровну; правила
  названий/цветов, один человек на команду и стартовое размещение подтверждены.
  Равенство очереди локомотивов закрыто. Все допустимые чётные и нечётные
  составы исполняются.
- [ ] Переданы инструкции ведущего/участника и методические приложения.
- [x] Mock-сессия хранится в PostgreSQL и восстанавливается после перезапуска.
- [x] Версия игрового пакета закреплена за сохранённой сессией через
  `bundleHash`, канонические байты пакета и точные блокировки; `LEGACY-0055`
  закрыт.
- [x] PM разрешил архивировать предреестровые тестовые сессии без их
  перезаписи. Их точные `1.0.0` locks зарегистрированы как `blocked`, а
  одинаковая для PostgreSQL и in-memory граница разрешает ведущему прочитать
  архив и запрещает новые команды.
- [ ] Перед следующей сменой Mechanics текущий `1.0.1` сохранён как настоящий
  frozen schema/checker/executor snapshot и проверен межверсионной фикстурой;
  до этого важная новая партия должна оставаться на той же версии сервера
  (`LEGACY-0072`).
- [ ] Фикстуры начала, середины и конца полной партии готовы.
- [x] Непубликационный каркас уже проходит несколько технических ходов по
  последовательности `news → maintenance → market → cargo → movement
  → settlement → construction → reporting → news`. Строительство исполняется
  на технических областях; рынок поддерживает повторяемые покупки и продажи
  либо завершение без сделок. Полная партия этим не считается.
- [x] Две методические паузы имеют отдельные состояния и действия `start`,
  `defer`, `complete`; после завершения ведущий возвращается к отчётности.
  Постоянный пропуск и обязательная причина не входят в цифровую механику и
  остаются на усмотрение ведущего.
- [x] Пять подтверждённых автором вопросов финальной рефлексии сохранены как
  неизменяемое содержимое. Правила финала подтверждены: граница полного
  раунда, текущие цены покупки, вычет займов и допустимость нескольких
  победителей при равенстве. Сам workflow ещё не готов.
- [ ] Доступный между атомарными действиями денежный корректор ведущего
  позволяет начислять и списывать суммы по таблице команд и журналирует
  изменения; отдельной механики совещания нет.
- [ ] Ручное отцепление бесплатно по единицам действия, доступно на терминале
  завершения движения для пустого/гружёного вагона, допускает повторное
  прицепление и рассчитывает плату за пройденный участок до отклонения от
  кратчайшего маршрута.
- [ ] Изъятие загруженного вагона по новости № 19 оставляет единственную
  физическую карту на текущем терминале доступной любой команде.
- [x] Поверхность ведущего проверяется отдельно от технического replay:
  92 действия имеют 92 плана, обычный facilitator flow связывает 85
  уникальных действий, а защищённая инициализация и шесть технических команд
  не входят в обычный сценарий. Runtime убирает недоступные действия и
  оставляет формы с параметрами только в подходящем состоянии.

## 7. Полная тестовая партия GSR-035

Этот раздел относится только к отдельной игре `cards-money-trains-mock` и не
снимает содержательные блокеры нормативной версии. Пакет сохраняется для
регрессии уже реализованных механизмов; расширять его новыми полноразмерными
вымышленными колодами, картой или методикой не планируется.

- [x] Все `metric.transfer` в исходниках, генераторе и скомпилированном
  манифесте используют `from.scope`/`to.scope` и `onInsufficient: "fail"`;
  старых полей `kind`/`insufficientFunds` нет.
- [x] Генератор mock-пакета выполнен, runtime/UI-манифесты заново скомпилированы,
  браузерный плагин пересобран; проверка рассинхронизации проходит.
- [x] Runtime реализует и серверными проверками покрывает фазы `setup`, `news`,
  `maintenance`, `market`, `cargo`, `operations`, `construction`, `debrief`,
  `finished`.
- [ ] Полный ручной проход ведущего через все фазы интерфейса ещё не принят:
  браузерная проверка покрывает видимый жизненный цикл, а точный 88-шаговый
  сценарий выполняется как серверная интеграция.
- [x] Общие операции колоды доказаны нейтрально; mock-колоды имеют не менее
  шести новостей и двенадцати грузов, скрывают будущее и воспроизводятся по
  seed. Открытое предложение держит карты вне ротации, а при исчерпании
  первоначального порядка атомарно перемешивает сброс; семиходовый сценарий
  подтверждает отсутствие искусственного лимита партии.
- [x] Рынок проверяет покупку, продажу, владельца, фазу и недостаток денег.
- [x] Общие операции загрузки, сцепления и отцепления доказаны нейтрально; mock проверяет свободный
  вагон, узел отправления, совместимость, единицу действия и автоснятие при
  доставке.
- [x] Общий план расчёта по кратчайшему маршруту — расчёт и проведение платежа по
  кратчайшему маршруту — доказан нейтрально; mock показывает маршрут и состав
  тарифа до/после закрытия или строительства.
- [x] Основной контрольный сценарий позволяет выполнить несколько строительств
  до отдельного завершения; неполная оплата не оставляет частичного результата,
  оба порядка строительства проверены до автоматического открытия в `N+2`.
- [x] Строительные действия не используют зашитые идентификаторы: узлы,
  существующая дорога и положение полустанка выбираются через карту либо DOM,
  а подтверждение дороги требует актуального серверного расчёта.
- [x] Положение полустанка проверяется сервером до оплаты и связано
  доказательством с последующим разделением именно той же геометрии. Точная
  координата узла хранится один раз; read-only вложенная проекция позволяет
  использовать её в общем упорядочивании без округления и дублирующего
  `positionX`.
- [x] Общий план расчёта мест по объявленной формуле доказан
  нейтрально; mock показывает отдельного победителя/равенство для каждого типа.
- [x] Обычная игра имеет независимые паузы после ориентиров ходов 3 и 5 с
  разными вопросами и проверенными переходами `start`, `defer`, `complete`.
  Постоянный пропуск не требует реализации. Read-only панель уже показывает ведущему
  пять подтверждённых финальных вопросов, время подготовки/выступления и число
  общих выводов. Без принятого финального workflow и итогового расчёта этот
  материал не завершает сессию (`LEGACY-0059`).
- [x] Шестиходовой контрольный replay включает успешный путь и безопасные отказы;
  после шестого хода возможен следующий ход без автоматического завершения.
- [x] Запрос завершения можно отменить и подтвердить; после `finished` игровые
  мутации отклоняются.
- [x] PostgreSQL-восстановление проверено после строительства и после открытия
  карты; состояние и будущий порядок колод не меняются.
- [ ] Browser-сценарий проверяет создание сессии, старт с клавиатуры,
  перезагрузку и видимый финал, но длинную середину проводит прямыми
  API-запросами. Полный ручной путь через все фазы интерфейса не доказан.
- [x] UI и данные заметно помечены `MOCK`; gameId отличается от нормативного,
  автоматического копирования в `cards-money-trains` нет. Пользовательская
  выгрузка пока не реализована и отдельно отложена как `LEGACY-0053` и
  `LEGACY-0054`.
- [ ] После первой нормативной партии сокращены дублирующие игровые
  mock-сценарии, но сохранены нейтральные схемные, контрактные и runtime-проверки
  общих механизмов.

## 8. Интерфейс «карта — основа»

- [x] PM принял общий декларативный режим; ADR-080 имеет статус `Accepted` и
  синхронизирован с общей архитектурой.
- [x] Право использования, изменения и публикации текущей карты подтверждено;
  web-производная и происхождение зарегистрированы.
- [x] UI JSON Schema и authoring-схема принимают `layout_mode: "map-first"` и
  ограниченный `workspaceSlot`, отклоняют неизвестную зону и отсутствие или
  дублирование `board`.
- [x] Manifest TypeScript-контракты, Presenter и routing поддерживают новый
  режим без отдельной ручной формы, расходящейся со схемой.
- [x] Нейтральная фикстура доказывает общий режим без названий, ассетов и
  правил «Карт, денег, поездов».
- [x] Общий renderer заполняет рабочую область картой и размещает status,
  primary/context panels, action tray, floating controls и overlay по
  семантическим зонам, а не по игровым CSS-классам.
- [x] `interactiveBoardSurface` в map-first режиме не резервирует постоянную
  колонку; независимые DOM-действия открываются поверх карты и работают при
  ошибке Phaser.
- [x] Mock UI использует разрешённую авторскую карту только как визуальный фон,
  сохраняет крупную маркировку `MOCK` и не переносит нормативную сеть или
  правила.
- [x] Нормативный UI использует `board-guinea-optimized` и каноническую
  плоскость `5079 × 3627`; черновые узлы совмещены с ней в отдельном
  контрольном наложении.
- [x] Карта и обычные формы используют единые связи терминалов 1–23 с
  10 странами; описание страны открывается из терминала и из отдельного
  каталога. Пространственное выделение страны ждёт полигонов.
- [ ] Подтверждённые узлы и контуры областей включены в runtime-проекцию;
  `review-draft` не считается выполнением этого пункта.
- [x] Камера имеет pan/zoom, границы, «Показать всю карту» и полноэкранный
  режим; повторное монтирование не накапливает обработчики.
- [x] На `1920 × 1080`, `1440 × 900` и `1024 × 768` canvas совпадает с рабочей
  областью с допуском 2 CSS-пикселя, а документ не имеет обычной вертикальной
  прокрутки.
- [x] На узком контрольном размере панели превращаются в нижние листы;
  критические действия остаются доступны с клавиатуры.
- [ ] Визуальная приёмка по
  `map-first-interface-specification.md` выполнена; `LEGACY-0062` закрыт только
  после фактического доказательства.

## 9. Перед заявлением о завершении

- [ ] GSR-031–033 имеют фактические доказательства приёмки; GSR-030 помечен `Superseded` и не является gate.
- [x] Полный сокращенный replay — повторное воспроизведение партии по транскрипту — проходит детерминированно.
- [ ] Browser-проверки покрывают мышь и действия без drag/drop.
- [x] Авторская карта является основой экрана, панели лежат поверх неё, а
  текущий малый встроенный canvas полностью выведен из целевого пути.
- [x] Повторное монтирование Phaser не оставляет обработчики или ресурсы.
- [x] Перезапуск runtime не меняет состояние и будущий порядок колод.
- [ ] Финальный расчёт объясним и определяет победителей или равенство внутри
  каждого типа: деньги плюс техника по текущим ценам покупки минус
  непогашенные займы. Проценты при необходимости заранее списывает ведущий
  через ручной денежный корректор.
- [ ] Нет второго континента, штрафа за отчетность и скрытой персональной аналитики.
- [ ] Все временные ограничения зарегистрированы или сняты.
- [ ] `PROJECT_ARCHITECTURE.md`, GSR, TSK, матрица требований и структура репозитория синхронизированы.
