# TSK-20260710-editor-author-version-history: История версий автора в редакторе

## Оглавление

- [Status](#status)
- [Parent](#parent)
- [Understanding](#understanding)
- [Why](#why)
- [Architecture Source](#architecture-source)
- [Architecture Gap](#architecture-gap)
- [Product Scenario](#product-scenario)
- [Current Baseline](#current-baseline)
- [Scope Classification](#scope-classification)
- [Scope](#scope)
- [Non-Goals](#non-goals)
- [Plan Approval](#plan-approval)
- [Plan](#plan)
- [Execution](#execution)
- [Acceptance](#acceptance)
- [Validation](#validation)
- [Artifacts](#artifacts)
- [Risks and Controls](#risks-and-controls)
- [Plan Amendments](#plan-amendments)
- [Handoff Log](#handoff-log)

## Status

completed

Status note: долговечная история, пользовательская панель, безопасный возврат и
межпроцессная сериализация ADR-077 реализованы и приняты. Корневая проверка:
editor-web 283/283, editor-engine 191/191, typecheck, обе production-сборки и
единый Playwright-прогон 5/5; задача завершена 2026-07-12.

## Parent

none

Задача выделена из прежнего общего хвоста
`TSK-20260707-editor-concept-surfaces`, но является самостоятельной корневой
работой с отдельным пользовательским результатом и приёмкой.

## Understanding

Работа должна дать автору игры понятную историю точек «Сохранить»: когда и кем
сохранена версия, что в ней изменилось и как безопасно вернуть прежнее состояние.
В интерфейсе не используется терминология Git. Возврат создаёт новую сохранённую
версию с содержимым выбранной точки и не переписывает существующую историю.

Это реализация уже принятой архитектуры редактора, а не новое архитектурное
решение. Git остаётся источником истории авторского проекта; рабочая копия
сессии остаётся источником несохранённых изменений; отдельная база версий или
второй источник истины не вводятся.

## Why

Сейчас редактор умеет создавать изолированную рабочую сессию и сохранять её
коммитом, но автор видит только факт последнего сохранения. Ошибка автора или
неудачное изменение требуют знания внутренних Git-команд. Новый срез должен
сделать восстановление штатным и безопасным продуктовым действием.

Пользовательский результат:

- автор понимает, сохранена ли текущая работа;
- после повторного открытия не теряет несохранённые правки;
- видит ограниченный список сохранённых версий с понятными резюме;
- может изучить состав версии и вернуть её без удаления более новых версий.

## Architecture Source

- `docs/architecture/editor-preview-first-ux.md` §9.7 — принятый UX-концепт
  истории версий автора.
- `docs/architecture/adrs/036-semantic-authoring-and-preview-timeline-editor.md`
  — Save как коммит и восстановление без `reset`.
- `docs/architecture/adrs/042-editor-session-versioning-and-lifecycle.md` —
  рабочая копия сессии, совместимость и жизненный цикл редакторных сессий.
- `docs/architecture/adrs/057-preview-first-editor-ux-architecture.md` — единая
  модель состояния редактора и пользовательские поверхности.
- `docs/architecture/adrs/065-editor-as-product-hosted-authoring-studio.md` — Git
  как история проекта автора и будущая продуктовая граница студии.
- Актуальная документация Next.js App Router: история читается динамическим
  серверным Route Handler в Node.js runtime; серверный Git-код не попадает в
  клиентскую сборку; входные данные и безопасные ответы об ошибках проверяются
  на HTTP-границе.
- `docs/architecture/adrs/075-durable-author-version-history.md` — принятое
  решение обнаруженного разрыва между временной веткой сессии и долговечной
  историей проекта автора.
- `docs/architecture/adrs/077-serialized-editor-session-mutations.md` — принятое
  решение о единой межпроцессной аренде Save/Restore/Close/GC, обязательной
  пользовательской сессии и безопасной точке фиксации Save.

## Architecture Gap

Текущий план предполагал читать версии только из ветки активной сессии. Однако
ADR-042 разрешает очищать чистые сессии, а новая сессия создаётся от текущей
линии проекта и не обязана видеть старую session branch. Поэтому обещание
«истории проекта после повторного открытия» сейчас не обеспечивается.

Пробел закрыт решением PM 2026-07-11: ADR-075 принят, каждая точка «Сохранить»
продвигается в долговечную линию проекта, доступную новым сессиям. Ограниченная
session-only альтернатива отклонена. Реализация обязана доказать это сценарием
close/GC/new session, а не только чтением ветки текущей сессии.

Критическое ревью реализации выявило второй пробел: CAS долговечной вершины не
сериализует изменения одной рабочей папки, GC доверяет устаревшему признаку
чистоты, а старый Save без `sessionId` обходит авторскую историю. PM принял
ADR-077 2026-07-12. Архитектурное решение больше не блокирует работу, но пробел
остаётся активным до реализации контракта и корневой сквозной проверки.

## Product Scenario

Контрольный сценарий выполняется на игре `games/simple-choice`:

1. Автор открывает редактор и существующую сессию игры.
2. Вносит ручное изменение и применяет одно изменение помощника.
3. Нажимает «Сохранить», при желании уточняет предложенное резюме.
4. После повторного открытия редактора видит созданную точку в «Истории».
5. Вносит и сохраняет второе изменение.
6. Открывает сведения о первой версии и выбирает «Вернуть эту версию».
7. Редактор создаёт новую точку восстановления, перезагружает документы и
   предпросмотр; в истории остаются обе исходные версии и новая версия возврата.

Общая возможность дополнительно доказывается изолированной временной Git-фикстурой
без имён и правил `simple-choice`.

## Current Baseline

Уже готово и должно быть переиспользовано:

- `createProjectGitSession` и `saveProjectGitSession` создают изолированную
  рабочую копию и точки Save;
- `restoreSavedVersion` восстанавливает только разрешённые пути и затем создаёт
  новый коммит;
- `EditorSessionDocument` хранит `lastSavedCommit` и вычисляемый `dirtySummary`;
- API файлов уже сохраняет активный документ в рабочую копию сессии;
- журнал редактора и `EditorDiffSummaryItem` дают понятные факты об изменениях;
- операции редакторного ядра и текущая Git-база покрыты модульными тестами.

Первый срез истории уже реализовал постраничное чтение, клиентские структуры,
комментарии и резюме, панель «История», возврат с перезагрузкой состояния и
плашку восстановления грязной сессии.

Не готово:

- межпроцессная аренда, общая для Save, Restore, Close и разрушительного GC;
- отказ пользовательского `PUT /api/editor/file` без действующей сессии;
- повторная проверка фактического Git status и безопасного worktree root под
  арендой перед удалением;
- корневая сквозная приёмка полного пользовательского сценария после этих
  изменений.

## Scope Classification

- **Общая возможность платформы:** история версий любого авторского проекта,
  нормализация фактов изменений, безопасное восстановление и восстановление
  несохранённой сессии.
- **Игровое содержимое:** отсутствует. `simple-choice` используется только как
  реальный доказательный сценарий.
- **Запрещённая утечка:** в `editor-engine`, `editor-web` и API не добавляются
  проверки идентификатора конкретной игры или правила конкретного манифеста.

## Scope

1. **Контракты истории.** Ввести клиентские структуры `EditorVersionSummary`,
   `EditorVersionDetails`, результат страницы истории и запрос восстановления.
   Идентификатор версии считается непрозрачным; UI не показывает ветки, SHA,
   коммиты, checkout, reset или revert.
2. **Чтение Git-истории.** После решения ADR-075 добавить серверные функции,
   которые:
   - в рекомендованном варианте читают долговечную линию текущего проекта и
     используют session branch только для несохранённого черновика;
   - в ограниченном варианте читают только ветку текущей сессии и явно
     возвращают session-only lifetime в клиентском контракте;
   - ограничивают записи разрешёнными путями текущей игры;
   - возвращают не более 50 записей на страницу и непрозрачный курсор;
   - отдают автора, время, пользовательский комментарий, структурированное
     резюме и перечень изменённых файлов;
   - не читают произвольные refs из клиентского ввода.
3. **Резюме сохранения.** Перед Save собрать факты текущего журнала и файлового
   diff. Клиент может предложить автору короткий комментарий. Сервер валидирует
   длину и сохраняет комментарий вместе с машинно-читаемыми фактами в метаданных
   коммита. Если журнал неполон или отсутствует, детерминированный fallback
   строится из diff разрешённых файлов; история никогда не остаётся без резюме.
4. **Серверный API.** Добавить динамический Node.js Route Handler:
   - `GET` — страница истории и состояние восстановления сессии;
   - `POST` — восстановление выбранной версии;
   - единая проверка `sessionId`, принадлежности игры, статуса/совместимости
     сессии, курсора, идентификатора версии и ожидаемой текущей версии;
   - статусы `400/404/409` для ошибочного ввода, отсутствующей версии и конфликта;
     внутренние подробности Git не возвращаются клиенту.
5. **Безопасность возврата.** Возврат разрешён только когда рабочая копия чиста.
   При несохранённых изменениях UI предлагает сначала сохранить их; тихое
   затирание запрещено. Перед применением сервер повторно проверяет текущую
   вершину истории по `expectedHead`, достижимость выбранной версии из ветки
   сессии и политику разрешённых путей.
6. **Поверхность «История».** Добавить открываемую панель со списком версий,
   относительным и точным временем, автором, резюме и постраничной загрузкой.
   Детали версии показывают комментарий и понятный список изменений. Возврат
   требует явного подтверждения с объяснением, что будет создана новая версия.
7. **Восстановление грязной сессии.** При повторном открытии существующей сессии
   использовать серверный `dirtySummary` и показывать плашку «Есть
   несохранённые изменения из прошлой сессии» с числом/списком изменённых файлов.
   Продолжение работы использует существующую рабочую копию; автоматический
   сброс или автоматический коммит запрещены.
8. **Синхронизация редактора после возврата.** После успешного возврата заново
   загрузить список файлов, активный документ, sibling-проекцию, проверки и
   предпросмотр. Локальные undo/redo и очередь ещё не применённых намерений
   очищаются как относящиеся к прежней вершине истории; очистка объясняется в UI.
9. **Наблюдаемость и ошибки.** Все ожидаемые отказы получают стабильные коды и
   понятные русские сообщения; необработанные команды, пути и stderr Git в UI не
   попадают.
10. **Документация.** После реализации обновить текущую архитектурную сводку,
    пользовательский UX-документ только при выявленном расхождении, `NEXT_STEPS.md`
    и Handoff Log. Новый ADR не нужен, пока сохраняются зафиксированные здесь
    источник истины и границы.

## Non-Goals

- просмотр полностью работающей исторической игры в отдельном runtime;
- создание копии игры от версии;
- сравнение произвольных двух версий;
- синхронное совместное редактирование и разрешение конфликтов нескольких
  авторов;
- слияние ветки сессии, публикация игры или откат опубликованной версии;
- durable-база метаданных облачной студии, роли и мультиарендность;
- восстановление платформенного кода, сгенерированных файлов или путей вне
  политики текущего проекта;
- новый формат хранения авторского содержимого или второй источник истины.

## Plan Approval

`approved_2026-07-11`

PM принял ADR-075: Save продвигается в долговечную проектную линию, доступную
между сессиями; session-only альтернатива отклонена. Остальная внутренняя
декомпозиция плана повторного согласования не требует.

PM принял ADR-077 2026-07-12: пользовательский Save требует действующую сессию,
а Save/Restore/Close/GC сериализуются общей межпроцессной арендой. Это снимает
архитектурный блокер, но не заменяет реализацию и финальную приёмку.

## Plan

### Этап 0. Зафиксировать базовую линию

1. Запустить тесты `editor-engine` и Git/session-модулей `editor-web`.
2. Уточнить текущие форматы сессии, Save-запроса, журнала и `dirtySummary`.
3. Создать нейтральную временную Git-фикстуру для истории, постраничного чтения,
   конфликта и возврата. Все временные репозитории тестов размещать под `.tmp/`
   либо в системном каталоге тестового раннера с обязательной очисткой.

### Этап 1. Чистая модель истории

1. Добавить framework-agnostic структуры фактов изменения и нормализованного
   резюме в `packages/editor-engine` только если они нужны более чем одному
   интерфейсному пути; Git-типы и Node.js зависимости туда не помещать.
2. Реализовать детерминированную агрегацию ручных/агентских фактов с fallback по
   изменённым путям.
3. Покрыть порядок, множественные документы, неизвестные факты и локализационные
   данные модульными тестами.

### Этап 2. Серверная история и Save-метаданные

1. Расширить Save входными `authorComment` и нормализованными change facts с
   ограничениями длины и количества.
2. Добавить чтение страниц истории и сведений о версии в Git-слой.
3. Проверить достижимость версии, разрешённые пути и отсутствие утечки
   произвольных refs.
4. Добавить API `GET/POST`, Node.js runtime, динамическое выполнение и безопасное
   отображение ошибок.
5. Покрыть серверный слой тестами реального временного Git-репозитория и тестами
   HTTP-границы.

### Этап 3. Безопасный возврат

1. Добавить optimistic concurrency: клиент передаёт ожидаемую текущую версию,
   сервер возвращает `409`, если история успела измениться.
2. Запретить возврат грязной/несовместимой/закрытой сессии.
3. Выполнить `restoreSavedVersion` только для разрешённых путей и создать новый
   Save с явным типом `restore` и ссылкой на выбранную пользовательскую версию.
4. После успеха обновить session metadata и вернуть новую текущую версию.
5. Доказать тестом, что исходная и более новая версии остаются достижимыми.

### Этап 4. Пользовательская поверхность

1. Добавить API-клиент и состояние панели истории без импорта серверного Git-кода.
2. Реализовать список, детали, загрузку следующей страницы, состояние загрузки,
   пустое состояние и ожидаемые ошибки.
3. Добавить предложение резюме/комментария в Save-поток без дополнительного
   обязательного шага при обычном сохранении.
4. Реализовать подтверждение возврата и блокировку при несохранённых изменениях.
5. После возврата атомарно обновить данные редактора и очистить устаревшее
   клиентское временное состояние.
6. Добавить плашку восстановления грязной повторно открытой сессии и сведения
   «Сохранено N минут назад · M несохранённых файлов» в статусную строку.

### Этап 4.5. Сериализация изменений сессии по ADR-077

1. Требовать действующую editor-сессию для пользовательского Save; прежний
   прямой HTTP Save без `sessionId` отклонять стабильной ошибкой.
2. Выполнять Save, Restore, Close, разрушительный GC и изменение session
   metadata под одной межпроцессной арендой конкретной сессии.
3. После получения аренды заново читать Git status; не удалять грязную сессию
   без явного `removeDirty`.
4. Перед рекурсивным удалением проверять через `realpath`, что путь находится в
   разрешённом `.tmp/editor-worktrees` и зарегистрирован как editor worktree.
5. Зафиксировать точку успешного Save на атомарном продвижении долговечной
   версии и восстанавливать вспомогательные metadata из неё при следующем чтении.
6. Покрыть конкурирующие Save/Restore/Close/GC, устаревший `dirtySummary`,
   безопасную границу удаления и отказ Save без сессии тестами.

### Этап 5. Сквозная приёмка и закрытие

1. Пройти контрольный сценарий `simple-choice` и нейтральную Git-фикстуру.
2. Проверить русские тексты, клавиатурную доступность панели и подтверждения,
   узкий viewport и отсутствие Git-терминов.
3. Запустить целевые модульные, интеграционные, e2e и сборочные проверки.
4. Обновить документы и Handoff Log; перенести задачу в архив только после
   выполнения всей приёмки.

## Execution

- [x] Этап 0 — базовая линия и фикстура.
- [x] Этап 1 — модель фактов и резюме.
- [x] Этап 2 — серверная история и Save-метаданные.
- [x] Этап 3 — безопасный возврат.
- [x] Этап 4 — пользовательская поверхность и восстановление сессии.
- [x] Этап 4.5 — сериализация изменяющих операций по принятому ADR-077.
- [x] Этап 5 — сквозная приёмка и документация.

Исполнитель может менять внутреннюю декомпозицию в этих границах. Одновременно
в статусе `in_progress` должен находиться только один этап, если работа не была
явно распараллелена на независимые результаты.

## Acceptance

1. История показывает только точки Save текущего проекта игры, отсортированные
   от новой к старой; первая страница ограничена, следующая загружается курсором.
2. Каждая строка содержит понятное резюме, автора, относительное время и точное
   время в доступном описании; Git-терминология пользователю не показывается.
3. Пользовательский комментарий необязателен, ограничен по длине и сохраняется;
   при его отсутствии доступно детерминированное автоматическое резюме.
4. Сведения о версии перечисляют только разрешённые авторские файлы и не
   раскрывают абсолютные пути, команды или внутренние ошибки Git.
5. Повторное открытие грязной сессии показывает плашку восстановления и
   продолжает работу с существующими несохранёнными файлами без сброса.
6. Возврат при несохранённых изменениях заблокирован с понятным предложением
   сначала сохранить работу.
7. Возврат чистой сессии создаёт новую точку Save; ни выбранная, ни более новые
   предыдущие версии не исчезают из истории.
8. Неактуальный `expectedHead`, чужая/недостижимая версия, несовместимая или
   закрытая сессия не изменяют файлы и возвращают ожидаемый `4xx` ответ.
9. После возврата открытый документ, проектная проекция, диагностики и
   предпросмотр соответствуют новой вершине; устаревшие undo/redo и очередь
   намерений не применяются к возвращённому содержимому.
10. Функциональность не содержит условий для `simple-choice` или другой игры и
    доказана нейтральной фикстурой.
11. Серверный Git-код отсутствует в клиентской сборке; Route Handler выполняется
    в Node.js runtime и не кэширует историю между запросами.
12. Целевые тесты, typecheck, production build и редакторный e2e зелёные.
13. Закрытие/GC чистой сессии и открытие новой сессии не скрывают ранее
    сохранённые версии; новая сессия при этом использует актуальный код
    платформы и восстанавливает только разрешённое игровое содержимое.
14. Пользовательский Save без действующей сессии отклоняется и не изменяет
    основной checkout или долговечную историю.
15. Save, Restore, Close и разрушительный GC одной сессии не выполняются
    одновременно; конкурентная операция получает ограниченное ожидание или
    стабильный конфликт без частично применённого состояния.
16. GC под арендой заново проверяет Git status и `realpath`; грязная сессия без
    `removeDirty` и путь вне разрешённого editor-worktree root не удаляются.

## Validation

Минимальные автоматические проверки:

```bash
npm test --workspace @cubica/editor-engine
npm test --workspace @cubica/editor-web -- --no-file-parallelism
npm run typecheck --workspace @cubica/editor-web
npm run build --workspace @cubica/editor-web
npm run test:e2e:prod -- apps/editor-web/e2e/editor-session-preview.spec.ts
```

Если для истории будет создан отдельный e2e-файл, запускать его вместе с
`editor-session-preview.spec.ts` и записать точную команду в Handoff Log.

Обязательные ручные проверки:

1. Два сохранения и один возврат на `simple-choice`; убедиться, что история имеет
   три соответствующие точки и возврат не удалил второе сохранение.
2. Повторно открыть грязную сессию и убедиться, что показана плашка, а содержимое
   рабочей копии сохранено.
3. Попытаться вернуть версию при грязной сессии и при устаревшем `expectedHead`;
   убедиться, что файлы не изменились.
4. Проверить панель с клавиатуры и на узкой ширине; в пользовательских строках
   не должно быть слов `Git`, `commit`, `SHA`, `branch`, `checkout`, `reset`,
   `revert`.
5. Закрыть сохранённую сессию, выполнить безопасный GC, открыть новую сессию
   проекта и проверить поведение, выбранное в ADR-075.
6. Параллельно запустить Save/Restore и Close/GC одной сессии; убедиться, что
   операции сериализованы, а пользовательский Save без сессии отклонён.

## Artifacts

На старте постоянные отдельные артефакты не требуются. Нормативные результаты —
код, тесты и этот TSK. Если во время реализации понадобится зафиксировать
расширенную матрицу сценариев или измерения производительности истории, хранить
их в `docs/tasks/artifacts/TSK-20260710-editor-author-version-history/` и добавить
описание в этот раздел.

## Risks and Controls

| Риск | Контроль |
|---|---|
| Возврат затирает несохранённую работу | Серверный dirty-check непосредственно перед изменением; UI-блокировка не считается достаточной |
| Клиент передаёт произвольный Git ref | Непрозрачный version id разрешается только среди достижимых версий ветки текущей сессии |
| История растёт без границ | Страница не более 50 записей, непрозрачный курсор, отсутствие полного `git log` в одном ответе |
| Журнал не описывает ручные изменения | Детерминированный fallback из diff разрешённых путей; Git остаётся источником факта изменения |
| Два возврата или Save выполняются одновременно | Межпроцессная аренда сериализует всю изменяющую операцию; `expectedHead` сохраняет контроль вершины истории |
| GC удаляет рабочую папку по устаревшим metadata | Под арендой повторно читать Git status и проверять разрешённый root через `realpath` |
| После возврата применяется старый undo | Полная перезагрузка редакторного состояния и явная очистка локального журнала/очереди |
| В клиентскую сборку попадает Node/Git код | Разделение Route Handler/server library и клиентских DTO; production build как обязательная проверка |
| Реализация создаёт второй источник истины | Метаданные являются производными подсказками; содержимое и порядок версий определяет только Git |

## Plan Amendments

### 2026-07-11 — архитектурный аудит

- Обнаружено: ветка текущей сессии не является долговечной историей проекта и
  может стать недоступной после lifecycle cleanup.
- Решение на момент аудита: создан Proposed ADR-075; этапы 1–5 были
  заблокированы до решения PM. Ниже зафиксировано последующее принятие решения.
- Независимая работа: базовые unit/typecheck проверки и документационная
  синхронизация продолжаются.

## Handoff Log

### 2026-07-10 — Codex

- Changed: создан
  `docs/tasks/active/TSK-20260710-editor-author-version-history.md`; история
  выделена из общего списка концептуальных поверхностей редактора.
- Evidence: подтверждено наличие `restoreSavedVersion`, `lastSavedCommit`,
  `dirtySummary`, текущих Save API и клиентского журнала изменений; базовые тесты
  `@cubica/editor-engine` прошли — 12 файлов, 191 тест.
- Context7: учтены актуальные правила Next.js App Router — динамический Route
  Handler, Node.js runtime, явные методы, проверка входа и безопасные ошибки.
- Remaining: реализация этапов 0–5 не начиналась.
- Next safe step: перевести задачу в `in_progress`, выполнить Этап 0 и записать
  точные результаты базовых проверок без изменения архитектурных границ.
- Known risks: полнота авто-резюме для ручных изменений и конкурентный Save;
  оба риска имеют обязательные fallback/`expectedHead` проверки в плане.

### 2026-07-11 — аудит перед реализацией

- Changed: статус переведён в `blocked`, добавлена
  ссылка на Proposed ADR-075 и безопасная команда production E2E для
  маломощного общего хоста.
- Evidence: `@cubica/editor-engine` — 191/191 тестов; `editor-web` typecheck —
  успешно; `editor-web` unit baseline — 234 теста прошли, один существующий
  plugin-validation тест упал из-за параллельного изменения PlayerPluginApi.
- Remaining: решение PM по ADR-075, затем этапы 1–5 и новый сценарий «закрыть
  сессию/выполнить GC/открыть новую сессию/увидеть те же версии».

### 2026-07-11 — решение PM

- Decision: ADR-075 принят; долговечная проектная линия Save обязательна,
  session-only вариант отклонён.
- Changed: статус задачи переведён в `in_progress`; backend/model и UI
  реализуются параллельно в пределах принятого контракта.

### 2026-07-11 — критическое ревью конкурентной безопасности

- Evidence: editor-web 276/276, editor-engine 191/191, typecheck и production
  build зелёные; долговечная Git-фикстура доказывает close/GC/new session.
- Finding: CAS защищает вершину истории, но полный цикл Save/Restore/Close/GC
  одной рабочей папки не сериализован; старый PUT без сессии обходит историю.
- Changed: создан Proposed ADR-077 и `LEGACY-0051`; задача переведена в
  `blocked` до решения PM. Реализация новой границы не начата.

### 2026-07-12 — решение PM по конкурентной безопасности

- Decision: ADR-077 принят целиком; пользовательский Save требует сессию, а
  Save/Restore/Close/GC сериализуются общей межпроцессной арендой.
- Changed: задача возвращена в `in_progress`; добавлен этап 4.5 и критерии
  приёмки конкурентных операций, свежего Git status и безопасного удаления.
- Remaining: реализовать ADR-077, затем выполнить корневую сквозную проверку
  истории и только после неё рассматривать перенос TSK в архив.
- Evidence: на этом шаге синхронизирована только документация; новый root/E2E
  прогон не выполнялся и завершение задачи не заявляется.

### 2026-07-12 — реализация ADR-077 и итоговая приёмка

- Changed: Save/Restore/Close/GC одной сессии сериализованы файловой
  межпроцессной арендой с owner token/PID, ограниченным ожиданием и стабильным
  `session_busy`; Save без `sessionId` отклоняется.
- Safety: GC перечитывает Git status под арендой, повторно проверяет metadata
  при orphan cleanup и удаляет только зарегистрированный worktree внутри
  `.tmp/editor-worktrees`; stderr и абсолютные пути не возвращаются клиенту.
- Commit boundary: сводка версии строится до CAS, а отказ записи session
  metadata после успешного продвижения не превращает сохранённую версию в
  повторяемую ошибку; чистая сессия сверяется с долговечной вершиной.
- Evidence: целевые ADR-077 tests — 25/25; полный editor-web — 59 файлов,
  283/283; editor-engine — 12 файлов, 191/191; editor-web typecheck и
  `git diff --check` — успешно.
- Production evidence: последовательные production builds player-web/editor-web
  успешны; итоговый Playwright-run — 5/5, включая два Save, History, Restore,
  перезагрузку `_label`, Web orientation и Telegram structural viewer.
- Result: `LEGACY-0044` и `LEGACY-0051` закрыты; задача перенесена в архив.
