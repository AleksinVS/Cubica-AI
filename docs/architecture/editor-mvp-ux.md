# MVP игрового редактора: предпросмотр, чат и рисование

Принято PM 2026-09-19, уточнено после проверки и принятия ADR-108 2026-09-21. Уточняет ADR-049/052/057 и заменяет прежнюю компоновку
редактора в границах MVP. Эталон композиции — [набросок автора](../../draft/макет%20редактора.png).
Красные надписи — комментарии; фактура бумаги и рисованные линии не являются
требованием к визуальному оформлению продукта. Более поздние текстовые замечания
PM имеют приоритет над наброском.

## Оглавление

- [1. Цель и границы](#1-цель-и-границы)
- [2. Поверхности и меню](#2-поверхности-и-меню)
- [3. Чаты и правила](#3-чаты-и-правила)
- [4. Выбор и правка элемента](#4-выбор-и-правка-элемента)
- [5. Область задания и рисование](#5-область-задания-и-рисование)
- [6. Единый предпросмотр](#6-единый-предпросмотр)
- [7. Прохождение и сценарий](#7-прохождение-и-сценарий)
- [8. Отзывчивость и доступность](#8-отзывчивость-и-доступность)
- [9. Архитектурные зависимости](#9-архитектурные-зависимости)

## 1. Цель и границы

Лаконичность, отзывчивость и понятность важнее количества инструментов. Основные
поверхности — предпросмотр игры и ИИ-чат. Дерево манифеста, диаграмма исходников
и JSON-редактор не входят в пользовательский интерфейс MVP. Их внутренние
проекции и проверки могут переиспользоваться там, где нужны поведению; скрытый
инструмент не должен загружать тяжёлую библиотеку только ради старой оболочки.

Авторские JSON-манифесты остаются источниками данных. YAML — временное
человекочитаемое представление одного элемента. Модель изменения и компиляция
единые для всех способов ввода. Сокращение интерфейса не отменяет валидацию,
отмену действий, историю сохранений и различие сохранения с публикацией.

## 2. Поверхности и меню

У верхней границы любой поверхности без отступа находится **панель инструментов**:
**Чат, Редактор, Рисование, Игра, Сценарий, Добавить**. Подписи доступны как подсказки.
При сворачивании остаются только текущая иконка и её полупрозрачный фон; положение
иконки не меняется. Остальные кнопки и фон панели исчезают. Приближение курсора
к верху по всей ширине развёрнутой панели плавно раскрывает её; удаление сворачивает.
Панель перемещается влево/вправо за любую область её поверхности и закрепляется
раскрытой. Положение ограничено шириной окна. Фокус клавиатуры и открытое подменю удерживают раскрытие.
Для сенсорного ввода раскрытие доступно явным нажатием.

Правила — первая компактная строка подменю Сценарий с небольшой иконкой. Чат и Правила выбирают чатовую поверхность. Редактор, Рисование и Игра работают
на одной поверхности предпросмотра с разным поведением ввода. Сценарий —
выпадающее меню этой поверхности, а не самостоятельный экран. После Правил в нём
сохранённые состояния прохождения, затем все объявленные этапы игры, если у
игры есть предопределённый сценарий. Отдельно доступны страницы и авторское
содержимое, включая карточки и информационные окна: они не обязаны быть
самостоятельными этапами хронологии. Игра без сценария не получает выдуманные этапы.

Предпросмотр занимает всю страницу, управление располагается поверх него.
Слева сверху кнопка-гамбургер открывает выбор игры и команды версии проекта.
«Добавить» создаёт правило, страницу или элемент на текущей странице; там же
находятся доступные локальные прототипы. Источник шаблонов — авторские определения
игры, отдельного браузерного хранилища шаблонов нет.

## 3. Чаты и правила

Общий чат имеет список диалогов слева, переписку в центре и документы,
изображения и другие материалы справа. Чат Правила ведёт отдельный диалог
именно о правилах текущей игры. Подготовленное правило сразу появляется справа
и редактируется там либо через продолжение диалога.

Правый документ и диалог должны ссылаться на одну версию правила; параллельное
изменение не может молча затереть правку автора. Видимый текст правила не
становится исполнимым правилом сам по себе: структурные изменения игры проходят
общий конвейер. Связь редактора с принятой системой продуктовых знаний и
хранение диалогов должны использовать согласованные границы, а не создавать
конкурирующую базу знаний или временный псевдопостоянный браузерный архив.

## 4. Выбор и правка элемента

Щелчок выбирает верхний элемент под курсором. Повторные щелчки в той же точке
последовательно выбирают элементы нижних слоёв. При щелчке на одну секунду
появляется список всех найденных элементов по другую сторону курсора от окна
промта. Наведение или клавиатурный выбор удерживает список. Заголовок плавающего окна открывает тот же
список. Положение исчезающего списка учитывает фактические размеры и сторону
окна промта, включая его перетаскивание и растягивание. У краёв используется
вертикальная пара; при нехватке места список сокращается с прокруткой. На
крайне малом экране приоритет — видимость и отсутствие перекрытия, поскольку
строгое размещение по разные стороны точки у самого угла физически невозможно.
Пункты «Игра» и «Страница» выбирают область действия промтов.
Если рамка уже есть, внешний щелчок только снимает выделение. Следующий щелчок
может выбрать другой объект.

Выбранный элемент получает пунктирную рамку с точками в углах и серединах сторон
и тонкими двунаправленными стрелками снаружи углов. Фоновых кнопок на рамке нет.
Светлая линия с тёмным контуром сохраняет видимость на светлом и тёмном фоне.
Перемещение выполняется за внутреннюю область; поворот — за крупную невидимую
область снаружи угла. Точки изменяют размер, сохраняя противоположную сторону.
Прямой жест меняет элемент; он не должен вызывать ИИ. Предварительный вид
обновляется локально, а завершение жеста проходит проверяемую фиксацию. Для
обычных DOM-элементов MVP сохраняет явные размеры и каноническое перемещение/поворот
в существующем `style` авторского UI. Эти значения имеют приоритет над оформлением
игры; удаление геометрии возвращает базовую раскладку. Другие единицы измерения,
неизвестные преобразования и отдельный игровой canvas не преобразуются жестом:
панель объясняет ограничение, остаётся правка через намерение.

Непосредственно рядом с курсором, по возможности вне рамки, появляется компактное
плавающее окно. Заголовок не редактируется; название меняется в структурированной
части. Три промта редактируются **в одном многострочном поле**, с общим курсором,
выделением, копированием и отменой:

1. **Произвольный промт** — разовое задание агенту. Он не становится постоянным
   описанием элемента и не отправляется повторно после успешного применения.
2. **Авторское описание** — сохраняемое намерение автора, которое нельзя
   восстановить из структурных полей. Повторный выбор показывает сохранённый
   текст. Изменение выполняется тем же проверяемым конвейером.
3. **Структурированный YAML** — актуальная проекция значимых свойств. Свободная
   правка трактуется как намерение; простой однозначный случай может пройти
   без ИИ, остальное обрабатывает агент по ADR-057.

Состав YAML задаётся прототипом по [ADR-108](adrs/108-prototype-defined-semantic-yaml-editing.md).
Прототип определяет показываемые свойства, их понятные названия и представление.
Свойства организованы в YAML-секции с отступами: явная группа из прототипа
имеет приоритет, иначе используется смысловая сторона свойства («Элемент»,
«Содержание», «Показатели» и т.д.). Единственное поле не требует обёртки.
Одна секция выводится один раз; одинаковые подписи различаются видимыми
суффиксами, а точный владелец каждого поля сохраняется в скрытой карте записи.
Для заголовка показывается его фактический авторский текст, для вычисления —
правило обычным языком. Внутренние привязки, id и source map скрыты; «Источник»
не добавляется автоматически. Агент получает точные скрытые связи с авторскими
данными. Пустые разделы имеют подсказки назначения, не являющиеся частью текста.

Обычная правка относится к экземпляру и создаёт локальное переопределение
наследуемого свойства. Команда сброса удаляет переопределение. Режим
«Редактировать прототип» включается явно из контекста элемента/каталога в том же
предпросмотре: видны название прототипа и возврат, окружение закрыто для правок,
несохранённый промт экземпляра сохраняется. В нём редактируются значения по
умолчанию и состав проекции; перед сохранением понятны затронутые наследники.
Образец показывает прототип, не подменяя его значениями открывшего экземпляра.

Правка общего игрового свойства, например правила метрики, остаётся правкой
его игрового владельца. При такой правке объясняется область воздействия;
локальная формула в индикаторе не создаётся молча. Правило на естественном
языке разбирается агентом в проверяемое изменение текущего авторского выражения
или игровой механики. После применения YAML строится заново; вторая исполнимая
копия правила в свободном тексте не хранится.

Между частями находятся две отдельные строки `=======================`.
По решению PM защита от удаления отложена на следующую итерацию: разделители
сейчас являются обычным текстом. При сохранении число строк-разделителей должно
быть ровно два. Если их меньше или больше, показывается фактическое количество
и предложение исправить границы; сохранение не выполняется, черновик остаётся
целиком. Знаки равенства внутри строки не являются разделителем. Автоматического
объединения неоднозначных частей нет.

Видимые кнопки — закрытие сверху справа и сохранение снизу справа, только иконки.
Удержание сохранения открывает «Сохранить как шаблон». Прототип содержит
переиспользуемое содержимое выбранного UI-элемента; новые экземпляры получают
собственные идентификаторы. Неподтверждённая ИИ-правка сначала проходит обычное
подтверждение, чтобы шаблон соответствовал принятому элементу.

Произвольное намерение отправляется в тот же чат вместе с ограниченным контекстом
выбранного источника. Инструмент `editor.prepareCandidate` принимает только
JSON-правки этого источника и передаёт их существующему серверному конвейеру
подготовки. Агент не получает право применения: пользователь видит работающий
вариант и подтверждает его отдельно. Смена игры или исходного текста делает
прежний контекст непригодным для подготовки. В текущем MVP свободное
JSON-предложение через `editor.prepareCandidate`
требует привязанного запроса из панели элемента или YAML. Общий чат сохраняет
путь планирования точных текстовых замен через `editor.planChangeSet` с тем же
предпросмотром и подтверждением. Отдельная модель агентских ходов не вводится.

Название означает отображаемую подпись, а не технический идентификатор.
Окно растёт под текст в ограниченных пределах и растягивается за нижний правый угол.
У края окна она размещается с доступной стороны и не следует за курсором после
открытия. Перемещение доступно за поверхность окна. Обычное протягивание по тексту
выделяет текст; удержание перед протягиванием либо Alt+протягивание перемещает окно
из области текста. Это пересматриваемая деталь различения двух жестов.
Все три части сохраняют черновой ввод при работе с панелью.
Смена режима скрывает окно без потери текста. Если пересборка сбрасывает
выделение, повторный выбор того же авторского элемента восстанавливает черновик
из памяти вкладки. Явное закрытие окна удаляет его; смена игры очищает черновики
предыдущей игры. Это временный ввод интерфейса, а не сохранённая версия игры.
Устаревание источников показывается до фиксации.

## 5. Область задания и рисование

Протягивание прямоугольника создаёт область задания. Изменение её рамки меняет
только координаты области и не преобразует элементы игры. Справа — поле промта
без заголовка, с закрытием сверху справа и отправкой снизу слева поверх поля;
агент получает область, находящиеся в ней сущности и снимок, если
его поддерживает адаптер. YAML для области не строится.

Рисование работает поверх игры или загруженного изображения. Несколько
последовательных штрихов и надписей образуют один черновик до отправки.
Нажатие инструмента рисования открывает выбор цвета и толщины карандаша.
После отпускания кнопки рядом с курсором появляется маленькое поле с мигающим
курсором без видимой подписи; набор текста увеличивает поле. Щелчок без движения
не рисует точку, а открывает такой же ввод. Поля перемещаются и растягиваются
за нижний правый угол; следующий штрих можно начать на свободной области.

Внизу поля две разные операции:

- **Промт** — отправить текст как инструкцию агенту вместе с контекстом рисунка;
- **Текст** — сохранить текст как видимую надпись на рисунке, которая не является
  инструкцией агенту.

Агент получает исходное изображение, нанесённые пометки и промты с областью
применения (регион либо всё изображение). Загрузка изображения сама по себе
не заменяет фон игры и не публикует ассет. Инструмент рисования использует текущую
выделенную область или элемент, если они есть. После отправки возвращается
поверхность, с которой началось рисование.

## 6. Единый предпросмотр

Вход открывает Редактор с автоматической подготовкой игры; чат доступен во время
подготовки. Структурный макет — заполнение неизвестных/неоформленных элементов
прототипами в том же игровом renderer. Отдельного стартового экрана макета нет.
Неизвестные игровые правила не симулируются. Ошибка незавершённого документа
сохраняет последний доступный кадр с сообщением о неактуальности.

## 7. Прохождение и сценарий

Первое нажатие Игры запускает отладочное прохождение, последующие переключают
паузу и продолжение. Переход к Редактору или Рисованию ставит прохождение на
паузу. **Пауза останавливает всё:** пользовательские действия, игровые таймеры,
запланированные серверные операции и действия игрового ИИ. Отключение ввода или
анимации в iframe не считается выполнением этого требования.

В Сценарии есть иконка сохранения с подсказкой **Сохранить состояние прохождения**. Сохранённая точка имеет
имя и позволяет продолжить от соответствующего игрового состояния. Она
отличается от сохранения авторской версии проекта. Объявленный этап сценария
и сохранённая точка — разные категории. Этап открывает временный просмотр
сцены в том же renderer, в подтверждённой паузе, без записи прогресса партии.
Сохранение прохождения в таком просмотре недоступно; Игра возвращает реальную
партию. Неизвестная связь сцены даёт диагностику, а не выдуманное состояние.

Серверная модель согласована в [ADR-106](adrs/106-editor-debug-session-control-and-checkpoints.md):
пауза подтверждается после завершения текущей атомарной операции; снимок остаётся
на сервере, а возврат проверяет совместимость с текущей моделью и создаёт новую сессию
с текущими правилами и оформлением. Совместимые сохранения не удаляются только
из-за пересборки. Совместимость проверяется при выборе снимка; отдельной кнопки
проверки нет. При несовместимости показываются причина и предложение удалить
снимок; сетевой сбой или недоступная проверка не служат основанием для удаления.
Старые UI/изображения
специально для сохранений не удерживаются. Внешний игрок не получает доступ
к закрытому состоянию через editor-инструмент.

## 8. Отзывчивость и доступность

Ввод текста, рамки, рисунок, раскрытие меню и переключение панелей не ожидают
сеть, компиляцию или ИИ. Состояние незавершённого жеста и текстового ввода
остаётся локальным своему компоненту. Не нужны новые UI-библиотеки, универсальная
система окон или второй редакторский движок.

Компиляция начинается после применения изменения к авторским данным, а не
на каждый символ разового промта или каждый штрих. Визуальная обратная связь
жеста и черновой текст обновляются локально. После проверенной фиксации изменения
игрового содержания запускается обновление
предпросмотра. Одновременно выполняется одна сборка, а ожидающая работа заменяется
самым новым запросом. Отдельный таймер старого редактора (800 мс) в MVP выключен,
чтобы он не запускал вторую сборку вслед за этим же изменением. Правки только
названия и авторского описания обновляют проекцию без повторного запуска игры.
Старая файловая шкала отладки в MVP не записывается: пользователь сохраняет
только явно названные состояния через серверный механизм. Ответ для устаревших
исходников не подменяет более свежий предпросмотр. Явный запуск игры ожидает
актуальную успешную сборку. При ошибке остаётся последний доступный макет с
сообщением о неактуальности, без имитации успешной компиляции. UI-правки при
неизменных игровых артефактах доставляются в существующий iframe без навигации
по ADR-107. Сохранение неиспользуемого прототипа не обновляет игровой кадр.

Практические стартовые параметры для проверки: зона приближения меню 24 px,
задержка сворачивания 250 ms, допуск повторной точки 5 px. Это пересматриваемые
детали реализации. Односекундное время списка задано PM. Управление доступно
клавиатурой; на узком окне панели занимают доступное место и не создают
горизонтальную прокрутку всей страницы. Снижение движения учитывается.

## 9. Архитектурные зависимости

ADR-049/052 сохраняют authoring-источник и связывание сущности с несколькими
документами; ADR-057 — интерпретацию и проверяемое изменение. ADR-105 определяет
единый серверный путь точного предпросмотра/подтверждения
изменения. Он интегрирован в MVP: агент подготавливает проверенный вариант,
человек применяет именно просмотренный результат. Прямые действия человека
используют те же проверки; самостоятельных инструментов записи у агента нет.

Реализованность, проверки и открытые решения отслеживаются только в
[задаче MVP](../tasks/active/TSK-20260919-editor-mvp.md) и
[текущей итерации отзывчивости и смыслового редактирования](../tasks/active/TSK-20260921-editor-live-preview.md).
