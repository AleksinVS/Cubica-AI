# GSR-047: Estate Race — общая модель локальных участников S8

- **Дата:** 2026-08-13
- **Статус:** Completed
- **Предусловие:** GSR-046 завершил локальную продуктовую поверхность; PM
  одобрил session-owned participants и отказ от совместимости с тестовыми
  сессиями до production
- **Архитектура:** действующие ADR-019, ADR-051, ADR-059, ADR-060, ADR-084 и
  принятое уточнение ADR-059 от 2026-08-12

## Оглавление

- [Наблюдаемый результат](#наблюдаемый-результат)
- [Общая граница](#общая-граница)
- [Разрушительный pre-release cutover](#разрушительный-pre-release-cutover)
- [Отсутствие дрейфа игровой семантики](#отсутствие-дрейфа-игровой-семантики)
- [Приёмка](#приёмка)
- [Условия остановки](#условия-остановки)

## Наблюдаемый результат

Локальная партия Estate Race возвращает явный список мест сессии. Каждое место
имеет стабильный `seatId`, связанный игровой `playerId`, вид `human` и состояние
подключения `local`. Те же данные сопровождают создание, чтение и результат
действия, но не меняют деньги, владение, порядок хода или правила доступности.
Перед первой новой локальной партией Player позволяет выбрать от 2 до 6
участников из диапазона того же манифеста; runtime проверяет выбор и создаёт
ровно столько же игровых actor и неизменяемых мест сессии.

## Общая граница

`participants` принадлежит записи сессии общей платформы. S8 создаёт только
локальные человеческие места; `agent` зарезервирован принятым контрактом для
S9, а invite/join/WebSocket/reconnect остаются S10. Существующие единые
строители персональной проекции и canonical action availability переиспользуются
без второго вычислителя или отдельного Estate Race API.

`seatId` не является игровым термином. В player/turn-игре `playerId` остаётся
actor, на который точно ссылаются `state.players`, порядок и активный ход.
Нейтральная неходовая сессия не получает искусственные `state.players` или
`public.turn`, поэтому actor без отдельно ограниченного principal scope в ней
не определяется. Совпадение `seatId` и `playerId` в локальном materializer —
исполнительский инвариант S8, а не универсальное тождество идентичностей.

## Разрушительный pre-release cutover

Pre-release evidence не обнаружил поддерживаемого production deployment или
пользовательских сохранённых партий. Поэтому миграция S8 удаляет прежние
`game_sessions` и каскадно связанные principals, receipts, events и schedules,
затем делает `participants` обязательным. Immutable `game_bundles` сохраняются.
Disposable PostgreSQL proof подтвердил migrations 001–005 и store restart
roundtrip (`1/1`); миграция не подключается к неизвестной внешней базе
самостоятельно, а проверка использовала только одноразовую локальную базу.

## Отсутствие дрейфа игровой семантики

Общие contracts/runtime/player слои не содержат `estate-race`, названий клеток,
правил хода или условий победы. Платформа знает только места, actor scope,
проекцию и доступность опубликованных действий. Estate Race продолжает
определять фактических игроков и правила через Game Intent → Mechanics IR.

## Приёмка

- OpenAPI/JSON Schema и производные типы согласованы (canonical generated
  contracts и OpenAPI validation — `PASS`, contracts `7/7`);
- create/get/action/restore и agent-facing snapshot возвращают один и тот же
  неизменяемый список participants;
- локальный principal получает actor scope только из session participants;
- дубликаты мест/actor, `agent` в S8 и попытка передать participants клиентом
  отклоняются;
- публичный create-контракт принимает только необязательное число участников;
  значение вне диапазона манифеста отклоняется до создания сессии, а отсутствие
  поля сохраняет прежнее поведение с минимальным числом мест;
- миграция удаляет только session-owned данные и сохраняет bundles;
- player/turn fixture доказывает два локальных места, приватную проекцию,
  чужой actor и canonical availability; нейтральная неходовая fixture
  подтверждает отсутствие искусственных `state.players`/`public.turn`, а не
  наличие игровой очереди;
- Estate Race доказывает через обычный Player/create-путь выбор 2–6 мест,
  setup и ротацию тем же manifest без правки правил игры;
- game-agnostic (`10/10`) и player-core seam проверки не находят предметной
  ветки; полный Runtime API (`371` успешно, `2` штатно пропущено), Player Web
  (`280/280`) и production-сборка Player пройдены. Полный CMT suite в приёмку
  S8 не входит; проверен representative session-setup после обновления прямых
  consumers.

## Условия остановки

Срез останавливается при необходимости принимать actor из тела команды,
вводить второй builder проекции/availability, угадывать старые participants,
добавлять сетевое состояние или помещать термин Estate Race в shared слой.
