# ADR-079: Серверная проекция доступности действий

- **Статус:** Accepted
- **Дата:** 2026-07-12
- **Автор:** Codex по подтверждённому решению PM
- **Компоненты:** контракты сессии, deterministic runtime, Presenter, web/Phaser delivery
- **Уточнено:** ADR-084 разделяет неизменяемое определение действия,
  actor-scoped доступность и server-only авторизацию

## Оглавление

- [1. Контекст](#1-контекст)
- [2. Решение](#2-решение)
- [3. Отклонённые альтернативы](#3-отклонённые-альтернативы)
- [4. Последствия и ограничения](#4-последствия-и-ограничения)
- [5. Связанные документы](#5-связанные-документы)

## 1. Контекст

Runtime уже проверяет guard — декларативное условие допустимости действия —
перед изменением состояния. Однако клиентский снимок сессии раньше не сообщал
результат этой проверки. Интерфейс мог оставить активной кнопку после
изменения состояния и получить ожидаемый серверный отказ.

Копирование правил в React или Phaser создало бы второй источник истины и
неизбежное расхождение с сервером. Передача полного технического текста guard
раскрыла бы внутренние пути состояния и потенциально секретные значения.

## 2. Решение

1. Канонический каталог имеет две транспортные проекции. Неизменяемая
   `actionDefinitions` содержит `actionId`, безопасное описание и публичную
   схему параметров и кэшируется по `bundleHash`. Динамическая
   `actionAvailability` содержит только видимые данному actor действия, их
   статус и `basisStateVersion`; она зависит от principal, actor, session,
   версии состояния и bundle. Существующая объединённая форма внутри снимка
   считается переходной до миграции публичного API.
2. Для каждого видимого действия сервер возвращает только `actionId`, статус
   `available`, `unavailable` или `parameter-dependent` и при необходимости
   стабильный безопасный код причины. Технические выражения, пути и фактические
   значения guard в публичный ответ не входят.
3. Состояние `parameter-dependent` означает, что серверные проверки, не
   требующие пользовательского выбора, пройдены, а окончательное решение
   зависит от параметров команды. Клиент не должен блокировать такое действие
   до выбора параметров.
4. Проверка при dispatch всегда повторяется по актуальному серверному
   состоянию; клиентская блокировка не является границей безопасности.
   `availability` читает только доступный участнику контекст, а
   `authorization` остаётся на сервере и не превращается в публичный сигнал.
   Предикат, зависящий от закрытых данных, нельзя публиковать как доступность.
5. Presenter и delivery-клиенты блокируют `unavailable`, используют только
   опубликованную UI-привязку к `actionId` и сохраняют `commandId`, пока
   ожидается квитанция. Phaser получает только признак временной блокировки и
   серверную проекцию; он не вычисляет игровые правила.
6. Человекочитаемое объяснение формируется из безопасного кода причины и,
   когда игра объявила более точный текст, из содержимого манифеста. Клиент не
   показывает пользователю внутренний диагностический текст runtime.
7. Изменение является общей возможностью платформы и доказывается нейтральной
   фикстурой без идентификаторов или правил конкретной игры.
8. Наличие, отсутствие, порядок и число элементов, варианты выбора, preview и
   коды причин считаются частью информационного потока. Если два состояния
   имеют одинаковую actor-visible проекцию, их ответы каталога должны быть
   неразличимы. Скрытое намерение не возвращается; публично известное, но
   временно недоступное может вернуться с безопасным статусом.

## 3. Отклонённые альтернативы

1. **Оставить только серверный отказ после нажатия.** Отклонено: безопасность
   сохраняется, но интерфейс предлагает заведомо невозможное действие и мешает
   ведущему проводить игру.
2. **Повторить guard в игровом React/Phaser-коде.** Отклонено: появляются два
   источника истины, а общая платформа получает игровые исключения.
3. **Передать клиенту guard и вычислять его локально.** Отклонено: контракт
   раскрывает внутреннюю модель состояния, усложняет все каналы доставки и
   может открыть секретные данные.
4. **Удалять недоступные действия без причины.** Отклонено как единственная
   политика: для части интерфейсов скрытие уместно, но контракт должен позволять
   объяснить ведущему, почему ожидаемое действие заблокировано.
5. **Считать параметрическое действие недоступным до ввода всех параметров.**
   Отклонено: это заблокировало бы сам переход к выбору допустимых целей и
   значений.

## 4. Последствия и ограничения

- Публичный API получает раздельные definition/availability projections; до
  cutover существующее обязательное поле снимка остаётся документированным
  legacy-контрактом.
- Роль и состояние становятся согласованными между server-rendered проекцией и
  dispatch, а устаревшая вкладка всё равно безопасно получает отказ сервера.
- Для параметрических guard сервер до выбора может доказать только независимую
  от параметров часть. Полная проверка выполняется после отправки команды.
- Проекция доступности не становится универсальным построителем интерфейса
  параметров; форму допустимых значений задаёт опубликованная схема действия.
- Новые типы guard обязаны поддержать безопасную проекцию либо закрыто
  возвращать `runtime_unsupported`, не имитируя доступность на клиенте.
- Предикат с server-only данными не может использоваться как public
  availability даже без публикации текста причины: сам статус раскрыл бы один
  бит закрытого условия.
- Общий CDN/shared cache для actor-scoped availability запрещён. Ключ кэша
  обязан включать principal, actor, session, `basisStateVersion` и
  `bundleHash`.

## 5. Связанные документы

- ADR-051 — текущий OpenAPI-контракт модульного runtime-api.
- ADR-055 — чистота общего рендерера и декларативные действия UI.
- ADR-071 — доверенная роль ведущего фасилитируемой сессии.
- ADR-073 — граница Phaser-поля и обычного web-интерфейса.
- ADR-074 — безопасные параметрические ссылки на игровые ресурсы.
- ADR-084 — типизированный транзакционный язык и раздельные каналы
  availability/authorization.
