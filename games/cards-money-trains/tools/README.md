# Инструменты приёма карты «Карты, деньги, поезда»

Каталог хранит воспроизводимые преобразования авторских материалов в
проверяемые черновики. Ни один инструмент здесь не должен обходить общие
схемы или напрямую записывать неподтверждённые данные в runtime-манифест.

## Оглавление

- [Аннотация транспортной сети](#аннотация-транспортной-сети)
- [Карта для браузерной доставки](#карта-для-браузерной-доставки)
- [Локальный предпросмотр реальной игры](#локальный-предпросмотр-реальной-игры)
- [Векторный AI-источник](#векторный-ai-источник)
- [Грузы и новости из XLSX](#грузы-и-новости-из-xlsx)
- [Описания стран из PDF](#описания-стран-из-pdf)
- [Связанные страны и каталог](#связанные-страны-и-каталог)
- [Настройка партии](#настройка-партии)
- [Старт первого хода и обязательное обслуживание](#старт-первого-хода-и-обязательное-обслуживание)
- [Займы, денежный корректор и исключение команды](#займы-денежный-корректор-и-исключение-команды)
- [Порядок локомотивов и явный пропуск движения](#порядок-локомотивов-и-явный-пропуск-движения)
- [Формирование состава текущего локомотива](#формирование-состава-текущего-локомотива)
- [Погрузка, доставка и расчёты](#погрузка-доставка-и-расчёты)
- [Динамическое строительство](#динамическое-строительство)
- [Методические паузы и финальная рефлексия](#методические-паузы-и-финальная-рефлексия)
- [Итоговый расчёт и завершение партии](#итоговый-расчёт-и-завершение-партии)
- [Поверхность действий ведущего](#поверхность-действий-ведущего)
- [Повторяемый технический цикл ходов](#повторяемый-технический-цикл-ходов)
- [Проверки](#проверки)
- [Первый replay на реальных данных](#первый-replay-на-реальных-данных)
- [Граница ответственности](#граница-ответственности)

## Аннотация транспортной сети

`convert-map-annotation.mjs` — тонкий игровой адаптер общего конвейера
аннотаций карты. Он подставляет правила именно этой игры, но не дублирует
общую проверку координат, ссылок и статусов.

## Карта для браузерной доставки

`build-map-asset.mjs` воспроизводимо создаёт оптимизированную WebP-карту из
авторского файла `Начальная транспортная сеть.png` и синхронизирует
`asset-provenance.json` — запись происхождения, размеров, контрольных сумм и
параметров преобразования.

Десять начальных дорог являются постоянной частью партии и не удаляются,
поэтому они входят в единственную неизменяемую текстуру вместе с зелёными
маркерами подключённых станций. Это даёт точное совпадение с макетом без
второй полноразмерной текстуры. Phaser рисует поверх неё только изменяемые
состояния:

- новые дороги и полустанки;
- закрытия и строительство;
- выбор и серверные подсветки;
- технику, грузы и анимации.

Такое разделение является оформлением этой игры. Сервер по-прежнему хранит
все узлы и дороги как данные и не читает пиксели карты.

Сознательная пересборка:

```bash
node games/cards-money-trains/tools/build-map-asset.mjs
```

Проверка актуальности без изменения файлов:

```bash
node games/cards-money-trains/tools/build-map-asset.mjs --check
```

## Локальный предпросмотр реальной игры

`prepare-local-preview.mjs` создаёт временную копию нормативного пакета только
под `.tmp/editor-worktrees`, снимает публикационные блокеры лишь с этой копии
и регистрирует её через существующую границу `contentSourceId` — идентификатор
изолированного источника содержимого предпросмотра.

Исходный `games/cards-money-trains/game.manifest.json` остаётся
`runtimeReady: false`. Mock-пакет не изменяется и используется только как
регрессионный стенд.

При уже запущенных Runtime API и Player Web реальную игру можно открыть одной
командой:

```bash
PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=/snap/bin/chromium \
node games/cards-money-trains/tools/prepare-local-preview.mjs \
  --runtime-url http://127.0.0.1:3001 \
  --player-url http://127.0.0.1:3000 \
  --open
```

Подготовщик создаёт сессию внутри того же браузера, который её открывает,
поэтому защищённый runtime-токен остаётся в недоступном JavaScript файле
cookie. Закрытие браузера или `Ctrl+C` завершает только браузер
предпросмотра; временные файлы остаются в `.tmp` для последующего сравнения.

Для агентского запуска без нового браузера можно только зарегистрировать
источник и получить его параметры:

```bash
node games/cards-money-trains/tools/prepare-local-preview.mjs \
  --runtime-url http://127.0.0.1:3001
```

## Векторный AI-источник

`extract-vector-map-review.mjs` читает PDF-совместимую часть файла
`draft/trains/Карта Гвиней  а4.ai`. Алгоритм ограничен текущим авторским
источником и выполняет следующие действия:

1. Читает страницу и её открытый сжатый поток рисования; служебные данные
   Illustrator не разбираются.
2. Находит в слое терминалов 23 повторяющиеся шестерёнки по их векторному
   примитиву, а не по закодированному тексту.
3. Сопоставляет центры шестерёнок с `terminal-1`…`terminal-23` из проверяемой
   аннотации начальной сети и вычисляет аффинное преобразование координат.
4. Извлекает ровно 981 верхнеуровневую обводку `S` в анонимные кандидаты;
   известные вложенные XObject не раскрываются. Новый XObject или другой
   оператор рисования останавливает обработку.
5. Проверяет PNG-фон по контрольной сумме и заголовку IHDR `5079 × 3627`.
6. Проверяет результат по локальной JSON Schema и создаёт контрольное SVG-
   наложение.

AI-файл, опорная JSON-аннотация и PNG-фон читаются как стабильные снимки:
инструмент сверяет файловый идентификатор, размер и время изменения до и после
чтения. Опорный JSON разбирается из тех же байтов, по которым вычислена его
контрольная сумма. Сжатый PDF-поток имеет жёсткий лимит распаковки 16 МиБ.

Перед записью проверяются обычные пути, символические ссылки и жёсткие ссылки
(два имени одного файла). JSON и SVG сначала полностью записываются в новые
временные файлы с исключительным созданием, синхронизируются с диском и только
затем атомарно переименовываются поверх производных файлов.

Сознательная перегенерация:

```bash
npm run generate:cards-money-trains-vector-review
```

Проверка, что производные файлы не устарели:

```bash
node games/cards-money-trains/tools/extract-vector-map-review.mjs --check
```

`build-vector-map-topology-review.mjs` выполняет следующий безопасный шаг:
создаёт отдельный fail-closed отчёт (то есть результат, который при
неопределённости запрещает публикацию) о формальных замыканиях и разрывах.
Инструмент:

1. повторно проверяет сырой векторный результат и классификацию по их JSON
   Schema и контрольным суммам;
2. сохраняет координаты начала и конца всех 978 предложенных к включению
   открытых путей;
3. перечисляет 15 пар разных концов на расстоянии не более трёх пикселей, но
   не соединяет ни одну из них;
4. отдельно фиксирует один неоднозначный замкнутый контур и две исключённые
   рамки страницы;
5. подтверждает, что непосредственно публикуемых полигонов пока нет.

Сознательная генерация и проверка актуальности:

```bash
node games/cards-money-trains/tools/build-vector-map-topology-review.mjs
node games/cards-money-trains/tools/build-vector-map-topology-review.mjs --check
```

Отчёт намеренно не аппроксимирует кривые, не вычисляет пересечения, не
«приклеивает» близкие концы и не создаёт области. Эти действия потребуют
отдельного проверяемого этапа после смыслового визуального решения.

## Грузы и новости из XLSX

`import-cargo-news.mjs` создаёт воспроизводимый непубликационный снимок двух
листов `draft/trains/Колоди + Новости.xlsx`. Исходный XLSX не изменяется.
Вспомогательный `read-xlsx-rows.py` читает ограниченный и не содержащий формул
OpenXML-пакет стандартной библиотекой Python; сторонние пакеты не нужны.

Производный файл
`authoring/fixtures/cargo-news.intake.json` содержит:

- 174 исходные строки грузов с номером строки, терминалами, выплатой и
  принадлежностью базовой колоде либо добавлению новости;
- 34 новости и прямые связи новостей № 1–10 с добавляемыми строками грузов;
- контрольную сумму исходного XLSX;
- подтверждение, что каждая строка означает одну физическую карту, а
  одинаковые строки являются разными экземплярами;
- исходные формулировки, происхождение и связи, по которым исполнимые эффекты
  проверяются отдельно от отображаемого текста.

`build-card-lifecycle.mjs` переносит этот снимок в непубликационную игровую
сборку: создаёт 23 защищённые колоды терминалов, одноразовую колоду из 34
новостей, жизненный цикл выбора грузов и все 34 подтверждённых эффекта
новостей. В частности, № 19 рассчитывает совместную квоту вагонов и
локомотивов и позволяет ведущему выбрать конкретные единицы по решению
команды; № 28/29 выплачивают бонус сразу при первом выходе груза из исходного
терминала и сбрасываются перед новостью следующего хода. Новость № 14 включает
прогрессивный налог до конца партии или до его отмены другой новостью. Налог
пересчитывается при каждом завершении обслуживания: паровозная гильдия платит
за активные локомотивы сверх трёх, компания-перевозчик — за активные вагоны
сверх пяти. Недостаток денег отклоняет всё завершение обслуживания без
частичных списаний. Новость № 16 атомарно собирает по пять монет только с
команд, имевших больше пятнадцати монет в начале новостного этапа. Новость
№ 22 включает на текущий ход сбор в одну монету с первого успешного движения
каждого локомотива; повторный переход того же локомотива не оплачивается
повторно. Временные эффекты сбрасываются при входе в новостной этап следующего
хода, а постоянные цены новости № 34 сохраняются до конца партии.

Артефакты имеют `publishable: false`: это проверенный исполняемый черновик, но
не готовая к выпуску игра. Структуру входного снимка задаёт соседняя JSON
Schema, проверяемая Ajv в строгом режиме.

Сознательная перегенерация:

```bash
npm run generate:cards-money-trains-cargo-news
```

Проверка исходника, схемы, детерминизма и актуальности производного файла:

```bash
npm run verify:cards-money-trains-content
```

Сознательная сборка жизненного цикла после обновления входного снимка:

```bash
node games/cards-money-trains/tools/build-card-lifecycle.mjs
npm run compile:manifests -- --game cards-money-trains
```

Проверка детерминизма генератора и выполнение правил через обычный серверный
исполнитель:

```bash
node games/cards-money-trains/tools/build-card-lifecycle.mjs --check
node --experimental-strip-types --test \
  games/cards-money-trains/tools/card-lifecycle.test.mjs \
  games/cards-money-trains/tools/news-economic-effects.test.mjs \
  games/cards-money-trains/tools/news-progressive-tax.test.mjs \
  games/cards-money-trains/tools/news-scalar-effects.test.mjs
npm run compile:manifests -- --game cards-money-trains --check
```

## Описания стран из PDF

`import-country-descriptions.mjs` воспроизводимо извлекает 10 страниц из
`draft/trains/Описания стран Гвинеи 1 стол.pdf` системным `pdftotext`; новые
пакеты для этого не нужны. Производный
`authoring/fixtures/country-descriptions.intake.json` сохраняет:

- путь и контрольную сумму точной версии PDF;
- номер страницы и исходный порядок каждого описания;
- исходное название, строку терминалов и метки терминалов;
- текст с исходным переносом строк и копию, в которой мягкие переносы PDF
  соединены пробелами.

Артефакт имеет статус `review-draft` — «черновик для проверки» — и
`publishable: false`. Его номера терминалов остаются исходными метками
авторского документа: импортёр не объявляет их подтверждёнными
идентификаторами карты, не назначает странам `countryId`, не добавляет
координаты и не подключает содержимое к runtime-манифесту.

Сознательная перегенерация:

```bash
npm run generate:cards-money-trains-country-descriptions
```

Проверка выполняется общей дешёвой командой содержимого:

```bash
npm run verify:cards-money-trains-content
```

## Связанные страны и каталог

`build-country-content.mjs` соединяет проверенные описания с игровыми
объектами, не меняя исходный PDF-снимок. Генератор создаёт 10 стран с
устойчивыми идентификаторами, связывает с ними терминалы 1–23 и добавляет
описания в два доступных ведущему места:

- карточку выбранного терминала на карте;
- отдельный каталог стран, который позволяет открыть описание без выбора
  терминала.

Это уже исполняемая связь содержимого с картой, однако она остаётся
непубликационным игровым черновиком. Точные полигоны стран и областей ещё не
подготовлены, поэтому страна пока не является кликабельной геометрической
областью карты.

Сознательная перегенерация и узкая проверка:

```bash
node games/cards-money-trains/tools/build-country-content.mjs
node games/cards-money-trains/tools/build-country-content.mjs --check
node --experimental-strip-types --test \
  games/cards-money-trains/tools/country-content-linking.test.mjs
```

## Настройка партии

`build-session-setup.mjs` создаёт непубликационный, но исполнимый серверный
срез обычной настройки. Он использует только общие операции игрового движка:
создание объектов, выбор, воспроизводимое случайное упорядочивание, переход по
очереди и атомарное изменение состояния.

В срез входят:

- динамические команды вместо прежних четырёх записей с фиксированными
  идентификаторами;
- все подтверждённые составы от 4 до 12 команд: при чётном количестве типы
  делятся поровну, при нечётном перевозчиков на одну команду больше;
- 10 начальных монет, два резервных вагона перевозчика либо один резервный
  локомотив гильдии;
- неповторяющийся цвет из ограниченной контрастной палитры;
- воспроизводимая случайная очередь размещения;
- размещение ведущим на открытых терминалах технической начальной сети;
- ограничение не более двух локомотивов на терминале и переход к следующей
  команде только после размещения всей её техники.

Техническая сеть берётся из `annotations/initial-network.review.json`. Она
допустима для разработки и проверок, но остаётся непубликационной до
визуального подтверждения наложения автором. Генератор принимает только девять
точных сочетаний состава для 4–12 команд и атомарно отклоняет любое другое
соотношение типов.

Генератор жизненного цикла карт больше не владеет моделью команд и не может
вернуть прежнюю фиксированную структуру. Рекомендуемый порядок сознательной
перегенерации:

```bash
node games/cards-money-trains/tools/build-card-lifecycle.mjs
npm run generate:cards-money-trains-session-setup
npm run compile:manifests -- --game cards-money-trains
```

Узкая проверка настройки:

```bash
npm run verify:cards-money-trains-session-setup
npm run compile:manifests -- --game cards-money-trains --check
```

## Старт первого хода и обязательное обслуживание

`build-operating-turn.mjs` соединяет завершённую расстановку, физические колоды
и первый обычный ход. Генератор остаётся локальным для этой игры и собирает
правила из существующих общих операций Mechanics: проверок состояния,
денежного перевода, изменения атрибутов объектов, ограниченной выборки и
журналируемого события.

Исполняемая граница включает:

- защищённый запуск обычной партии только после полной настройки и подготовки
  колод;
- явный пропуск новости первого хода без расходования карты или случайности;
- оплату по одной монете за каждый активный локомотив, вагон и удерживаемый
  груз;
- определение плательщика по владельцу объекта в серверном состоянии, а не по
  данным от интерфейса;
- атомарный отказ при повторной оплате, недоступном объекте или недостатке
  денег;
- завершение обслуживания только после обработки всех обязательных единиц;
- прямое завершение без списаний и без пообъектных действий при новости № 25;
- точный пересчёт прогрессивного налога новости № 14 при завершении
  обслуживания и атомарный отказ при недостатке денег;
- переход к фазе `market`;
- повторяемые покупки вагонов перевозчиками и локомотивов гильдиями по
  серверной цене с учётом запретов и скидок новостей;
- повторяемая продажа пустой и отсоединённой техники по фиксированной цене;
- отсутствие игровых пределов рынка и числа единиц у команды: вместимость
  коллекции в 64 объекта остаётся только технической защитой исполнения;
- явное завершение рынка после любого количества сделок, включая ноль;
- завершение отчётности после пустого строительства, увеличение номера хода
  ровно на один и переход к новости следующего хода.

Реальное строительство и содержательная отчётность намеренно не входят в этот
генератор. Поэтому пакет остаётся непубликационным и сохраняет
`runtimeReady: false`.

Сознательная последовательная перегенерация:

```bash
node games/cards-money-trains/tools/build-session-setup.mjs
node games/cards-money-trains/tools/build-card-lifecycle.mjs
node games/cards-money-trains/tools/build-operating-turn.mjs
npm run compile:manifests -- --game cards-money-trains
```

Три генератора композиционно идемпотентны: повторный запуск каждого после
остальных не должен менять результат. Узкая проверка границы:

```bash
node games/cards-money-trains/tools/build-session-setup.mjs --check
node games/cards-money-trains/tools/build-card-lifecycle.mjs --check
node games/cards-money-trains/tools/build-operating-turn.mjs --check
node --experimental-strip-types --test \
  games/cards-money-trains/tools/operating-turn-lifecycle.test.mjs \
  games/cards-money-trains/tools/news-progressive-tax.test.mjs
npm run compile:manifests -- --game cards-money-trains --check
```

## Займы, денежный корректор и исключение команды

`build-facilitator-economy.mjs` реализует подтверждённый выбор ведущего при
нехватке денег на обязательный платёж. Сам обязательный платёж по-прежнему
атомарно отклоняется: система не создаёт автоматический кредит и не допускает
отрицательный баланс. После отказа ведущий явно:

- выдаёт займ, который одновременно увеличивает деньги и непогашенный долг;
- либо исключает команду из партии.

Отдельные действия позволяют ведущему в любой момент начислить или списать
целое неотрицательное число монет. Списание при нехватке средств полностью
отклоняется. Погашение займа является отдельным действием: оно одновременно
списывает деньги и уменьшает долг, поэтому ручное списание процентов не может
неявно изменить основной долг.

При исключении команда получает состояние `excluded` и больше не проходит
проверки активного участника. Её долг списывается, техника переводится в уже
существующее рыночное состояние `sold`, а исторический владелец сохраняется
только для аудита. Груз из вагона становится доступным на текущем терминале;
напечатанная станция отправления карточки не меняется. Если исключается
владелец текущего локомотива, сервер атомарно переводит очередь к следующему
локомотиву либо завершает этап движения.

Генератор использует только существующие общие операции выбора, ограниченного
перебора, денежного перевода, изменения объектов и журнала. Новых
платформенных операций или специальных ветвей выполнения нет.

Пять действий публикуются в общем экране ведущего и временно используют
универсальную доступную форму: выбор действующей команды и, где требуется,
целая сумма. Целевой табличный корректор «команды × начисление/списание» пока
остаётся долгом интерфейса; его реализация не должна менять уже готовые
серверные действия и денежные правила.

Сознательная генерация и узкая проверка:

```bash
node games/cards-money-trains/tools/build-facilitator-economy.mjs
node games/cards-money-trains/tools/build-facilitator-economy.mjs --check
node --experimental-strip-types --test \
  games/cards-money-trains/tools/facilitator-economy-lifecycle.test.mjs
```

## Порядок локомотивов и явный пропуск движения

`build-movement-order.mjs` реализует отдельную серверную границу после рынка и
выбора грузов. Рынок уже допускает повторяемые защищённые сделки и соединён с
грузовой фазой явным действием ведущего; приоритетная последовательность
выбора грузов развивается отдельным срезом.
Генератор не вычисляет порядок в интерфейсе и не добавляет предметную ветку в
общий сервер. Он собирает правило игры из существующих универсальных операций
выборки, многокритериального упорядочивания, сохранённой последовательности,
перемещения по объявленному графу, изменения состояния и журналирования.

В границу входят:

- выбор всех активных локомотивов с защитой от рассогласования их служебных
  признаков;
- порядок по положению справа налево, затем по деньгам владельца, затем по
  числу его активных локомотивов; полное равенство разрешается
  воспроизводимой случайностью;
- однократное сохранение полного порядка и текущего локомотива на весь этап;
- установка пяти единиц действия каждому участвующему локомотиву;
- перемещение только текущего локомотива по выбранной ведущим дороге
  технической сети: интерфейс передаёт дорогу, а идентификатор локомотива
  остаётся серверным;
- выбор дороги кликом непосредственно по карте либо через динамический
  доступный список текущих публичных дорог; оба пути отправляют только
  `{edgeId}` и не вычисляют локально допустимость движения;
- атомарная проверка открытости дороги и станций, смежности, вместимости,
  перенос прицепленных вагонов и списание одной единицы действия;
- при действующей новости № 22 — атомарный разовый сбор с владельца при
  первом успешном движении конкретного локомотива; недостаток денег отклоняет
  весь переход, но не запрещает ведущему явно пропустить локомотив;
- возможность выполнить несколько переходов тем же локомотивом до явного
  пропуска либо исчерпания его запаса действия;
- явный пропуск только сервером выбранного текущего локомотива, без
  передаваемого из интерфейса идентификатора;
- переход к следующему элементу сохранённого порядка, а после последнего
  пропуска — к отдельному этапу расчётов;
- отдельные записи игрового журнала о подготовленном порядке, каждом пропуске
  и завершении этапа движения.

Формирование состава, погрузка и доставка реализованы следующими отдельными
срезами. Обслуживание уже соединено с грузовым этапом повторяемым рынком.
Техническая стартовая сеть и вся обычная партия остаются непубликационными до
завершения остальных блоков и визуального подтверждения наложения автором.

Сознательная последовательная перегенерация:

```bash
node games/cards-money-trains/tools/build-session-setup.mjs
node games/cards-money-trains/tools/build-card-lifecycle.mjs
node games/cards-money-trains/tools/build-operating-turn.mjs
node games/cards-money-trains/tools/build-movement-order.mjs
npm run compile:manifests -- --game cards-money-trains
```

Узкая проверка:

```bash
node games/cards-money-trains/tools/build-movement-order.mjs --check
node --experimental-strip-types --test \
  games/cards-money-trains/tools/movement-order-lifecycle.test.mjs
npm run compile:manifests -- --game cards-money-trains --check
```

## Формирование состава текущего локомотива

`build-train-formation.mjs` добавляет к серверному этапу движения сохраняемый
выбор вагонов и атомарное групповое сцепление. «Атомарное» означает, что
операция либо полностью прицепляет все выбранные вагоны и списывает одну
единицу действия, либо не меняет партию вообще.

В срез входят:

- отдельные действия «отметить» и «снять отметку», принимающие только один
  скалярный `wagonId`; условного двойного переключения в одном плане нет;
- сохранение целевого локомотива на самом вагоне в публичном серверном
  состоянии, поэтому обновление страницы не теряет незавершённый выбор;
- текущий локомотив берётся только из серверного порядка движения и никогда
  не передаётся интерфейсом;
- проверка фазы, текущего и ещё не завершившего движение локомотива, наличия
  хотя бы одной единицы действия, типа и активности вагона, общей сети,
  станции, свободы и совместимости;
- подтверждение без параметров: сервер заново выбирает все отмеченные вагоны,
  сверяет полную группу и применяет универсальную ограниченную операцию
  `core.entities.each` в каноническом порядке идентификаторов;
- ровно одна единица действия за всю подтверждённую группу независимо от числа
  вагонов;
- очистка отметок после успешного сцепления и при явном пропуске текущего
  локомотива;
- одна точная запись игрового журнала с локомотивом, полным списком вагонов,
  их количеством, владельцем, стоимостью и номером хода;
- отдельные бесплатные действия ручного прицепления и отцепления после того,
  как текущий локомотив завершил хотя бы одно движение на открытом терминале;
- расчёт тарифа ручного отцепления по 2 монеты за участок: у гружёного вагона
  учитывается путь до первого отклонения от любого равного кратчайшего
  маршрута, у пустого — весь путь после прицепления;
- включение самого участка отклонения в оплачиваемый путь и остановка счётчика
  после него; последующие участки тариф не увеличивают;
- атомарный перевод денег от перевозчика паровозной гильдии: недостаток денег
  не оставляет частичной оплаты или отсоединённого вагона;
- после отцепления гружёного вагона начало его активного перевозочного участка
  переносится на текущий терминал, поэтому следующая гильдия получает оплату
  только за последующий путь;
- управление через карту кликом по вагону, видимый знак сохранённого выбора и
  те же действия в обычных доступных формах ведущего. Интерфейс не вычисляет
  допустимость: окончательное решение всегда принимает сервер.

Если терминал назначения временно закрыт, первый следующий участок считается
точкой отклонения без запуска поиска пути. Если терминал открыт, но закрытия
других дорог полностью разорвали путь до него, поиск кратчайшего пути
возвращает неаварийный результат «пути нет»: уже пройденный участок входит в
тариф, после чего счётчик этого вагона останавливается. Законное движение
локомотива в доступной части сети при этом сохраняется. Неизвестная сеть,
неизвестный или недоступный узел по-прежнему считаются ошибкой данных и
откатывают транзакцию.

Сознательная последовательная перегенерация:

```bash
node games/cards-money-trains/tools/build-session-setup.mjs
node games/cards-money-trains/tools/build-card-lifecycle.mjs
node games/cards-money-trains/tools/build-operating-turn.mjs
node games/cards-money-trains/tools/build-movement-order.mjs
node games/cards-money-trains/tools/build-train-formation.mjs
node games/cards-money-trains/tools/build-cargo-settlement.mjs
node games/cards-money-trains/tools/build-construction-cycle.mjs
node games/cards-money-trains/tools/build-country-content.mjs
node games/cards-money-trains/tools/build-facilitated-session.mjs
node games/cards-money-trains/tools/build-facilitator-economy.mjs
node games/cards-money-trains/tools/build-session-completion.mjs
npm run compile:manifests -- --game cards-money-trains
```

Все одиннадцать генераторов сохраняют результат друг друга при повторном запуске.
Узкая проверка среза:

```bash
node games/cards-money-trains/tools/build-train-formation.mjs --check
node --experimental-strip-types --test \
  games/cards-money-trains/tools/train-formation-lifecycle.test.mjs
npm run compile:manifests -- --game cards-money-trains --check
```

## Погрузка, доставка и расчёты

`build-cargo-settlement.mjs` добавляет обычной партии погрузку выбранного
груза и отдельный этап расчётов после движения. Интерфейс передаёт только
идентификаторы публичных объектов; команды-владельцы, локомотив, маршрут,
выплаты и тариф берутся из подтверждённого состояния на сервере.

В срез входят:

- `cargo.load` с закрытыми параметрами `{wagonId, cargoId}`: активный пустой
  вагон должен принадлежать держателю груза, находиться на открытом исходном
  терминале и использовать ту же сеть;
- груз может остаться у логистической компании без погрузки; завершение этапа
  блокируется только незавершённым предложением карт, а не придуманной
  последовательностью команд;
- последний явный пропуск локомотива открывает этап `settlement`, сохраняя
  очистку незавершённого выбора состава;
- `settlement.cargo.deliver` принимает только `{wagonId}` и на сервере
  определяет груз, прицепленный локомотив и обе команды;
- банк сначала переводит стоимость груза с текущей надбавкой логистической
  компании, затем компания полностью платит гильдии по 2 монеты за ребро
  текущего кратчайшего открытого маршрута;
- недостаток средств после банковской выплаты отклоняет всю доставку вместе с
  переводами, журналом и изменением связей;
- успешная доставка журналирует полный расчёт, отцепляет вагон, освобождает
  его грузовой слот, помечает карту доставленной и снимает владение картой;
- отдельное завершение расчётов открывает строительство даже при грузе,
  доставку которого пришлось отклонить из-за недостатка средств.

Генератор расчётов не владеет рынком, очередностью выбора грузов, показом
оставшейся карты или новостями № 28/29: эти правила реализованы соседними
генераторами. Он сохраняет их готовый статус при повторной сборке, чтобы
поздний генератор не возвращал уже закрытые пункты в список блокеров.

Сознательная последовательная перегенерация:

```bash
node games/cards-money-trains/tools/build-session-setup.mjs
node games/cards-money-trains/tools/build-card-lifecycle.mjs
node games/cards-money-trains/tools/build-operating-turn.mjs
node games/cards-money-trains/tools/build-movement-order.mjs
node games/cards-money-trains/tools/build-train-formation.mjs
node games/cards-money-trains/tools/build-cargo-settlement.mjs
node games/cards-money-trains/tools/build-construction-cycle.mjs
node games/cards-money-trains/tools/build-country-content.mjs
node games/cards-money-trains/tools/build-facilitated-session.mjs
node games/cards-money-trains/tools/build-facilitator-economy.mjs
node games/cards-money-trains/tools/build-session-completion.mjs
npm run compile:manifests -- --game cards-money-trains
```

Все одиннадцать генераторов сохраняют результат друг друга при повторном запуске.
Узкая проверка среза после компиляции:

```bash
node games/cards-money-trains/tools/build-session-setup.mjs --check
node games/cards-money-trains/tools/build-card-lifecycle.mjs --check
node games/cards-money-trains/tools/build-operating-turn.mjs --check
node games/cards-money-trains/tools/build-movement-order.mjs --check
node games/cards-money-trains/tools/build-train-formation.mjs --check
node games/cards-money-trains/tools/build-cargo-settlement.mjs --check
node games/cards-money-trains/tools/build-construction-cycle.mjs --check
node games/cards-money-trains/tools/build-country-content.mjs --check
node games/cards-money-trains/tools/build-facilitated-session.mjs --check
node games/cards-money-trains/tools/build-facilitator-economy.mjs --check
node games/cards-money-trains/tools/build-session-completion.mjs --check
node --experimental-strip-types --test \
  games/cards-money-trains/tools/construction-cycle-lifecycle.test.mjs \
  games/cards-money-trains/tools/cargo-settlement-lifecycle.test.mjs \
  games/cards-money-trains/tools/movement-order-lifecycle.test.mjs \
  games/cards-money-trains/tools/train-formation-lifecycle.test.mjs \
  games/cards-money-trains/tools/card-lifecycle.test.mjs \
  games/cards-money-trains/tools/facilitator-economy-lifecycle.test.mjs \
  games/cards-money-trains/tools/session-completion-lifecycle.test.mjs \
  games/cards-money-trains/tools/real-operating-turn.test.mjs
npm run compile:manifests -- --game cards-money-trains --check
```

## Динамическое строительство

`build-construction-cycle.mjs` заменяет прежние действия, рассчитанные на
фиксированные идентификаторы четырёх команд, шестью параметризованными
действиями обычной партии. Ведущий может менять согласованные вклады любой из
4–12 команд, выбирать режим, строить несколько дорог и полустанков, а затем
отдельно завершить фазу.

Сервер сам определяет маршрут дороги с минимальным числом областей, стоимость
по две монеты за область и воспроизводимый случайный выбор при равенстве.
Вклады остаются только предварительными соглашениями: деньги списываются
атомарно вместе с созданием объекта и не списываются при отказе. Полустанок
стоит пять монет, проверяется вне областей обоих концов дороги и делит её на
две части. Обычные построенные объекты открываются в начале `N+2`.
Обе конечные станции строящейся дороги также закрываются до её открытия; если
несколько проектов используют одну станцию, она ждёт самого позднего срока,
а независимые причины закрытия не снимаются.

Новость № 26 предоставляет скидку до шести сегментов только первой успешно
построенной дороге текущего хода; полустанок и отклонённая команда скидку не
расходуют. Новость № 27 создаёт государственную дорогу `12 ↔ 22`, которая
открывается в `N+1`, либо безопасно завершается без изменения сети, если такая
дорога уже существует.

Пока автор не подтвердил наложение сети и точные замкнутые контуры областей,
генератор использует явно технические вертикальные области поверх
канонического поля `5079 × 3627`. Они позволяют исполнять и проверять весь
цикл, но не являются содержимым для публикации. Исходные дороги получают
согласованный с этими областями проверяемый план маршрута; после замены
геометрии игровые действия и серверные операции менять не потребуется.

Сознательная перегенерация и узкая проверка:

```bash
node games/cards-money-trains/tools/build-construction-cycle.mjs
node games/cards-money-trains/tools/build-construction-cycle.mjs --check
node --experimental-strip-types --test \
  games/cards-money-trains/tools/construction-cycle-lifecycle.test.mjs
```

Проверка покрывает идемпотентность генератора, произвольный состав 4–12
команд, атомарность денег и графа, неверную цену и недостаток средств,
допустимость полустанка, сохранение независимых причин закрытия, открытие в
`N+2`, а также новости № 26 и № 27.

## Методические паузы и финальная рефлексия

`build-facilitated-session.mjs` добавляет игровой методический сценарий —
управляемые ведущим остановки, во время которых участники обсуждают
происходящее и возвращаются к партии. Для каждой из двух пауз доступны только
три явных перехода: начать, отложить и завершить. Постоянный пропуск паузы и
обязательное указание причины не являются цифровой механикой: дополнительные
решения ведущий принимает вне системы.

Генератор также хранит пять подтверждённых вопросов для финального разбора:

1. Какая была стратегия изначально?
2. К чему нужно было адаптироваться?
3. К чему удалось адаптироваться? За счет чего?
4. К чему адаптироваться не удалось? Почему?
5. Как бы вы оценили результаты игры для вас и для других команд?

Эти вопросы готовы как содержимое. Исполнимый финальный расчёт и завершение
партии собирает следующий генератор; отдельное визуальное представление
результатов остаётся задачей интерфейса.

Сознательная перегенерация и узкая проверка:

```bash
node games/cards-money-trains/tools/build-facilitated-session.mjs
node games/cards-money-trains/tools/build-facilitated-session.mjs --check
node --experimental-strip-types --test \
  games/cards-money-trains/tools/facilitated-session-lifecycle.test.mjs
```

## Итоговый расчёт и завершение партии

`build-session-completion.mjs` заменяет ранние временные действия завершения
защищённым игровым сценарием. Ведущий может запросить завершение только на
этапе отчётности после полного раунда, отдельно отменить запрос либо
подтвердить необратимый переход к завершённой партии.

При подтверждении сервер одним атомарным действием:

- выбирает фактически активные команды и не включает исключённые;
- фиксирует у активных вагонов и локомотивов действующую цену покупки;
- считает `деньги + техника − непогашенный долг`;
- публикует отдельные рейтинги перевозчиков и паровозных гильдий;
- сохраняет совместное место и всех победителей при равенстве;
- переводит сессию из `finishing` в `finished`.

Результат и его исходные цены сохраняются в публичном состоянии сессии.
Рыночная техника не входит даже в расшифровку расчёта. Генератор использует
только универсальные ограниченные выборки, подсчёт и стабильное ранжирование,
поэтому не создаёт специальной ветви в общем сервере.

Этот генератор выполняется последним после генераторов обычного хода,
методических пауз и экономики ведущего:

```bash
node games/cards-money-trains/tools/build-session-completion.mjs
node games/cards-money-trains/tools/build-session-completion.mjs --check
node --experimental-strip-types --test \
  games/cards-money-trains/tools/session-completion-lifecycle.test.mjs
```

## Поверхность действий ведущего

Текущая авторская сборка содержит 99 действий и столько же исполняемых
планов. Из них 93 относятся к обычной партии, включая защищённую подготовку
колод и пять доступных в любой момент действий ведущего для денег, займа и
исключения. Ещё шесть технических replay-действий находятся вне обычного
сценария и предназначены только для воспроизводимых проверок.

Исполнитель дополнительно скрывает действия, которые недоступны в текущем
состоянии партии. Если доступность зависит от ещё не введённого параметра,
интерфейс оставляет форму, а окончательную проверку выполняет сервер. Такое
разделение не позволяет интерфейсу самостоятельно объявлять действие
разрешённым.

Целевая проверка счётчиков, видимости и связи действий с полем:

```bash
node --experimental-strip-types --test \
  games/cards-money-trains/tools/facilitator-action-surface.test.mjs
```

## Повторяемый технический цикл ходов

`build-operating-turn.mjs` добавляет повторяемые рыночные сделки и две
закрывающие команды ведущего без параметров:

- `market.purchase.*` и `market.sell.*` оставляют фазу открытой, поэтому
  ведущий может провести любое нужное число сделок;
- `market.phase.finish` переводит рынок в грузовую фазу после сделок или сразу;
- `reporting.phase.finish` доступна только после корректно завершённого
  строительства, фиксирует в журнале номер завершённого хода, увеличивает его
  ровно на один и открывает новостной этап следующего хода.

В результате техническая последовательность имеет вид:

```text
setup → news → maintenance → market → cargo → movement → settlement
  → construction → reporting → news
```

Метки уже выполненных действий текущего хода не очищаются принудительно:
после увеличения номера они естественно относятся к предыдущему ходу.
Временные эффекты новости сбрасываются только защищённым действием открытия
следующей новости или периода застоя. Постоянные цены, созданные объекты,
последовательность строительства и остальные долгоживущие данные сохраняются.

Для PM это означает: уже можно многократно прогонять техническую партию,
проводить реальные сделки на рынке и использовать строительство на технических
областях. До полной партии остаются другие перечисленные в плане блоки:
визуальное подтверждение наложения начальной сети, реальные полигоны областей,
ручное отцепление и финальный сценарий с итоговым расчётом и учебной
рефлексией.

Узкая проверка границ и их композиции с остальными генераторами:

```bash
node games/cards-money-trains/tools/build-operating-turn.mjs --check
node --experimental-strip-types --test \
  games/cards-money-trains/tools/turn-cycle-lifecycle.test.mjs
npm run compile:manifests -- --game cards-money-trains --check
```

## Проверки

`extract-vector-map-review.test.mjs` защищает три свойства: повторяемость
артефактов, субпиксельную калибровку 23 терминалов и непубликационный статус
981 неразмеченного верхнеуровневого контура, происхождение PNG-фона и защиту
от символических/жёстких ссылок. Он также фиксирует особенность Illustrator:
внешнее состояние отсечения открывается в слое карты, а закрывается уже в
следующем слое PDF.

Целевой запуск всех проверок аннотаций карты:

```bash
npm run verify:map-annotations
```

Проверки XLSX и PDF выполняются отдельной дешёвой командой
`npm run verify:cards-money-trains-content`, потому что импорт содержимого не
является геометрической аннотацией карты.

`real-content-mapping.test.mjs` перекрёстно проверяет уже извлечённые реальные
материалы, не создавая второго справочника: номера терминалов из описаний
стран, грузов, начальной сети и векторной карты должны совпадать. Тест также
доказывает полное покрытие 62 грузов новостями № 1–10 и фиксирует единственный
груз, маршрут которого уже совпадает с одной начальной дорогой.

Целевой запуск этой проверки:

```bash
node --test games/cards-money-trains/tools/real-content-mapping.test.mjs
```

## Первый replay на реальных данных

`real-operating-turn.test.mjs` проводит через обычный серверный исполнитель
ограниченный сценарий с реальными идентификаторами автора: новостью № 24,
грузом `1 → 9`, дорогой `1 ↔ 9` и новостью № 11. Положительная ветвь
проверяет выплату `13 + 3`, тариф 2 монеты и завершение груза; отрицательная
ветвь доказывает, что закрытая дорога отклоняет движение без частичного
изменения состояния.

Исходные позиции техники, открытость дороги и один технический экземпляр
груза хранятся в
`authoring/fixtures/real-operating-turn.technical.json`. Эта фикстура
проверяется собственной JSON Schema, имеет `publishable: false` и не
утверждает состав обычной партии, кратность физических карт или жизненный цикл
колод. Все технические действия дополнительно проверяют специальный
`fixtureId`, поэтому обычное стартовое состояние не может их выполнить.

Целевой запуск:

```bash
node --experimental-strip-types --test \
  games/cards-money-trains/tools/real-operating-turn.test.mjs
```

## Граница ответственности

Векторный результат относится только к этой игре и остаётся
`review-draft`. Сам инструмент извлечения не назначает
`regionId`/`countryId`, не соединяет линии в полигоны и не создаёт фрагмент
runtime-манифеста. Отдельный генератор уже назначает идентификаторы 10 стран,
связывает их описания с терминалами 1–23 и добавляет каталог. Однако точные
полигоны стран и областей по-прежнему требуют ручной проверки структуры
контуров и потому не готовы для публикации.

Исходный снимок описаний остаётся редакторским материалом и не становится
источником координат. Нормативной для текущего непубликационного игрового
черновика является только воспроизводимая связь, созданная
`build-country-content.mjs`; будущая геометрия должна сохранить эти
идентификаторы.
