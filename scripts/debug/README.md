# Debug Scripts

## Оглавление

- [Назначение](#назначение)
- [Правила использования](#правила-использования)

## Назначение

Каталог содержит один поддерживаемый клиент ручных команд Runtime API. Прежние одноразовые визуальные сценарии удалены: их задачу выполняют проектные навыки `ui-compare` и `ui-from-mockup`, а история расследований остаётся в Git.

## Правила использования

- Запускайте скрипты вручную только для расследования конкретной регрессии.
- Для прямых runtime-вызовов используйте `runtime-command-client.cjs`: он
  сохраняет одноразовый Bearer-токен только в процессе и последовательно
  обновляет версию состояния. Проверки через player-web выполняют BFF-запросы
  внутри страницы Playwright, чтобы защищённая cookie оставалась в браузере.
- Временные результаты сохраняйте в `.tmp/`.
- Если новый сценарий становится обязательной проверкой, добавьте его в соответствующий тестовый набор, а не в этот каталог.
