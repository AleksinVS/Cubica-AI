{
  "name": "@cubica/sdk-shared",
  "version": "0.0.0",
  "private": true,
  "description": "UI building blocks and utilities reused across Cubica SDKs.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo \"TODO: wire up build\" && exit 1",
    "test": "echo \"TODO: add component tests\" && exit 1",
    "lint": "echo \"TODO: add linting\" && exit 1"
  },
  "peerDependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "framer-motion": "^11.1.7"
  },
  "engines": {
    "node": ">=18"
  }
}
