{
  "name": "@cubica/sdk-core",
  "version": "0.0.0",
  "private": true,
  "description": "Networking, session lifecycle and shared contracts for Cubica platform SDKs.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo \"[sdk-core] source-only TS package consumed via src; no build step\"",
    "test": "echo \"[sdk-core] no tests yet (LEGACY-0021: used view-protocol/state half; session half is dead)\"",
    "lint": "echo \"[sdk-core] no lint yet (LEGACY-0021)\""
  },
  "keywords": [
    "cubica",
    "sdk",
    "core"
  ],
  "engines": {
    "node": ">=18"
  }
}
