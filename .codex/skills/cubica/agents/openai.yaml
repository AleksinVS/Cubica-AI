interface:
  display_name: "Cubica Autonomous Development"
  short_description: "Явно включаемая автономная разработка Cubica"
  default_prompt: "Use $cubica only after the user explicitly invokes it, then execute the approved Cubica TSK plan autonomously with bounded subagents and verified acceptance."
