/**
 * Корневой модуль регистрации игр (composition root).
 *
 * Это единственное место в платформе, которое знает о конкретных играх.
 * Импорты запускают побочные эффекты регистрации резолверов
 * в глобальном реестре (game-config-registry).
 *
 * При добавлении новой игры — добавить один import:
 *   import "@/plugins/<gameId>/register";
 */
import "@/plugins/antarctica/register";